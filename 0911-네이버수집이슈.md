# 네이버 플레이스 수집 이슈 및 기술 함정 정리

본 문서는 동네장사 프로젝트의 `작업지시서.md`에 기록된 네이버 플레이스 데이터 수집(스크래핑) 관련 이슈와 차단(수집 불가) 현상에 대한 핵심 내용을 정리한 파일입니다.

## 1. 스크랩 횟수와 IP 차단 이슈 (수집 불가 현상)
**현상:**
특정 키워드(고정 리서치 키워드)를 짧은 시간(약 10분 내외) 안에 3~4번 반복 조회했을 때, **해당 특정 키워드/업체들에 한해서만** `플레이스 상태 데이터를 추출할 수 없습니다`(APOLLO_STATE 파싱 실패, 즉 정상 HTML이 오지 않음) 에러가 발생. 전혀 다른 새 키워드로는 동시에 정상 작동함.

**분석 (차단 원인 가설):**
- 전면 IP 차단이 아니라 **같은 대상을 짧은 시간에 반복 조회할 때 걸리는 국소적·일시적 제한**으로 판단.
- 대상(URL/키워드) 기준 제한일 가능성이 가장 높음.
- Cloudflare의 IP 풀을 사용해 IP가 매번 바뀌더라도, 같은 타겟을 집중적으로 요청하면 네이버 측에서 봇 방어 기제가 작동함.

**방어 설계 및 조치:**
- **KV 캐시 적용 (필수):** 매장 ID 기준 24시간, 키워드 기준 6시간 캐시를 적용하여 실제 네이버로 나가는 요청(fetch)을 최소화. (여러 고객이 조회해도 최초 1건만 요청이 나가므로 반복 조회 차단 회피 가능)
- **수동 테스트 주의:** 관리자가 테스트 목적으로 동일한 항목을 짧은 간격으로 수동 트리거(연타)하는 것을 피해야 함 (차단 후 수분 내 회복되나 위험함).
- Gap 분석 1회당 '내 매장 1 + 상위 14'로 최대 15회 fetch가 발생하므로 Promise.all로 병렬 처리하되, 캐시 없이 운영하면 네이버 차단이 확정적임.

## 2. 통합검색 위젯의 한계 및 봇 차단 (진짜 순위 누락)
**현상:**
데스크톱/모바일 통합검색의 "플레이스" 위젯 HTML을 파싱할 때, 틈새/저경쟁 키워드 등에서 실제 네이버 지도 순위 10위 이내에 있는 매장이 위젯에 아예 안 잡히는 현상 발생 (위젯 자체에 오가닉 업체가 6개 등 소수만 노출됨).

**원인:**
- 통합검색 위젯은 전체 리스트가 아닌 상위 소수(보통 3~7개)만 보여주는 **미리보기 위젯**에 불과함.
- 전체 리스트를 보려면 '지도에서 더보기'에 해당하는 클라이언트사이드 API(`map.naver.com/p/api/search/allSearch`)를 호출해야 하나, 이를 봇(Worker)에서 직접 호출하면 **`ncaptcha`(네이버 봇 차단)에 즉시 걸림**.
- 스크립트성 API 요청을 네이버가 강하게 차단하고 있어 우회 시도 시 전면 IP 차단 위험이 매우 큼.

**임시 조치:**
- 위젯에 노출되는 만큼만 수집하고, "상위 10개" 대신 실제로 찾은 개수(`rankSearched`)로 동적 표기하도록 수정. 네이버 검색 결과 개수의 한계를 명확히 고지함.

## 3. DOM 파싱 및 데이터 구조 함정
* **"새로 오픈했어요" 캐러셀 혼입:** 통합검색 위젯 중간에 들어가는 최근 3개월 이내 개업 홍보 캐러셀이 정규식에 걸려 오가닉 순위로 오탐되는 문제(실측 피해 64%). `data-nop_res-doc-id` 속성 마커로 솎아내어 전량 제외 처리함.
* **광고 판별 취약성:** 앵커 주변 '광고' 문자열 유무로 판단 시 일반 리뷰에 포함된 단어까지 오탐해 순위를 망가뜨림. `ico_ad` / `sp_ad` 클래스로만 신뢰도 있게 판별하도록 교체.
* **구버전 필드 잔존 (APOLLO_STATE):** `PlaceDetailBase:{id}` 객체의 필드(예: `businessHours`, `cafeBlogReviewsTotal`)는 네이버 UI 개편 이후 갱신되지 않는 쓰레기 값일 확률이 높음. 반드시 `ROOT_QUERY.placeDetail`(`pd`) 하위의 최신 필드(`newBusinessHours`, `fsasReviews.total`)를 교차 검증하여 참조해야 함.
* **매장 새소식 분리:** `/home` 탭 응답에는 새소식 데이터가 없으며, `/feed` 탭을 별도로 요청하여야 가져올 수 있음. 요청 수 증가를 방지하기 위해 경쟁사 스크랩에는 제외하고 내 매장 조회 시에만 켜도록 구성.
* **KV 캐시 버그 되살림:** 파서 로직을 고쳐서 배포하더라도 KV 캐시에 24시간 동안 구버전(버그) 응답이 남아있어 버그가 재현될 수 있음. 스크래퍼 추출 로직을 바꿀 때마다 반드시 캐시 키 버전을 올려 기존 캐시를 무효화해야 함.
