# 0911 수집 안정화 작업

상태: 사용자 승인 후 운영 배포 완료. 배포 버전 e53d8f53-255a-4b40-9943-d097607c33c8. DB 스키마/스케줄 변경 없음.

## 중단 시 인계
- 사용자 요청: 장애 원인 식별과 중복 요청 억제부터 진행.
- 로컬 검증 완료 후 사용자 요청으로 배포했다. 중단 후 재개 시 아래 운영 검증 결과를 먼저 읽고 중복 배포하지 않는다.
- 기존 미추적 파일은 이전 작업자의 자료이므로 삭제하거나 일괄 커밋하지 않는다.
- 운영 DB, KV, 스케줄은 변경하지 않는다.
- 완료 조건: 차단/시간 초과/파싱 오류 구분, 잘못된 빈 결과 캐시 방지,
  매장 ID 기준 캐시 재사용과 새소식 구분, 동시 요청 병합, 공개 테스트 경로 폐쇄,
  외부 네이버 요청 없는 회귀 검사 및 배포용 빌드 검증.
- 배포 여부와 남은 한계는 검증 후 아래에 기록한다.

## 구현 내용
- `worker/src/collection.ts`: 공통 요청 처리. HTTP 429/403/기타 오류, 네트워크,
  15초 시간 초과, 파싱 실패를 code/stage로 분류한다. 오류 시각과 upstreamStatus를 구조화 로그에 남긴다.
  HTML 본문은 로그에 저장하지 않는다. ncaptcha 스크립트 존재만으로 차단을 단정하지 않는다.
- 동일 인스턴스의 동일 URL 동시 요청 병합, 요청 시작 간격 1초,
  429/403 감지 시 해당 호스트 최소 60초 일시 중단. 자동 즉시 재시도 제거.
- `place-cache.ts`: 입력 별칭→ID 캐시와 ID 기반 데이터 캐시 분리.
  home/feed 캐시 구분. Step 1 상호 조회 후 Step 2 ID 조회가 상세/새소식을 재수집하지 않는다.
  결과 객체를 복제하여 동시 호출의 키워드별 파생값이 서로 섞이지 않게 한다.
- 기존 빈 순위 캐시 무시, 실패 결과 캐시 금지. 상세 기본 상호 없는 응답은 성공으로 취급하지 않는다.
- 고객 API 수집 실패: 503 + code/stage 반환. 고객 입력 실수로 유도하는 추가 문구 제거.
- 공개 `/api/test-*` 네이버 수집 경로는 404로 차단.
- 고객의 순위 기준/등급 산식, DB 저장 구조, 자동 수집 주기는 이번에 변경하지 않았다.

## 검증
- `cd worker; npm run test:collection`: 11개 회귀 검사 통과. fetch를 가짜 응답으로 대체하며 실제 네이버 요청 없음.
- 정상 Step 1→Step 2, ID 재사용, 지표/등급 유지, 동시 병합, 새소식 캐시 구분,
  HTTP 상태 분류, 타임아웃, 빈 데이터 거절, 공개 테스트 경로 차단, 단축링크 확인.
- TypeScript: src/index.ts 진입점, ES2022 + Cloudflare types, noEmit 검사 통과.
- Wrangler `deploy --dry-run --outdir dist/collection-check`: 종료 코드 0. 실제 업로드/배포하지 않음.
  첫 실행 로그 폴더 권한 경고 후 승인된 동일 dry-run 재검사로 완료.
- git diff --check 통과.

## 운영 반영 전 남은 확인과 한계
- 실제 장애 응답을 이번 세션에서 수집하지 않았다. 네이버 차단 원인 확정/해제나 운영 복구 완료를 의미하지 않는다.
- 병합/간격/일시 중단은 동일 Worker 인스턴스 범위다. 분산 인스턴스 전역 중복 방지는 보장하지 않는다.
- cron은 시계열 관측 시점을 보존하기 위해 24시간 고객 캐시로 대체하지 않았다. 공통 전송 계층 보호만 적용한다.
- 새 place:v10 캐시는 기존 v9 캐시와 분리된다. 반영 직후 최초 요청은 다시 수집한다. 순위 정상 캐시는 유지한다.
- 새소식 파싱/수집 실패도 성공으로 숨기지 않고 진단 실패로 처리한다. 실제 정상 매장의 /feed 구조 확인 필요.
- '결과가 정말 없음'과 '구조 변경으로 목록을 못 읽음'을 확증할 응답 표본은 아직 없어서 둘 다 보수적으로 PARSE_CHANGED로 취급한다.
- 다음 운영 확인은 지정 테스트 매장 2058645213만 최소 횟수 사용. 실패 시 연타하지 않는다.
- 배포 전에 현재 운영 버전과 로컬 기준 커밋 37b2dde를 대조하고 롤백 버전을 확보해야 한다.
- 이후 사용자 요청으로 운영 배포를 진행했다. 아래 최신 배포 결과가 이전 미배포 기록보다 우선한다.

## 운영 배포 착수 (사용자 승인: Git 복원 가능하면 즉시 배포)
- 검증된 코드 커밋: b87d6ff
- 배포 직전 운영 버전: 33c572e7-9f21-4527-8f80-9090603d0ef4 (2026-09-11 08:25 UTC)
- 즉시 복원 명령: cd worker; node node_modules/wrangler/bin/wrangler.js rollback 33c572e7-9f21-4527-8f80-9090603d0ef4 --message collection-regression
- Git은 소스 복원용이며 운영 즉시 복원은 위 Cloudflare 버전 사용. DB 스키마 변경 없음.
- 결과: 배포 성공. 현재 버전 e53d8f53-255a-4b40-9943-d097607c33c8.

## 배포 후 운영 검증
- https://dongbiz.com/ : 200, 메인 검색 UI 확인.
- /api/test-html : 404, 공개 테스트 경로 폐쇄 확인.
- 필수 입력 없는 /api/place, /api/gap : 400 정상 입력 검증.
- 지정 매장 2058645213 Step 1 : 200, success=true, 백세보리밥 닭한마리 실제 수집 성공.
- Step 2 '안산 상록구맛집' : 기존 하루 3회 IP 한도에 의해 429. 네이버 수집 단계 전 제한이므로 Step 2 실데이터/공유 저장 검증은 미완료. 우회하거나 연타하지 않았다.
- 배포 실패/회귀 증거는 발견되지 않아 새 버전을 유지했다. 새소식/상세 조회 성공은 이 매장 1건 범위이며 전체 업종이나 모든 키워드 정상화를 의미하지 않는다.
- 롤백 시 위 직전 운영 버전 사용. Git 소스 커밋은 b87d6ff, 운영 버전 복원과 데이터 복원은 별개다.
