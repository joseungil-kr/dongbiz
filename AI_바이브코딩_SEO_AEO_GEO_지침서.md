# AI 바이브코딩을 위한 SEO·AEO·GEO 최적화 지침서 (2026 에디션)

> **개정 이력**: 2026-09-08 전면 개정. 초판의 사실관계 오류 5건을 수정하고, 바이브코딩 특유의 위험 5건을 추가했다. 개정 사유는 §7에 남겼다.
>
> **이 문서를 읽는 법**: 모든 조언에 **근거 수준**을 붙였다. `[검증]`은 공식 문서나 실측으로 확인된 것, `[추정]`은 업계 통설이나 확증이 없는 것, `[효과없음]`은 널리 퍼졌지만 근거가 없거나 이미 무력화된 것이다. **`[추정]`과 `[효과없음]`에 시간을 쓰지 말 것.**

---

## 1. 개요: 하나의 가시성 레이어

SEO·AEO·GEO는 **별개의 최적화 채널이 아니다.** 마케팅 용어로는 나뉘지만, 실제로는 대부분 같은 뿌리에서 나온다.

- Google AI Overviews는 **기존 검색 랭킹 위에 얹혀** 동작한다.
- ChatGPT Search·Perplexity의 인용도 **자체 검색 인덱스에서의 순위**가 1차 관문이다.

즉 **"AEO 전용 작업", "GEO 전용 작업"이라는 것은 대체로 존재하지 않는다.** 검색에서 잘 잡히지 않는 페이지는 AI도 인용하지 않는다. 이 프레임을 잘못 잡으면 스키마 마크업만 잔뜩 붙이고 정작 콘텐츠가 없는 페이지를 만들게 된다.

| 구분 | 실제로 하는 일 | 근거 수준 |
|---|---|---|
| **SEO** | 크롤링·색인 접근성 확보. **모든 것의 전제** | `[검증]` |
| **AEO** | 질문에 즉답하는 문장 구조. 스니펫·음성 답변 대응 | `[검증]` 구조 자체 / `[추정]` 전용 최적화 효과 |
| **GEO** | LLM 인용 유도. 실제로는 **SEO + 독창적 데이터**의 결과 | `[추정]` |

**핵심 지시어 하나로 줄이면**: *"JS 없이도 본문이 보이게 SSR로 만들고, 질문에 바로 답하는 문장을 상단에 배치해 줘."*

---

## 2. 개발 우선순위 (이 순서를 바꾸지 말 것)

1. **[1순위] SSR / 정적 HTML로 본문 노출** `[검증]`
   대부분의 AI 크롤러(GPTBot, PerplexityBot 등)는 **JavaScript를 실행하지 않는다.** CSR로만 그려지는 페이지는 AI 입장에서 본문이 존재하지 않는다. 이것이 실무에서 가장 흔하고 가장 치명적인 병목이다.

2. **[2순위] 읽을거리 확보 (콘텐츠 볼륨)** `[검증]`
   히어로 문구와 입력 폼만 있는 페이지는 검색엔진이 주제를 판단할 재료가 없다. 사용 방법·FAQ·용어 설명 등 **사람이 읽을 본문**을 먼저 채운다. 스키마보다 이게 먼저다.

3. **[3순위] Answer-First 문장 구조** `[검증]`
   제목 바로 아래에 질문의 답을 먼저 쓰고, 근거는 뒤에 붙인다.

4. **[4순위] 구조화 데이터(JSON-LD)** `[추정]`
   기계가 파싱하기 쉬워지는 건 사실이나, **구글은 구조화 데이터가 랭킹 요인이 아님을 반복해서 밝혔다.** 1~3번을 끝낸 뒤에 하는 마무리 작업이지, 출발점이 아니다.

---

## 3. 프롬프트 & 코드 템플릿

### A. Answer-First 구조 (AEO)

**🗣️ 프롬프트 예시**
> "FAQ 섹션을 만들어 줘. 질문은 h3, 답변은 바로 아래 p 태그에 **공백 포함 150~200자**로 핵심부터 먼저 쓰고, 부연은 그다음 문단에 분리해 줘. FAQPage JSON-LD도 함께 출력해 줘."

**⚠️ 길이 기준 정정**: 흔히 인용되는 **"50단어"는 영어권 기준**이다. 한국어는 단어 수 개념이 달라 그대로 적용되지 않는다. **공백 포함 150~200자**로 잡는다. `[검증]`

**⚠️ FAQ 리치 결과에 대한 기대치**: 구글은 **2023년 8월 FAQ 리치 결과를 정부·의료 등 권위 있는 사이트로 제한**했다. 일반 상업 사이트는 `FAQPage` 마크업을 넣어도 **검색결과에 아코디언이 표시되지 않는다.** `[검증]`
그럼에도 FAQ를 만드는 이유는 리치 결과가 아니라 **본문 콘텐츠가 생기기 때문**이고, 구조화 데이터는 AI가 Q&A 쌍을 정확히 파싱하는 데 도움이 된다. **목적을 혼동하지 말 것.**

```html
<section id="faq-ssr">
  <h3>AI 크롤러를 위해 SSR이 꼭 필요한가요?</h3>
  <p class="answer-summary">
    <strong>필요합니다.</strong> GPTBot·PerplexityBot 등 대부분의 AI 크롤러는 JavaScript를
    실행하지 않기 때문에, 클라이언트에서 그려지는 본문은 크롤러 입장에서 존재하지 않는
    것과 같습니다. 최소한 핵심 본문만이라도 서버가 HTML로 내려줘야 합니다.
  </p>
  <p class="answer-detail">부연 설명…</p>
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [{
    "@type": "Question",
    "name": "AI 크롤러를 위해 SSR이 꼭 필요한가요?",
    "acceptedAnswer": {
      "@type": "Answer",
      "text": "필요합니다. 대부분의 AI 크롤러는 JavaScript를 실행하지 않아 클라이언트에서 그려지는 본문을 읽지 못합니다."
    }
  }]
}
</script>
```

### B. 독창적 데이터 노출 (GEO)

인용을 유도하는 것은 스키마가 아니라 **다른 곳에 없는 데이터**다. 자체 측정값, 실측 통계, 시계열처럼 **원본이 우리뿐인 정보**가 있어야 인용될 이유가 생긴다. `[추정]` — 상관은 관측되나 인과는 확증되지 않음.

**🗣️ 프롬프트 예시**
> "우리가 직접 측정한 데이터를 table로 출력하고, caption에 측정 기준일과 표본 수를 명시해 줘. 수치는 내가 준 값만 쓰고 임의로 만들지 마."

```html
<article>
  <h1>2026년 3분기 업종별 리뷰 수 실측</h1>
  <p>측정 기준일: <time datetime="2026-09-08">2026년 9월 8일</time> · 표본 120개 매장</p>

  <table>
    <caption>업종별 1페이지 진입 리뷰 수 중앙값 (자체 수집, n=120)</caption>
    <thead>
      <tr><th scope="col">업종</th><th scope="col">중앙값</th><th scope="col">표본</th></tr>
    </thead>
    <tbody>
      <tr><td>한식</td><td>340건</td><td>52</td></tr>
      <tr><td>미용실</td><td>180건</td><td>41</td></tr>
    </tbody>
  </table>
</article>
```

**스키마에 대한 주의** `[효과없음]`
- `Article`의 `citation` 속성은 실존하지만 **구글이 랭킹에 사용한다는 근거가 없다.**
- `about`에 `Dataset`을 중첩하는 것은 **잘못된 사용법**이다. Dataset 마크업은 Google Dataset Search용이며, 쓰려면 **독립 타입으로 발행**해야 한다. 일반 상업 페이지에는 대개 불필요하다.
- **저자(EEAT) 정보**: EEAT는 랭킹 신호가 아니라 **평가자 가이드라인의 개념**이다. 스키마에 저자를 적는다고 신뢰도가 생기지 않는다. 실제로 작동하는 것은 그 저자의 **외부 평판**이다. 저자 표기는 사용자를 위해 하는 것이지 알고리즘을 위한 것이 아니다.

---

## 4. 🚨 바이브코딩 특유의 위험 (초판 누락분)

AI에게 페이지 생성을 맡기면 **사람이 손으로 만들 때는 생기지 않던 사고**가 난다. 아래 5가지는 실제로 발생한 사례다.

### 4.1 대량생성 콘텐츠 `[검증]` — 가장 큰 위험
템플릿 페이지를 수백 장 찍어내는 것이 너무 쉬워졌다. 구글의 **scaled content abuse** 정책은 "가치 추가 없이 대량 생성된 페이지"를 직접 겨냥한다.

**방어 규칙**
- **발행 조건을 코드에 박을 것.** 데이터가 빈약한 페이지는 아예 생성하지 않거나 `noindex` 처리한다.
- 보일러플레이트 비율을 낮춘다. 숫자만 바뀌고 문장이 전부 같으면 위험하다.
- **페이지 수를 욕심내지 말 것.** 수십~수백 장은 버티지만 수천 장은 위험이 급격히 올라간다.

### 4.2 중복 URL `[검증]`
입력 변형만큼 페이지가 생긴다. 띄어쓰기·대소문자·인코딩·쿼리 파라미터 순서가 각각 다른 URL이 되어 **같은 내용이 여러 번 색인**된다.

**방어 규칙**: 정규화 함수를 만들어 대표 표기 하나만 발행하고, 변형으로 들어온 요청은 `<link rel="canonical">`로 대표 URL을 가리키게 한다.

### 4.3 고아 페이지 `[검증]`
사이트맵에는 있는데 **어디서도 링크되지 않는** 페이지가 양산된다. 사이트맵은 발견 수단일 뿐이고, 내부 링크는 **권위 전달 통로**다. 고아 페이지는 재크롤링 우선순위에서도 밀린다.

**방어 규칙**: 자동 생성 페이지는 반드시 **허브 페이지**를 함께 만들고, 그 허브를 홈이나 푸터에서 링크한다.

### 4.4 AI 크롤러 접근 정책 `[검증]`
GEO를 하겠다면서 `robots.txt`를 방치하는 경우가 많다. **인용되고 싶다면 AI 크롤러를 허용해야 한다.**

주요 크롤러: `GPTBot`(OpenAI), `OAI-SearchBot`(ChatGPT 검색), `ClaudeBot`(Anthropic), `PerplexityBot`, `Google-Extended`(구글 AI 학습 제어)

**결정할 것**: 허용하면 인용 가능성이 생기고, 차단하면 콘텐츠를 지킨다. **둘 다 가질 수는 없다.** 의식적으로 선택하고 그 이유를 문서에 남긴다.

### 4.5 공개 범위의 전략적 판단 `[검증]`
일반 SEO 지침서는 "많이 노출하라"고만 말한다. 하지만 **색인시킨다는 것은 경쟁사에게 데이터를 넘긴다는 뜻**이기도 하다. AI가 요약까지 해서 떠먹여 주므로 노출 비용은 예전보다 커졌다.

**판별 기준**
- 이미 공개된 사실을 모아놓은 것 → 공개해도 잃을 게 없다
- 우리만 축적한 시계열·변화량 → **자산**
- 그 변화의 **원인·해석** → **핵심 자산.** 이걸 공개하면 상품 자체를 무료로 주는 것이다

**"SSR = 색인"이 아니다.** 크롤러가 읽게 하되 색인은 막는 조합(`noindex`)도 선택지다.

---

## 5. 체크리스트

**반드시 할 것** `[검증]`
- [ ] 핵심 본문이 **JS 없이** HTML 소스에 보이는가 (`curl`로 직접 확인할 것)
- [ ] 사람이 읽을 본문이 충분한가 (폼과 히어로 문구만 있지 않은가)
- [ ] 자동 생성 페이지에 **발행 조건**이 걸려 있는가
- [ ] 중복 URL이 canonical로 통합되는가
- [ ] 자동 생성 페이지가 **내부 링크로 연결**되어 있는가
- [ ] `robots.txt`의 AI 크롤러 정책을 **의식적으로 결정**했는가
- [ ] 공개하면 안 되는 데이터가 색인되고 있지 않은가
- [ ] AI가 **통계·인용구를 임의로 지어내지** 않았는가 (제공한 데이터만 쓰도록 프롬프트에 강제)

**해도 되지만 기대는 낮출 것** `[추정]`
- [ ] JSON-LD 구조화 데이터 — 기계 가독성엔 도움, 랭킹 요인은 아님
- [ ] 시맨틱 태그(`article`, `section`) — **접근성에는 확실히 좋으나 순위 효과 근거는 없음**
- [ ] 저자 정보 표기 — 사용자 신뢰용이지 알고리즘용이 아님

---

## 6. 프로젝트별 정본과의 관계

이 문서는 **범용 지침**이다. 개별 프로젝트에서 실측으로 확인된 내용이 있다면 **그쪽이 우선한다.**

- 동네비즈 프로젝트: `작업지시서.md`의 **§6.1(GEO·pSEO 기획)**과 **§6.2(경쟁사 벤치마킹)**가 정본이다. 특히 §6.1.2b의 공개범위 3단 분리는 위 4.5의 실제 적용 사례다.

---

## 7. 초판 대비 변경 사항 (2026-09-08)

**수정한 오류**

| 초판 내용 | 문제 | 수정 |
|---|---|---|
| "H2 아래 50단어 요약" | 영어권 기준을 한국어에 그대로 적용 | 공백 포함 150~200자 |
| FAQ 스키마로 리치 결과 노출 | 2023년 8월 이후 일반 사이트는 표시 안 됨 | 기대효과를 본문 확보와 AI 파싱으로 재정의 |
| `Article`의 `citation`, `about > Dataset` | 랭킹 근거 없음 / 잘못된 스키마 사용 | `[효과없음]`으로 표기하고 사용법 정정 |
| "저자 정보 명시로 EEAT 확보" | EEAT는 랭킹 신호가 아님 | 외부 평판이 실체임을 명시 |
| SEO/AEO/GEO 3분법 | 별개 채널로 오인하게 함 | 하나의 레이어이며 SEO가 전제임을 명시 |

**추가한 내용**: §4 전체(대량생성·중복 URL·고아 페이지·AI 크롤러 정책·공개범위), 근거 수준 표기 체계, §6 프로젝트 정본 우선 원칙.

**유지한 내용**: SSR 최우선 원칙과 환각 방지 지시는 초판이 정확했으므로 그대로 두되 근거를 보강했다.
