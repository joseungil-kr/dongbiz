# 검색 개선 진행 상태

상태: 52cf0ca 배포 및 운영 Step 1/Step 2 실검증 완료.

## 요청과 구현
- Step 1: 상호 검색은 주소·전화번호가 있는 후보 선택 후 고유 ID로 진단. 전화번호/공유링크/ID 입력 유지.
- Step 2: 네이버 지도 기본 목록을 50개씩 최대 7페이지(350개) 순서대로 확인. 내 매장 ID와 일치하는 위치를 기록.
- ROOT_QUERY의 정렬 배열 및 목록 API 사용. 광고/신규 추천과 구분, 중복 페이지/중간 차단은 확인한 앞부분만 사용.
- search_histories.my_rank와 raw_data에 전체 수집 목록, 출처, 범위, 시각, 종료 이유, 비교 수 저장. 기존 위젯 rank_snapshots와 혼합하지 않음.
- 지도 실패 시 기존 위젯으로 비교할 수 있으나 출처·미완료 표시, 한도 차감 없음. 두 경로 모두 실패하면 오류 반환.
- 수집·저장 실패는 하루 3회 한도 미차감. 기존 KV 한도는 분산 원자적 카운터가 아니므로 동시 요청에 대한 엄밀한 제한은 별도 과제.
- 지역 키워드 추천, 중복 클릭 방지, 이전 결과/공유 링크 초기화, 수집 시각·실제 비교 수·범위 표시 개선.

## 조사 근거와 제한
- 기존 미추적 getPlaceList_new.ts는 운영과 연결되지 않은 초안. 목록 HTML의 start 변경은 무시되어 같은 첫 페이지를 반환함.
- 네이버 실제 페이지 번들의 getRestaurantsPcmap 작업과 pcmap-api.place.naver.com/graphql을 확인. 다음 50개가 별도 반환되는 것을 검증.
- 안산 상록구맛집 실측: 서로 다른 300곳, 301 시작 페이지는 items=[],total=0,sort=null. 앞 페이지 total=13477은 보존하고 전체 수집 완료로 판단하지 않음.
- 대상 ID 2058645213은 이 300곳에 미발견. 이는 전체 순위 밖의 증거가 아님. 네이버 개인화 화면과 같은 순위를 보장하지 않음.
- 403/429에서 반복 재시도하지 않음. 기존 로컬 인스턴스 쿨다운 적용. 서버 출발지에 따른 차단 가능성은 운영에서 별도 확인.
- API/페이지 구조 변경과 모든 업종에 대한 검증은 보장하지 않음. 이번 실측은 음식점 키워드.

## 검증
- 자동 검사 19개 통과: 동일 상호 선택, ID/전화/링크 유지, 327위, 중복·차단·빈 마지막 페이지, 실패 미차감, DB 저장·공유 복원.
- TypeScript 검사 및 Wrangler dry-run 통과, 브라우저 인라인 스크립트 구문 검사 통과.
- 네이버 호출 없는 로컬 브라우저: 후보 선택→350개 중 137위→확인350/비교6/출처·시간·공유링크 표시 정상.
- 재현용 브라우저 서버: worker에서 node tests/preview.cjs (127.0.0.1:8789). 가짜 데이터/로컬 메모리 DB이며 운영 연결 없음.

## 안전과 배포
- 기준 소스 b5a2f28, 이전 운영 코드 b87d6ff.
- 배포 직전 운영 버전 재확인: e53d8f53-255a-4b40-9943-d097607c33c8 (이번 단계 즉시 롤백 대상).
- DB 스키마/크론 설정 변경 없음. 기존 사용자 미추적 파일은 수정·커밋하지 않음.
- 중단되면 git status/log와 이 문서를 먼저 확인. 배포 결과 미기록이면 deployments list로 확인.
- 사용자 제공 토큰은 코드·문서·Git에 저장하지 않음.

## 운영 배포 기록
- 코드 커밋: 52cf0ca
- 신규 운영 버전: b69bccbc-a56d-444e-a7ae-677da2fdf3f7
- 롤백 대상: e53d8f53-255a-4b40-9943-d097607c33c8
- 홈페이지 200, 새 후보 선택 UI 제공 확인. 백세보리밥 검색 200: 충주 19988354 / 안산 2058645213 두 후보의 주소를 반환.
- Step 2 운영 실측: HTTP 200, 36초, 네이버 지도 서로 다른 300곳, 비교 6곳, myRank=null, stopReason=end, complete=false, total=13477.
- 공유 ID lyetkxex, /api/history 복원 JSON 전체가 최초 응답과 일치. 원본 전체 목록도 저장되어 향후 재분석 가능.
- 운영 브라우저에서도 충주/안산 후보 주소·전화번호 구분 및 선택 화면 확인.
- 결과 링크: https://dongbiz.com/?shareId=lyetkxex
- 이 결과는 해당 키워드/시점/기본 목록의 관측이다. 모든 검색어에서 350개를 제공한다는 보장은 없음.

## 2026-09-11 후속 점검
- Git HEAD `0b27421`; 운영 코드 커밋 `52cf0ca`, 검증 기록 `a2ac1dd` 유지. Cloudflare 배포 목록의 최신 버전도 `b69bccbc-a56d-444e-a7ae-677da2fdf3f7`로 인계 기록과 일치한다.
- 운영 홈페이지 200 및 후보 선택 UI 포함 확인. `/api/history?shareId=lyetkxex`는 매장 ID `2058645213`, 키워드, 지도 목록 300건, 비교 6곳, `stopReason=end`, `complete=false`를 그대로 복원한다.
- 운영 D1을 읽기 전용 조회한 결과, `rankObservation.source='naver-map'`인 기록은 현재 `lyetkxex` 1건뿐이며 `my_rank=NULL`이다. 따라서 새 지도 목록에서 **실제 발견된 순위의 계산→DB 컬럼 저장→raw_data 복원**은 운영 증거가 아직 없다.
- 정확 상호 키워드 `백세보리밥 닭한마리`로 발견 사례 1회 검증을 시도했으나 네이버 수집 전에 일일 IP 한도 429가 반환됐다. 재시도하지 않았다. 제한 우회 토큰을 새로 받아야 검증 가능하다.
- 모바일 390×844 읽기 전용 점검: 가로 오버플로는 없고 주요 실행 버튼은 52~56px 높이다. 다만 상단 로고·메뉴가 여러 줄로 깨지고, `특정 경쟁사와 1:1로 바로 비교하기` 토글의 실제 높이가 16px라 터치 목표가 작다. 저장 결과의 미발견 문구는 범위·출처·시각과 함께 정확히 표시된다.

### 남은 문제 우선순위
1. **발견 사례 운영 검증**: 정확 상호처럼 대상 ID가 목록에 포함될 검색어 1건으로 계산 순위, `search_histories.my_rank`, `raw_data.rankObservation.items[n]`, `/api/history` 복원이 모두 같은지 확인한다. 코드 변경 전에 해야 할 기준 검증이다.
2. **미발견 원인 분류**: 현재 `not_found_in_range`는 공급자 목록 종료, 350 제한 도달, 검색 조건 차이를 세분하지 않는다. `stopReason=end|limit`와 `complete`, `total`, `category`, `sort`를 조합한 고객/관리자용 원인 코드를 먼저 정의한다. 미발견 자체를 누락이나 전체 순위 밖으로 해석하지 않는다.
3. **업종별 제한 검증**: restaurant 1건 외에 `hairshop`, 일반 `place`를 각각 1건만 검증한다. 403/429에서는 즉시 중단하며 반복·격자 탐색은 하지 않는다.
4. **조회용 저장 구조 결정**: 전체 목록은 JSON에 보존되지만 키워드·매장별 비교 조회는 매번 JSON을 펼쳐야 한다. 실제 조회 화면/질의가 정해지기 전 새 테이블은 만들지 않는다. 필요가 확정되면 원본 `raw_data`를 보존한 채 파생 인덱스 테이블을 추가하고 재생성 가능한 마이그레이션으로 설계한다.
5. **모바일 UX 소규모 수정**: 헤더 줄바꿈과 1:1 토글 터치 영역을 먼저 고친다. 오류·미발견 안내는 이미 본문 상태 영역과 결과 메타에 있으므로 새 UI를 늘리기보다 상태별 문구만 세분한다.

## 2026-09-11 저장수 비교 구현
- 사용자 결정으로 비교 업체 수는 상위 6곳을 유지한다.
- 음식점 지도 수집에서 내 매장 저장수가 있고 경쟁사 공개 저장수가 6곳 중 최소 5곳일 때만 방사형 차트를 5축으로 표시한다. 그 외 업종·표본 부족·내 값 비공개는 기존 4축을 유지한다.
- 저장수 `null`은 0으로 바꾸지 않고 평균·중앙값에서 제외한다. 차트 아래와 저장수 비교 카드에 `확인 N/6곳`을 표시한다. 저장수는 네이버 공개 구간의 하한값이라 정확한 누적값으로 표현하지 않는다.
- 기존 저장 결과에는 `stats.saveCount`가 없으므로 공유 링크 복원 시 자동으로 4축을 유지해 하위 호환한다. DB 스키마·크론·비교 업체 수 변경 없음.
- 로컬 검증: 외부 호출 없는 회귀 검사 19개, TypeScript noEmit, 브라우저 인라인 스크립트 구문, Wrangler dry-run 통과.
- 1차 배포 `6c75326c-2afe-4e4e-a21c-397ddb75b23b` 후 운영 확인에서 개별 상세의 저장수가 음식점 표본 6곳 모두 null임을 확인했다. 저장수는 다른 정렬 목록이 아니라 **현재 기본순위 API의 동일 행 `saveCount`**를 함께 받아 붙이도록 수정하고 지도 목록 캐시를 v2로 올렸다. 순위 모집단은 그대로다.
- 도배솔루션/안산시 도배 운영 검증: placeId `1425223157`, 지도 목록 18위, 확인 300곳·비교 6곳. 응답 순위와 D1 `my_rank`, 공유 복원이 모두 18위로 일치했다. 일반 업종은 저장수 표본 0이라 4축 유지.
- 백세보리밥 닭한마리 정확 상호 키워드는 비교 업체가 없어 404로 종료됐으며 재시도하지 않았다. 제공된 우회 값은 검증 요청에만 사용했고 저장·문서화하지 않았다.
- 보완 코드 커밋 `2a83943`, 운영 버전 `252549dd-af23-44e6-8c9f-f61c64eb82eb`, 롤백 대상 `6c75326c-2afe-4e4e-a21c-397ddb75b23b`.
- 음식점 실검증: 안산 상록구맛집 기본 목록 1위 업체를 대상으로 순위 1위, 경쟁사 5곳 중 저장수 공개 5곳, 내 저장수 100+, 평균 하한 7,440을 확인했다. 결과 화면은 5축 조건을 충족하고 저장수 카드·`공개된 5/5곳 기준` 문구가 표시된다. 공유 복원도 일치한다.

## 2026-09-11 Step 2 진행 표시
- 30~60초 동안 버튼 문구만 반복돼 멈춘 것처럼 보이는 문제를 수정했다. 입력 카드 안에 계속 움직이는 진행 막대와 `검색은 정상 진행 중`, 예상 확인 구간(1~50, 51~100…), 경과 초를 표시한다.
- 현재 API는 완료 시 JSON을 한 번 반환하므로 실제 서버 진행률을 알 수 없다. 가짜 정밀 퍼센트 대신 서버의 50곳 단위·약 5초 간격에 맞춘 값을 **예상 구간**으로 명시한다. 완료·오류 시 표시와 타이머를 즉시 제거한다.
- 외부 호출 없는 회귀 검사 19개, TypeScript, 인라인 스크립트 구문, Wrangler dry-run, diff 검사 통과. DB·API 응답·수집 로직 변경 없음.
- 코드 커밋 `a09779d`, 운영 버전 `b573d308-2ee0-44eb-9922-e0a4ba2a8710`, 롤백 대상 `252549dd-af23-44e6-8c9f-f61c64eb82eb`. 운영 홈페이지 200 및 진행 영역·정상 진행 문구·예상 구간·모션 감소 설정 반영 확인.

## 2026-09-11 추가 수집지표 고객 노출
- Step 1에 네이버 예약·스마트오더 연동, 발행 쿠폰 수, 방문자 리뷰 첨부 미디어 수·리뷰당 개수를 추가했다. 운영 실측에서 `visitorReviewMediasTotal=1,959`, 방문자 리뷰 880으로 미디어 수가 리뷰 수보다 클 수 있음을 확인했다. 따라서 “사진 첨부 리뷰 건수/비율”이 아니라 **영수증·예약 방문자 리뷰에 첨부된 사진·영상 총개수**로 해석한다. 블로그·카페 리뷰는 제외한다.
- Step 2에 방문자 리뷰 1건당 첨부 미디어 개수의 내 매장/상위 평균/6위 비교 카드를 추가했다. 방문자 리뷰가 있는 업체만 표본에 포함한다.
- 상위 경쟁사 좌표가 있을 때 결과 출처 문장에 평균 직선거리를 추가한다. 원본 좌표는 공개하지 않는다.
- 저장수 5축은 기존 조건부 구현을 그대로 유지한다. 추가 네이버 요청, DB 스키마, 비교 업체 수 변경 없음.
- 최종 교정 코드 `5d96861`, 운영 버전 `11d6fc96-514e-43dc-90ae-2fd40015708b`, 롤백 대상 `6b430a94-7b4e-488a-ad69-c27e19281384`. 운영 재검증: 내 매장 미디어 1,959개/방문자 리뷰 880건=`리뷰당 2.23개`, 경쟁사 5곳 평균 1.1개, 평균 직선거리 2.9km. 구버전 비율 필드는 응답에서 제거됨.

## 2026-09-12 서비스명 변경: 동네장사
- 고객 화면, SEO 제목·구조화 데이터, 가이드·관리 화면, 이메일 발신명, Apps Script 발신명, 공유 미리보기 이미지를 `동네장사`로 통일했다. 기존 운영 도메인 `dongbiz.com`과 Worker·D1·KV 식별자는 연결 안정성을 위해 변경하지 않았다.
- 기존 공유 이미지의 상단 및 휴대폰 화면 안의 상호도 `동네장사`로 교체하고 1200×630 PNG 규격으로 배포했다.
- 코드·문서·이미지 커밋: `6326b72`. 회귀 검사 19개와 Wrangler 배포 사전 번들 검증 통과.
- 운영 배포 버전: `6489935f-36f4-4f04-8703-9a3de4d505b4`. 롤백 대상: `11d6fc96-514e-43dc-90ae-2fd40015708b`.
- 운영 확인: 홈·순위 페이지 HTTP 200, HTML의 `동네장사` 표기 확인 및 기존 상호 미검출, `/og-image.png` PNG 200.
