# [통합 작업지시서] 동네장사 (dongbiz.com) - 네이버 스마트플레이스 실시간 갭(Gap) 분석 SaaS

> **최종 갱신**: 2026-09-07
> **문서 성격**: PRD 겸 **세션 인계용 단일 진실 소스(Single Source of Truth)**
> **작업 환경**: VS Code(Claude Code) ↔ Antigravity 병행. 한쪽 세션이 소진되면 다른 쪽에서 이 문서만 읽고 즉시 이어받는다.

---

## 0. 이 문서 사용법 (핸드오프 프로토콜)

두 개의 IDE를 오가며 작업하므로, 작업의 연속성은 전적으로 이 문서에 달려 있다. 아래 규칙을 반드시 지킨다.

### 0.1 세션을 시작할 때
1. 이 문서를 **처음부터 끝까지** 읽는다.
2. `§4 현재 코드베이스 실제 상태`로 현재 무엇이 동작하고 무엇이 깨져 있는지 파악한다.
3. `§10 작업 로그`의 마지막 줄을 확인해 직전 세션이 어디서 멈췄는지 확인한다.
4. `§6 작업 항목`에서 체크되지 않은 최상단 항목부터 착수한다.
5. 코드를 수정하기 전에 `§7 기술 함정`을 확인한다. 여기 적힌 실수는 이미 한 번 겪은 것들이다.

### 0.2 세션을 끝낼 때 (필수)
1. `§6 작업 항목`의 완료된 체크박스를 `[x]`로 갱신한다.
2. `§10 작업 로그`에 **한 줄** 추가한다. (날짜 / 환경 / 한 일 / 다음 할 일 / 막힌 점)
3. 새로 발견한 네이버 파싱 함정이나 삽질은 `§7`에 추가한다. 이걸 안 적으면 다음 세션이 같은 삽질을 반복한다.
4. 작업이 중간에 끊긴 파일이 있다면 `§10`의 "다음 할 일"에 파일 경로와 줄 번호를 명시한다.

### 0.3 절대 하지 말 것
- `§8 확정된 의사결정`에 적힌 내용을 다시 논의하거나 뒤집지 않는다. 이미 결론이 난 사안이다.
- `§8 보류 항목`을 임의로 구현하지 않는다. 방향이 확정되지 않은 영역이다.
- 이 문서를 갱신하지 않은 채 세션을 끝내지 않는다.

### 0.4 환경 정보
```
프로젝트 루트 : C:\project\대행사 에이젼시 기획
프론트엔드    : worker/public/index.html (Worker가 정적 서빙, /api/* 외 요청 자동 처리)
백엔드        : worker/  (Cloudflare Workers + Hono + D1 + KV)
로컬 실행     : cd worker && npm run dev   (wrangler dev, http://localhost:8787)
배포          : cd worker && npm run deploy
D1 스키마 적용 : cd worker && npx wrangler d1 execute dongbiz-db --file=./schema.sql
```
※ Git 저장소 활성화됨 (Phase 0 완료). 세션 종료 전 반드시 `git add -A && git commit` 할 것 — 두 IDE 간 유일한 안전장치다.

**테스트/디버깅용 매장·키워드**: 백세보리밥 닭한마리(placeId `2058645213`) + 키워드 "안산 상록구맛집". **필두스카이(`1785101394`)는 테스트 샘플로 쓰지 말 것** (사용자가 명시적으로 금지함).

---

## 1. 프로젝트 개요 및 브랜드 아이덴티티

- **공식 서비스명**: 동네장사 (DongJangsa)
- **공식 도메인**: `dongbiz.com`
- **핵심 가치 제안**
  - "전화번호 하나로 우리 매장의 네이버 플레이스 현주소를 1초 만에 진단"
  - "상위 10개 업체 평균과의 결정적 스코어 차이(Gap)를 시각화하여 A/B/C 등급으로 제공"
  - "일회성 진단을 넘어, 매일 경쟁사와 내 순위를 추적해주는 구독형 SaaS"
- **수익 구조 (우선순위 순)**
  1. **대행 수주** (고단가·즉시 현금) → `interpiad.com` 등 기존 대행사 비즈니스로 연결
  2. **SaaS 구독** (저단가·리텐션) → 순위 추적 및 경쟁사 감시

---

## 2. 확정된 3단계 고객 동선

> 이 3단계는 **확정**이다. 구조를 변경하지 않는다.

### [1단계] 내 매장 진단 — 거울 보기
- 메인 페이지에는 검색창 하나만 배치한다. (구글/토스 스타일)
- **통합 입력 지원**: 일반 전화번호, 스마트콜(0507), 상호명, 플레이스 고유번호(7~12자리), 공유 링크(`naver.me`) — 어떤 형태든 자동 해석하여 타겟 매장을 확정한다.
- **로딩 연출**: 스피너 대신 `"영수증 리뷰 분석 중..."`, `"블로그 언급량 추출 중..."` 등 구체적 텍스트를 순환 노출하여 기대감을 높인다.
- **결과**: 내 매장의 상세 지표를 요약본이 아닌 **전체 로우 데이터(Raw Data)로 길고 자세하게** 노출한다. 시스템의 전문성을 어필하는 구간이다. *(2026-09-07 재확정: 사용자 판단으로 **고객 화면에서는 삭제**했다 — GUI 카드로 이미 충분히 상세하고, Raw Data 텍스트 블록은 오히려 정보과부하로 판단됨. 관리자는 `/admin/:shareId`에서 여전히 원본 JSON을 볼 수 있으므로 검증 목적은 그쪽으로 이관됨.)*
- **이 단계는 전면 공개한다.** 여기서 신뢰가 생겨야 2·3단계로 넘어간다. 리드 게이트를 걸지 않는다.

### [2단계] 키워드 기반 Gap 분석 — 적을 알기
- **"상위노출을 원하는 키워드를 입력하세요"** 입력창을 노출한다. 사용자가 **직접 입력**한다.
  - 입력 부담을 줄이기 위해 업종 기반 추천 칩 3개 정도를 입력창 위에 보조로 제시한다. (예: `안산 스카이차`, `상록구 스카이차`, `안산 사다리차`)
  - 기존의 카테고리 하드코딩 자동 생성 로직은 **폐기**한다.
- **상위 업체 수집**: 해당 키워드의 오가닉 순위 **1~14위**를 수집한다.
  - 평균 계산은 **상위 10개 기준**.
  - **내 매장이 10위 안에 있으면 본인을 제외한 9개**의 평균을 사용한다.
  - 순위 표기는 14위까지 정확히 한다. ("10위 밖"보다 "현재 12위"가 훨씬 강한 자극이다.)
- **비교 기준을 두 개 제시한다. 이것이 이 서비스의 핵심 차별점이다.**

  | 기준 | 값 | 역할 |
  |---|---|---|
  | 상위 평균(또는 중앙값) | 87건 | 현실 인식 — "내가 얼마나 밀렸나" |
  | **10위 업체 실측값** | **41건** | **행동 목표 — "얼마만 하면 되나" (1페이지 진입선)** |

- **표현 방식**: "역전까지 34년 소요" 같은 절망 유발 숫자는 **사용하지 않는다.** 사장님을 포기하게 만든다.
  ```
  블로그 리뷰
    내 매장        12건
    상위 평균      87건   ← 현실 인식
    10위 업체      41건   ← 1페이지 최소 진입선

    → 29건만 채우면 1페이지 진입권입니다.
       월 10건 × 3개월 = 진입선 돌파
  ```
  이 "부족분 29건"은 3단계 상품 수량(체험단 10건/20건 팩)과 그대로 곱해져 **견적이 자동으로 산출**된다.
- **시각화**: 방사형 차트로 내 매장 vs 상위 평균 대조. A/B/C 등급은 절대값이 아닌 **상위 평균 대비 백분율** 기준.

### [3단계] 개별 컨설팅 — 결핍별 처방전
- 부족한 항목마다 해결 상품 버튼을 **1:1로** 붙인다. 진단 항목이 곧 상품 SKU다.
- **매핑표** (구현 시 JSON 상수 1개로 관리. D1 테이블화는 나중에)

  | 결핍 항목 | 판정 기준 | 처방 상품 |
  |---|---|---|
  | 블로그/카페 리뷰 | 10위값 미만 | 블로그 체험단 / 배포형 리뷰 / 상위노출 원고 |
  | 영수증(방문자) 리뷰 | 10위값 미만 | 리뷰 이벤트 세팅 / 리워드 미션 |
  | 키워드 투표수 | 10위값 미만 | 키워드 투표 리워드 트래픽 미션 |
  | 등록 사진 수 | 10위값 미만 | 매장 사진 촬영 대행 |
  | 대표키워드 5개 미달·설명문 미포함 | 이미 구현됨 | 플레이스 세팅 대행 |
  | 톡톡/스마트콜 미연동 | boolean | **무료 셀프 가이드** |
  | 최근 소식 없음 | 30일 초과 | 소식 자동발행 대행 |
  | 영업시간·찾아오는길·편의시설 누락 | boolean | **무료 셀프 가이드** |

- **무료 가이드를 최소 2개 섞는다.** 전부 유료 버튼이면 광고 페이지로 읽힌다. 사장님이 5분 만에 직접 고치고 성취감을 느끼게 만든 뒤 유료로 전환시킨다.
- **정렬 순서는 결핍이 큰 순이 아니라 "싸고 빨리 되는 것" 순**이다. 톡톡 연동(무료·5분)이 맨 위, 리뷰 500건(고액·6개월)이 맨 아래. 첫 액션이 쉬워야 두 번째를 산다.

---

## 3. 핵심 엔진 기술 스펙

### 3.1 통합 입력 리졸버 (Universal Input Resolver)
사용자가 어떤 값을 넣어도 에러 없이 매장 ID를 찾아낸다.
- `naver.me/XXX` → `redirect: 'manual'`로 Location 헤더 추적 후 ID 추출
- 7~12자리 숫자 (`/^[1-9]\d{6,11}$/`) → 플레이스 ID로 간주. 0으로 시작하는 일반 전화번호는 이 분기를 타지 않는다.
- `place/` 포함 URL → 정규식으로 ID 추출
- 그 외(전화번호/상호명) → 네이버 통합검색 호출 후 최상단 업체 ID 매칭

### 3.2 오가닉 순위 및 지표 추출 엔진
- **순위 추출**: 광고를 배제하고 `search`(상단 비주얼 카드) 및 `entry`(하단 피드 리스트) URL 패턴을 모두 수집. → 함정은 `§7.4`, `§7.5` 필독.
- **지표 추출**: 개별 플레이스 페이지의 `window.__APOLLO_STATE__`를 파싱하여 리뷰 수, 톡톡 활성화 여부, 사진 총 개수, 키워드 투표수 등을 수집. → 파싱 규칙은 `§7.1`, `§7.2` 필독.

### 3.3 API 구조 (⚠️ 현재 구현과 다름 — 변경 대상)
확정된 동선상 1단계 결과를 보여준 **뒤에** 키워드를 입력받으므로, 요청은 반드시 2번으로 나뉜다.

| API | 입력 | 하는 일 | 응답 |
|---|---|---|---|
| `GET /api/place` | 상호/전번/URL/ID | 내 매장 1건 스크랩 | 1단계 진단 데이터 |
| `GET /api/gap` | `placeId` + `keyword` | 상위 14 스크랩 → 평균/중앙값/10위값 → 등급 → 처방 | 2·3단계 데이터 |

- 1단계 결과는 KV에 캐시하여 2단계에서 내 매장을 재스크랩하지 않는다. 사장님이 키워드를 2~3개 바꿔가며 돌려도 내 매장은 1회만 긁는다.

### 3.4 인프라
- Cloudflare Pages(프론트) + Workers(서버리스 크롤링 API) + D1(관계형) + KV(캐시)
- 진단 리포트는 즉각성이 생명이므로 Edge에서 동적 렌더링 + 캐싱한다.
- **캐시 정책 (필수)**: `placeId` 단위 24시간, `keyword` 단위 6시간. 캐시 없이 운영하면 네이버 IP 차단으로 서비스 전체가 정지한다.

---

## 4. 현재 코드베이스 실제 상태

> 세션을 이어받는 사람은 이 절을 먼저 읽는다. 계획이 아니라 **현재 사실**이다.

### 4.1 파일 지도

| 경로 | 상태 |
|---|---|
| `worker/public/index.html` | **배포 대상 정본 프론트엔드.** 2단계 분리 플로우(진단→키워드 Gap 분석). Tailwind/FontAwesome/Chart.js는 CDN. |
| `worker/src/index.ts` | Hono 라우터. `/api/place`, `/api/gap`, `/api/history`, `/share/:shareId`(동적 OG), `/admin`·`/admin/:shareId`(Basic Auth, 진단 리스트/raw데이터) + `scheduled` 핸들러(cron 헬스체크). KV 캐시 래퍼, `upsertPlace` 포함. |
| `worker/src/scraper.ts` | 스크래퍼. `getOrganicRanking`(1~14위, 중복제거+광고판별 개선), `scrapeFullPlaceMetrics`(B1·B2·B3 수정 완료). |
| `worker/src/notify.ts` | 운영자 알림 2함수: `notify`(텔레그램), `logDiagnosisToSheet`(구글시트). 시크릿 미설정 시 무동작. |
| `worker/.dev.vars.example` | 로컬 dev용 시크릿 템플릿. 실값 넣은 `.dev.vars`는 gitignore 처리됨. |
| `worker/wrangler.toml` | `[assets]` 정적서빙 + D1/KV **실제 바인딩 완료** (`dongbiz-db`, `dongbiz-worker-CACHE`). |
| `worker/schema.sql` | D1 스키마 (구 `scratch/db_schema.sql`을 이관). B8 컬럼(`top10_median_reviews`, `rank_boundary_reviews`) 추가됨. |
| `scratch/` | **제거됨.** 일회성 탐색 스크립트 43개 + 로컬 Node 서버 + 구버전 스크래퍼 전부 삭제. 결론은 `§7`에 보존됨. |
| `package.json` (루트) | **제거됨.** `better-sqlite3`/`uuid`는 `scratch/server.mjs`와 함께 불필요해짐. |
| `dongbiz_hero_preview.html` (루트) | **제거됨.** `worker/public/index.html`로 대체. |

### 4.2 알려진 버그

| # | 위치 | 증상 | 상태 |
|---|---|---|---|
| B1 | `scraper.ts` `directions` | `base.directions`(존재하지 않는 필드) → `base.road` | ✅ 수정됨 |
| B2 | `scraper.ts` `phone` | 스마트콜 전용 매장 phone null | ✅ 수정됨 (`base.phone \|\| base.virtualPhone`) |
| B3 | `isBizHourMissing` 등 | 추측 대신 네이버 실제 필드 `base.missingInfo.*` 사용으로 교체 (5개 항목: 영업시간/메뉴사진/찾아오는길/편의시설/상세설명) | ✅ 수정됨 |
| B4 | `scratch/server.mjs` | `talktalkUrl` 오타 참조 | ✅ 파일 제거로 해소 |
| B5 | `index.ts` fallback 매장 | `'1605910967'` 강제비교 | ✅ 삭제됨 (경쟁사 0명이면 404 응답) |
| B6 | 프론트 `localhost:3000` 하드코딩 | Worker가 프론트까지 서빙하므로 상대경로(`/api/...`)로 교체 | ✅ 수정됨 |
| B7 | 데모 입력값 고정 | `value="https://naver.me/..."` 제거, placeholder만 유지 | ✅ 수정됨 |
| B8 | `search_histories` INSERT | `my_rank` 항상 NULL, `top10_avg_reviews`에 1위 단독값 저장 | ✅ 수정됨 (`my_rank` 채움, `stats.visitorReviewsTotal.{avg,median}` + `boundary` 저장) |
| B9 | `index.ts` `/api/gap`의 `search_histories` INSERT | `place_id`가 `places(id)` FK를 참조하는데, `/api/gap`을 `/api/place` 없이 단독 호출하면(직접 API 테스트, 향후 공유링크 크롤러 등) `places`에 행이 없어 `FOREIGN KEY constraint failed`로 **조용히** 실패(try/catch로 삼켜짐, 응답은 200 유지되지만 히스토리 저장 안 됨). 실제 `wrangler dev` 테스트 중 발견. | ✅ 수정됨. `upsertPlace()` 헬퍼로 분리해 `/api/place`·`/api/gap` 양쪽 진입점에서 호출 |
| B10 | `scraper.ts` `isBizHourMissing` | 실사용자 리포트: 영업시간이 실제 등록돼 있는데도 "누락"으로 표시됨. `base.missingInfo.isBizHourMissing`이 구버전 `base.businessHours`(항상 null)만 보고 판정하는 것으로 추정. §7.8 참조 | ✅ 수정됨. `pd.newBusinessHours` 배열 실측으로 직접 판별 |
| B11 | `scraper.ts` `cafeBlogReviewsTotal` | 실사용자 리포트: 블로그 리뷰 898건인 매장이 0건으로 표시됨. `base.cafeBlogReviewsTotal`이 구버전 필드로 추정. §7.8 참조 | ✅ 수정됨. `pd.fsasReviews.total` 우선 사용 |
| B12 | `scraper.ts` `recentNews` 추출 | `BiznewsItem:`/`NewsItem:` 키가 애초에 존재하지 않는 이름이라 새소식이 항상 null. §7.11 참조 | ✅ 수정됨. `/feed` 탭의 `Feed:` 키 사용 |
| B13 | `scraper.ts` `getOrganicRanking` | "새로 오픈했어요" 캐러셀 업체가 순위 리스트에 섞여 들어감(실측 사례 64% 오염). §7.13 참조 | ✅ 수정됨. `data-nop_res-doc-id` 마커로 전량 제외 |
| B14 | `scraper.ts` `getOrganicRanking` | `<a href>` DOM 스캔 순서가 실제 순위와 다름(1위 업체가 4위로 집계, 무관한 업체 혼입). §7.15 참조 | ✅ 수정됨. 내장 JSON(`PlaceListBusinesses.items`)에서 순서 그대로 추출. 단, 지역명 없는 키워드는 네이버 측 IP기반 위치개인화로 Worker와 실사용자가 다른 결과를 볼 수 있는 잔여 한계 있음(§7.15, 미해결) |
| B15 | `index.ts` `renderRankPageHtml` + `scraper.ts` | 순위 페이지에 **같은 업체가 같은 순위로 두 줄** 표시("상록구 삼계탕"에서 1,1,2,2,3,3위). 한 관측 배치에 같은 `place_id` 행이 중복 저장된 것이 원인 | ✅ 수정됨(2026-09-09). 렌더 단계에서 `dedupeByPlace()`로 배치 내 중복 제거. **원인이 두 겹이었음** — §7.4에 "상위 N개 수집 시 중복 ID"가 함정으로 이미 기록돼 있었고 href 스캔 폴백에는 `seen` 셋이 있었는데, §7.15에서 JSON 우선 추출로 교체하면서 **그 방어를 옮기지 않았다.** 스크래퍼 쪽에도 `new Set()` 복원. **문서에 적어둔 함정을 코드 교체하면서 놓친 사례** |

### 4.3 남은 것
- Phase A, B **완료**.
- Phase C: `interpiad.com` 자동연결·결핍→상품 매핑 JSON·처방 카드 UI는 **폐기/보류**(2026-09-07 방향전환, §6·§10 참조 — 벤더 네트워크 부재로 결핍→상담 흐름으로 대체). 아래는 **완료**:
  - 상담 유도 CTA(전화번호 난독화 + PDF 리포트 미끼) + `GET /api/lead-click`(클릭 로깅)
  - 경쟁사 직접 지목 1:1 비교(`/api/gap?competitorId=`)
  - 운영자 본인용 일일한도 우회(`RATE_LIMIT_BYPASS_TOKEN`)
  - `GET /share/:shareId` — D1에서 매장명/등급/키워드/사진을 읽어 동적 OG 메타 생성, 사람이 열면 `/?shareId=`로 자동 리다이렉트
  - `GET /admin`, `GET /admin/:shareId` — Basic Auth(`ADMIN_USER`/`ADMIN_PASS` 시크릿, 값은 사용자가 직접 지정). 진단 리스트(최신 100건) + 상세 화면(내 매장·경쟁사 top10 raw JSON, 통계 전부 노출). 시크릿 미설정 시 `/admin/*` 전체가 503으로 막힘(인증 없이 열리는 사고 방지)
  - `GET /admin/:shareId/report` — 맞춤개선 PDF 리포트(고객용 진단 화면 그대로 재사용 + 항목별 확정 주석, §Phase C 로그 참조)
- Phase D: `rank_snapshots` 시계열 축적과 `dongbiz.com` 도메인 연결은 **완료**. 나머지(pSEO 자동발행, 순위 하락 알림, 카카오 알림톡 심사, 결제 연동)는 **미착수**

---

## 5. 프론트엔드 현황

- `worker/public/index.html` 단일 파일. Tailwind Play CDN + Font Awesome + Chart.js(radar) + Pretendard.
- **2단계 분리 플로우로 재구성 완료**: 검색 → `/api/place` 호출 → 1단계 결과 노출 + 키워드 입력창 노출 → `/api/gap` 호출 → 2·3단계 결과 노출.
- 구현 완료: 검색창, 로딩 텍스트 순환, 1단계 GUI 지표 카드(누락정보 감사는 네이버 실제 플래그 사용, SEO 지표 각 항목에 (i) 툴팁, 최근소식·설명분량(1,500자 기준) 항목 추가, 지번주소 항목 삭제), 업종 기반 키워드 추천 칩(하드코딩 분기 없음), 방사형 차트(내 매장 vs 상위 10 평균), **"상위 10개 업체 대비 내 위치" 카드**(5단계 등급: 최우수/우수/양호/개선필요/위험 — 10위 실측값 없어도 상위평균 기준으로 등급 표시, "확인불가" 방치 안 함), 결핍 자극 UI(키워드 미스매치 카드는 **구조적으로 항상 오탐이라 삭제** — 대신 개선필요/위험 등급 지표를 자동 요약하는 문장 배너로 대체 / 생명력 신호등 유지, 여기에 찾아오는길·편의시설·상세설명 누락 여부 3항목 추가), 미니홈피 프리뷰, 공유하기 버튼(상단+하단, 클립보드 복사, `/share/:shareId`로 카톡·페이스북 미리보기용 동적 OG 지원), 공유 링크(`?shareId=`) 복원.
- **Raw Data 텍스트 리포트는 고객 화면에서 삭제됨**(2026-09-07). 관리자는 `/admin/:shareId`에서 원본 JSON을 그대로 볼 수 있어 검증 기능 자체는 유지됨.
- **숨김 처리(코드는 남아있으나 화면엔 안 보임)**: 상단 "대행 파트너 문의" 버튼, "진단 리포트 PDF & 알림톡 무료 받기" 리드폼 블록.
- **1일 검색 제한**: `/api/gap`은 IP당 하루 3건까지만 허용(§9.8 참조). 상호 검색(`/api/place`)은 제한 없음 — 경쟁사 스크랩 비용이 큰 쪽만 제한.
- "1위 역전 소요시간" 카드는 제거됨 — 진입선 비교 카드가 그 역할을 대체 (§8.1 확정사항).
- 순위·등급 배지는 헤더 우측이 아니라 방사형 차트 카드 바로 아래, 더 큰 글씨로 배치.
- 리드폼(휴대폰 입력)은 §8.3 보류에 따라 UI만 유지, 클릭 시 "준비중" alert 그대로.
- **Font Awesome은 아이콘 8개만 사용**하면서 전체 CSS(~70KB)와 폰트 파일을 로드한다. 인라인 SVG 또는 이모지 대체 시 초기 로드가 크게 줄어든다. (우선순위 낮음, 미착수)
- Chart.js는 radar 1개만 쓰지만 직접 구현이 더 비싸므로 **유지**한다.
- **UX 세부 다듬기 완료** (사용자 리포트 반영):
  - 검색창 라벨을 "상호 / 전화번호 / 지도공유주소 / 플레이스번호"로 간결화
  - 방문자평점 색상 차등화(4.0 미만 rose / 4.8 이상 lime)
  - 설명 분량 한도 표기 1,500→2,000자로 정정
  - 최근 소식 기준 1개월→2개월로 완화 (SEO Scores 카드, 생명력 지수 둘 다)
  - 매장 누락 정보 감사 카드의 "메뉴 사진"→"메뉴판 이미지"에 이어, SECTION 6 갤러리 라벨도 "샘플 사진(최대5장, 전체 개수는 대표사진 등록수 참고)"로 명확화 — 두 군데서 사진 개수가 다르게 보여 혼란 유발하던 것 정리
  - 톡톡/스마트콜 "연동됨" 배지에 툴팁 추가: 과거 등록 기록 기준이며 현재 미사용이어도 표시될 수 있음을 명시
  - "상위 업체 대비 내 위치" 카드 4개 항목 전부에 툴팁 추가 — 사장님이 실제 앱에서 보는 숫자와 차이날 수 있는 이유(집계 기준 차이)를 명시해 신뢰도 저하 방지
  - 상위평균 표시값 소수점 반올림(정수만 표기, 내부 계산은 그대로 정밀 유지)
  - 매장 생명력 지수: 상세설명 1,000자 미만이면 "부족합니다" 신호 추가(3번째 판정 항목), 종합판정 3단계로 재조정. **타이틀을 "매장 생명력 지수"로 고정**하고 종합상태(양호/경고/위험)는 별도 배지로 분리 — 정상인 개별 항목(예: 톡톡/스마트콜 모두 연동됨)이 종합 경고 타이틀 때문에 문제처럼 보이던 것 해소

---

## 6. 작업 항목 (WBS)

> 위에서부터 순서대로 진행한다. 완료 시 `[x]`로 갱신한다.

### Phase 0 — 작업 연속성 확보 (최우선)
- [x] `git init` + `.gitignore` (`node_modules`, `*.db`, `.wrangler`) — 두 IDE가 같은 폴더를 편집하므로 버전 관리가 유일한 안전장치다
- [x] 최초 커밋 (현재 상태 스냅샷)
- [ ] 원격 저장소 연결 (선택이지만 권장 — 환경 간 이동이 파일 복사에서 `git pull`로 바뀐다). **미수행 — 사용자가 원격 저장소(GitHub 등) 준비 시 연결 필요**

### Phase A — 리포트 신뢰도 확보 (이게 없으면 나머지가 무의미) ✅ 완료
- [x] B1·B2 수정 후 `scratch/full_place_scraper.mjs` + `server.mjs` 제거
- [x] `scratch/*.mjs` 43개 및 `dongbiz.db` 삭제, 루트 `package.json`/`package-lock.json` 제거
- [x] `wrangler.toml`에 정적 자산 서빙 추가 (HTML을 Worker가 서빙 → 로컬 Node 서버 불필요)
- [x] API 2분할: `/api/place` + `/api/gap` (`§3.3`)
- [x] 키워드 직접 입력 UI 추가, 카테고리 하드코딩 분기(`index.ts:39-47`) 삭제, B5 fallback 삭제
- [x] 상위 14위 수집 — **중복 ID 제거**(`§7.4`) + **광고 판별 교체**(`§7.5`)
- [x] 본인 제외 상위 10 **평균 + 중앙값** 계산, **10위 업체값(진입선)** 산출
- [x] 내 순위(1~14위 / 14위 밖) 산출 및 표기
- [x] 프론트: 역전 소요시간 블록을 `§2` 2단계의 3줄 비교(내 매장 / 상위 평균 / 10위값 + 부족분)로 교체 → "진입선 비교 카드"로 구현
- [x] B3(`isBizHourMissing`), B6(localhost 하드코딩), B7(데모 기본값) 정리
- [x] KV 캐시 적용 (placeId 24h, keyword 6h) — `dongbiz-worker-CACHE` 생성 및 바인딩 완료
- [x] D1 생성(`dongbiz-db`) + `database_id` 바인딩 + 스키마 원격/로컬 적용 + B8 컬럼 반영
- [x] `wrangler dev`로 `/api/place`, `/api/gap`, `/api/history` 실제 네이버 데이터 왕복 검증. 검증 중 B9(FK 제약 조용한 실패) 발견 및 수정

### Phase B — 인프라 방어 ✅ 완료 (자격증명 발급 전까지는 무동작)
- [x] `notify(대상, 메시지)` **함수 1개** 추가, 내부는 텔레그램 봇으로 구현 (`worker/src/notify.ts`. 어댑터 계층·설정파일 없음 — 함수 2개(`notify`, `logDiagnosisToSheet`)뿐)
- [x] 파싱 실패 / 네이버 차단 / 신규 진단 발생 시 텔레그램 푸시. **상담 유입은 보류** — 리드폼이 §8.3에 따라 아직 inert(alert만 뜸)라 훅 걸 백엔드 이벤트 자체가 없음. 리드폼이 실제 엔드포인트를 갖게 되면 그때 추가
- [x] 구글시트 진단 로그 append (`/api/gap` 성공 시점에 상호/연락처/키워드/등급/순위 전송). Apps Script 웹훅 URL은 사용자가 준비해서 시크릿에 넣어야 동작
- [x] Cron 헬스체크: `HEALTHCHECK_PLACE_ID`(실제 동작 확인된 매장) 매일 04:00 UTC(13:00 KST) 스크랩, 실패 시에만 텔레그램 알림 (시각은 §9.4에서 순위변동 시간대 이후로 재조정됨)
- [x] `wrangler dev --test-scheduled`로 cron 수동 트리거 검증, 가짜 자격증명으로 notify/sheet 경로가 앱을 깨뜨리지 않음을 확인

**다음 세션에서 자격증명 채우면 바로 작동한다** (코드는 이미 완성, 값만 없음):
1. 텔레그램: [@BotFather](https://t.me/BotFather)에서 `/newbot` → 토큰 발급 → 봇에게 아무 메시지나 보낸 뒤 `https://api.telegram.org/bot<토큰>/getUpdates`에서 `chat.id` 확인
2. 구글시트: Apps Script로 웹훅 만들어 POST 받으면 시트에 한 행 추가하는 스크립트 배포 (`doPost(e)` → `JSON.parse(e.postData.contents)` → `sheet.appendRow(...)`), 배포 URL 확보
3. 배포용: `npx wrangler secret put TELEGRAM_BOT_TOKEN` (`TELEGRAM_CHAT_ID`, `SHEETS_WEBHOOK_URL`도 동일)
4. 로컬 dev용: `worker/.dev.vars.example`을 `worker/.dev.vars`로 복사 후 값 채움 (git 추적 안 됨)

### Phase C — 3단계 처방 + 유통
- [x] **(방향 전환, 2026-09-07 확정)** `interpiad.com` 자동 연결은 폐기. 결핍→상담 흐름으로 선회: CTA 클릭 시 전화번호(010-2490-0555, 조강사) 노출 → 카톡/문자로 사용자가 직접 문의 → PDF 리포트 발송 → 상담/강의·컨설팅 업셀. 이유: 체험단/리뷰이벤트 벤더 네트워크가 아직 없어 자동 견적·자동 매칭을 걸 상품이 없음(§8.3 참고). 현재 실제로 유통 가능한 건 미니홈페이지 트래픽과 파이썬 대량 블로그/카페 포스팅(배포형 리뷰)뿐이며, 후자는 리스크가 달라 별도 프로젝트로 분리
- [x] 상담 유도 CTA 블록 구현 (`worker/public/index.html`) — 결핍 진단 하단, PDF 리포트를 미끼로 카톡/문자 문의 유도. 전화번호는 스팸 스크래퍼 방지를 위해 문자열을 뒤집어 저장(`OBS_PHONE_REV`)했다가 클릭 시에만 복원 — 배포된 HTML 소스에 실제 번호(010-2490-0555)가 평문으로 존재하지 않음을 실배포 후 `grep`으로 확인 완료
- [x] `GET /api/lead-click` 신설(`worker/src/index.ts`) — CTA 클릭 시 조용히 텔레그램 알림 + 구글시트 로그 한 줄만 남김(기존 `notify()`/`logDiagnosisToSheet()` 재사용, fire-and-forget, 항상 200). 카톡/문자는 사이트 밖에서 이뤄지므로 이 로그가 "관심 보인 사람이 있었다"는 유일한 신호. 실배포 후 curl로 200 확인
- [x] **(신규, 2026-09-08 재구현)** 맞춤개선 PDF 리포트 — `GET /admin/:shareId/report`(기존 `/admin/*` Basic Auth 그대로 적용). **1차 구현(요약카드만 별도 렌더링하는 방식)은 사용자 피드백으로 폐기**: "주석만 모아놓은 페이지 말고, 고객이 실제로 보는 진단 화면 그대로에 항목 바로 아래 주석이 붙어있으면 좋겠다"는 요청 → 판정·수치 로직을 서버에 중복 구현하지 않고, `worker/public/index.html`(고객이 실제로 보는 그 페이지)을 `c.env.ASSETS.fetch()`로 그대로 가져와 `<script>window.__REPORT_SHARE_ID__=...</script>` 한 줄만 주입해서 반환하는 방식으로 전환(ladder: 이미 있는 렌더링 재사용, 서버측 중복 판정 로직 전부 삭제). `wrangler.toml`의 `[assets]`에 `binding = "ASSETS"` 추가해 Worker 코드에서 정적 파일을 가져올 수 있게 함. 프론트는 `IS_REPORT` 플래그(`__REPORT_SHARE_ID__` 존재 여부로 판별, 공개 페이지에선 항상 false)가 켜지면 각 실데이터 행(guiAuditTime/Menu/Dir/Amen/Desc, guiScore, guiRepKwdAudit, defiNews/Call/Desc, boundaryCard 4종) 바로 아래에 `REPORT_NOTES` 문구를 `insertAdjacentHTML('afterend', ...)`로 삽입(`addReportNote()` 헬퍼, 행의 `<li>` 또는 `div.flex` 컨테이너를 앵커로 사용). 같은 판정이 두 군데(SECTION7 vs 생명력지수 카드)에 중복 표시되는 항목(찾아오는길/편의시설/상세설명 누락)은 SECTION7 쪽에만 주석을 붙여 중복 방지. 리포트 모드에서는 헤더·2단계 검색폼·공유버튼을 숨기고, 상담CTA 전화번호는 클릭 없이 바로 평문 노출(관리자 본인 문서라 난독화 불필요), 우상단에 "인쇄/PDF로 저장" 버튼 추가(`window.print()`, 라이브러리 없음). CTA 클릭 시 텔레그램 알림에 이 리포트 링크를 자동 첨부하도록 `/api/lead-click`에 `shareId` 파라미터 추가 — 전체 흐름: 고객이 연락처 버튼 클릭 → 텔레그램에 리포트 링크 도착 → 관리자가 열어서 PDF로 저장 → 카톡 발송 → 상담. 실배포 후 인증 없이/오답 인증으로도 401(라우트 존재+인증 게이트+ASSETS 바인딩 배포까지는 확인), `/api/lead-click?shareId=`로 실제 텔레그램 알림에 링크 포함되는 것까지 확인 완료. **관리자 인증정보가 이 세션엔 없어 실제 렌더링(주석 위치, 인쇄 결과)은 사용자가 브라우저에서 직접 확인 필요**
- [x] 공유 링크 **OG 태그** — `GET /share/:shareId` 신설(사용자 피드백으로 예정보다 먼저 구현). D1 조회해 매장명/등급/키워드/대표사진으로 동적 og:title·description·image 생성, 사람이 열면 실제 앱으로 즉시 리다이렉트. 상단·하단 공유하기 버튼(클립보드 복사)도 함께 구현
- [x] 경쟁사 직접 지목 입력창 — `/api/gap?placeId=X&competitorId=Y` 신설, 만능 리졸버(`scrapeFullPlaceMetrics`) 그대로 재사용. 키워드 순위 계산 없이 1:1 지표 비교만 수행(`isDirectCompare:true`), `rank_snapshots`에는 안 남김(키워드 기반 시계열 오염 방지). 프론트는 키워드 입력창 아래 접이식 토글로 노출. 실배포 후 bypass 토큰으로 curl 검증 완료(`targetKeyword:"'필두스카이'와 직접 비교"`)
- [x] **(신규)** 일일 검색 한도(3건/IP) 운영자 본인 우회 — `RATE_LIMIT_BYPASS_TOKEN` 시크릿 + `?bypass=TOKEN` URL 쿼리 파라미터 체크. 공개 UI(버튼, 텍스트)엔 절대 노출 안 하고 프론트 JS가 URL에서만 읽어 API 호출에 자동 첨부. `wrangler secret put`로 등록 완료, 우회 시 200 / 미우회 시 429(한도초과) 둘 다 실배포 후 curl로 확인
- [x] **(신규)** 관리자용 진단 리스트·경쟁사 raw데이터 조회 화면. Basic Auth 채택(사용자 확정, 아이디/비번 직접 지정). `GET /admin`(리스트) + `GET /admin/:shareId`(상세: 내 매장·경쟁사 top10 raw JSON, stats). `wrangler secret`로 자격증명 등록 완료, 실배포 후 인증(401→200)·리스트·상세 페이지 전부 실데이터로 검증 완료

### Phase D — 축적 및 수익화
- [x] pSEO: 키워드별 순위 페이지 자동 발행 — `/rank/:keyword` SSR, `/rank` 허브, 동적 사이트맵, `ItemList` JSON-LD 구현 완료 (§6.1.5)
- [x] Cron 자동 순위 수집 → 시계열 축적 완료 (§9 참조, `rank_snapshots` + `runDailyKeywordCollection`). 예정보다 먼저 구현됨 — 사용자가 리버스엔지니어링을 위해 우선순위를 올림. **월간 리뷰 증가 속도** 등 파생 지표는 시계열이 더 쌓인 뒤 계산 로직 추가 필요(미착수)
- [ ] 순위 하락 알림 (SaaS 해지를 막는 유일한 기능) — 이제 `rank_snapshots`에 데이터가 쌓이므로 트리거 조건(예: 순위 2단계 이상 하락)만 추가하면 구현 가능
- [ ] 카카오 알림톡 발신프로필·템플릿 심사 신청 — **Phase C 시작 시점에 신청을 걸어둔다.** 심사에 1~2주 걸리므로 결정을 미뤄도 리드타임은 줄지 않는다
- [ ] 결제 시스템(포트원 등) 연동
- [x] `dongbiz.com` 도메인 연결 완료 — Cloudflare 대시보드 Workers&Pages > dongbiz-worker > Domains > Add Domain(서브도메인 비워 루트 도메인으로 연결)로 사용자가 직접 연결, `https://dongbiz.com/` 200 응답 확인

### 6.1 GEO·pSEO 기획 (2026-09-08 확정, 착수 전 필독)

#### 6.1.0 이 기획의 출발점 — 검증된 사실과 기각된 통념
외부 GEO 조언 2가지를 검증한 결과를 먼저 기록한다. **이 판단이 아래 설계의 근거이므로 뒤집으려면 여기부터 반박할 것.**

| 통념 | 판정 | 근거 |
|---|---|---|
| "GEO 하려면 JSON-LD·시맨틱 태그로 소스코드를 뜯어고쳐야 한다" | **과장** | 구글은 구조화 데이터가 랭킹 요인이 아님을 반복 확인(리치 결과 노출 자격일 뿐). AI Overviews는 기존 검색 랭킹 위에 얹혀 동작하므로 결국 일반 SEO가 본질. `<article>`/`<section>` 태그가 순위·인용에 영향 준다는 근거는 사실상 없음 |
| "서치콘솔 등록을 미뤄야 크롤러가 품질을 낮게 안 본다" | **대체로 틀림** | 서치콘솔 '등록'은 소유권 확인일 뿐 크롤링·색인을 유발하지 않음. 등록 자체의 불이익은 없고 오히려 데이터가 그때부터 쌓임. 다만 **빈 페이지를 URL 검사로 색인 요청**하면 "크롤링됨–색인 생성 안 됨"에 오래 머물 수 있음 → **등록은 지금, 개별 색인 요청은 내용 채운 뒤** |

**진짜 병목은 따로 있었다**: `/share/:shareId`가 본문 한 줄짜리 리다이렉트 껍데기이고(`worker/src/index.ts`), 진단 결과는 전부 JS로 그려진다. GPTBot·PerplexityBot 등 대부분의 AI 크롤러는 JS를 실행하지 않으므로 **인용할 본문 자체가 존재하지 않는 상태**다. 스키마 마크업보다 이게 우선이다.

#### 6.1.1 원래 우선순위와 그 수정
착수 전 기획 단계에서 1번의 **형태를 바꿨다**. 원안과 수정안을 모두 남긴다.

- **원안 1순위**: `/share/:shareId`를 서버렌더링(SSR)해서 색인 자산화
- **원안 2순위**: `LocalBusiness`/`FAQPage` JSON-LD 삽입
- **수정 사유**: 매장별 진단 결과 페이지는 ⓐ 검색 수요가 사실상 0이고 ⓑ 대량 자동생성 시 정책 리스크가 크며 ⓒ 타사 상호+등급을 공개 색인시키는 신용 리스크가 있다(§6.1.3). 같은 데이터로 **키워드별 순위 페이지**를 만들면 검색 수요·독창성·안전성이 전부 낫다.

#### 6.1.2 확정 설계 — 페이지 2종을 분리한다
**A. 키워드 순위 페이지 `/rank/:keyword` — SSR + 색인 대상 (이게 진짜 자산)**
- 데이터 출처: `rank_snapshots`(이미 매일 쌓이는 중, §9). 별도 수집 불필요
- 내용: 해당 키워드 상위 6곳의 순위표 + 상위권 집계(중앙값·1페이지 진입선) + 시장 변동성 1개 수치. **해석 문장은 넣지 않는다** — 상세 근거는 §6.1.2b
- 발행 조건: `rank_snapshots`에 **스냅샷 2회 이상 쌓인 키워드만** 발행. 1회짜리는 집계·변동성이 성립하지 않아 템플릿 껍데기가 되므로 색인 대상에서 제외
- N개 상호명이 자연스럽게 본문에 포함됨 → §6.1.4 질문의 유일한 현실적 해답

#### 6.1.2b 공개 범위 — 3단 분리 (2026-09-08 확정, 이 경계를 넘지 말 것)
당초 A 페이지에 **개별 업체의 시계열 변동**("3위→1위 사이 리뷰 40건 증가")을 실으려 했으나 철회했다. **그게 곧 컨설팅 상품 자체를 무료 공개하는 것**이기 때문이다 — 경쟁 서비스에는 완성된 데이터셋을, 각 자영업자의 경쟁자에게는 개선 포인트를 공짜로 준다.

전제: **GEO와 데이터 보호는 상충한다.** 색인시키는 순간 AI가 요약해서 남에게 떠먹여 준다. 그러므로 "가려서 공개"가 아니라 **공개해도 손해가 아닌 것만 올린다**.

자산 여부 판별 기준:
- **절대 수치**(A업체 리뷰 1,240건) → 자산 아님. 네이버에서 누구나 확인 가능. 우리는 모아놓았을 뿐
- **시계열 변화량**(리뷰 40건 증가) → 자산. 네이버는 과거를 안 보여준다
- **순위 변동과 지표 변동의 연결** → 핵심 자산. 컨설팅 상품 그 자체

| 단계 | 대상 | 공개 내용 |
|---|---|---|
| 1단 · 공개 색인 | `/rank/:keyword` | 순위표(순위·상호명·공개 절대 수치), 상위권 집계(중앙값·**1페이지 진입선**), 시장 변동성 1개 수치("최근 30일 상위권 순위 변동 N회"). **표와 숫자만, 해석·권고 문장 없음** |
| 2단 · noindex | `/share/:shareId`, 진단 결과 화면 | 내 매장 대비 격차, 결핍 판정, 등급 |
| 3단 · 비공개 | 관리자 전용 | 업체별 시계열 델타, 순위↔지표 상관, 맞춤개선리포트 |

- **진입선을 1단에 두는 이유**: 우리가 계산한 값이라 독창성이 있으면서, 개별 업체에 대해 네이버 이상을 알려주지 않는다. 동시에 "내 매장은 이 기준 대비 어디?"라는 질문을 만들어 진단 CTA로 밀어 넣는 미끼로 기능한다
- **시장 변동성을 1단에 두는 이유**: 개별 업체를 지운 집계라 "누가 뭘 해서 올랐는지"는 안 드러나면서 "관리 안 하면 밀린다"는 위기감은 전달된다
- **[2026-09-08 경계 조정] 개별 업체의 "순위 상승" 사실은 1단에 공개한다.** 당초 개별 업체 시계열을 전부 3단에 뒀으나, 순위 변동 자체는 경쟁자가 네이버를 매일 검색하면 알 수 있는 정보라 감출 실익이 없다. **감춰야 하는 건 "왜 올랐는가"(순위↔지표 연결)이고 그건 계속 3단이다.** 오히려 "올랐다는 사실만 보여주고 이유는 안 알려주는" 구성이 진단 CTA로 이어지는 퍼널이 된다. 단 **하락은 표시하지 않는다** — 특정 업체명 옆의 "하락"은 §6.1.3 리스크 2(신용훼손 시비)를 그대로 되살린다
- **감수하는 대가**: 시계열 상세를 빼면 §6.1.0의 독창성 방어 논리가 약해진다. 순위표+집계만으로는 **수십~수백 장 규모까지만** 안전하다. 수천 장을 뿌리면 대량생성 콘텐츠로 걸릴 위험이 급격히 올라간다 — 페이지 수를 욕심내지 않는 조건에서만 성립하는 설계다

**B. 공유 결과 페이지 `/share/:shareId` — SSR 하되 `noindex`**
- SSR은 한다(JS 꺼진 환경·카톡 인앱브라우저·AI 크롤러 대응)
- 단 `<meta name="robots" content="noindex">`를 넣어 색인은 막는다. 이유는 §6.1.3
- 즉 **"SSR = 색인"이 아니다.** 이 둘을 분리하는 게 이번 기획의 핵심 결정

#### 6.1.3 리스크 (착수 전 사용자 승인 필요 항목)
1. **대량 자동생성 콘텐츠 정책** — 구글의 scaled content abuse 정책은 "가치 추가 없이 대량 생성된 페이지"를 타깃한다. 고유 실측 데이터는 방어 논리가 되지만, **보일러플레이트 비율이 높고 한꺼번에 수백 장을 뿌리면 위험**하다. → 위 "스냅샷 2회 이상" 조건 + 점진 발행으로 완화
2. **타사 상호 + 부정 평가의 공개 색인** — 특정 업체명과 함께 "위험/C등급" 같은 판정이 검색에 노출되면 신용훼손 시비 소지가 있다. → **A 페이지에는 등급·진단 표현을 쓰지 않고 실측 수치와 순위만 중립 서술한다.** 등급/결핍 판정은 B(noindex)와 관리자 리포트에만 유지
3. **네이버 데이터 재게시** — 스크랩 데이터를 대량 공개 게시하는 것은 ToS 마찰 소지가 있다. 수치 재가공(평균·변동폭) 위주로 서술하고 원문·사진 복제는 하지 않는다

#### 6.1.4 사용자 질문에 대한 답 (기대치 조정)
- **독창성 확보되는가** → A는 확보되지만 **§6.1.2b 이후로는 방어선이 얇아졌다.** 남은 독창 요소는 우리가 계산한 진입선·집계·변동성뿐이고, 가장 강력했던 개별 시계열은 전략적 이유로 뺐다. 따라서 "스냅샷 2회 이상" 조건과 **발행 규모 제한**을 반드시 지켜야 한다. B는 독창적이지만 검색 가치가 없다
- **검색 SEO 확보되는가** → 기술적으로는 확보된다(SSR+정적 HTML). 다만 **순위가 잡히는 것과 트래픽이 오는 것은 다르다.** "○○ 플레이스 순위" 류 질의는 검색량이 거의 0이다. 실질 유입은 "안산 스카이차" 같은 **지역+업종 키워드 본편**에서 순위표 페이지가 잡힐 때 발생하며, 이쪽은 경쟁이 있다
- **N개 상호명으로 각각 노출되는가** → **개별 매장 페이지 방식으로는 거의 안 된다.** 한 페이지가 여러 상호로 동시에 잡히려면 그 페이지가 각 상호에 *대한* 페이지여야 하는데, "A업체 진단" 페이지는 B·C를 언급만 할 뿐이라 "B 플레이스 순위"로는 사실상 안 잡힌다. **키워드 순위 페이지(A)는 구조상 그 N개 업체 전부에 대한 페이지이므로 이 기대에 부합하는 유일한 형태다**

#### 6.1.5 착수 순서와 실제 진행 결과
1. **[완료 2026-09-08]** `/rank/:keyword` SSR + 발행 조건(관측 2회 이상) + `/rank` 허브 + `sitemap.xml` 동적 생성. 띄어쓰기만 다른 표기는 대표 1개만 발행하고 나머지는 canonical로 통합(`normKeyword()`)
2. **[보류 — 착수했다가 취소]** `/share/:shareId` 본문 SSR. **§6.1.2b에서 이 페이지를 `noindex`로 확정한 순간 SSR의 근거가 대부분 사라졌다** — 원래 명분이 "AI 크롤러가 JS를 실행 못 해 인용할 본문이 없다"였는데, 색인을 막을 거면 인용도 안 될 것이기 때문. 남는 이득은 JS 꺼진 환경·인앱브라우저 대응뿐인데 현재 실측된 문제가 없다. `noindex` 메타만 추가하고 SSR은 하지 않는다. **JS 미실행 환경에서 공유 링크가 안 열린다는 제보가 실제로 들어오면 그때 하면 된다**
3. **[완료 2026-09-08]** JSON-LD `ItemList` — 순위·상호명 등 사실만. 등급·평가는 구조화 데이터로 내보내지 않는다(§6.1.3 리스크 2). `LocalBusiness`는 우리 매장이 아니므로 부적절해서 쓰지 않음
4. **[완료 2026-09-08]** 고아 페이지 해소 — 홈 푸터에서 `/rank` 링크, 상세 페이지에서 허브로 역링크. 사이트맵만으로는 발견은 되나 권위 전달·재크롤링 우선순위에서 불리하다
5. 서치콘솔·네이버 서치어드바이저 등록은 **지금 해도 무방**(§6.1.0) — 네이버 소유확인 메타태그는 삽입 완료, 구글은 도메인 속성(DNS TXT)으로 사용자가 직접 진행

### 6.2 경쟁사 벤치마킹 (삐딱한마케팅 무료 진단 도구, 2026-09-08 분석)

**대상**: `삐딱한마케팅`(주식회사 사선)의 "플레이스 종합분석" 무료 도구. 사용자가 실제 진단 결과 전문을 가져와 분석 요청.

**포지션 차이 (이걸 먼저 이해할 것)**: 그쪽은 **내 매장 자체 점검(절대 점수)**, 우리는 **경쟁사 대비 격차(상대 비교)**다. 그래서 기능을 그대로 베끼면 우리 차별점이 사라진다.

| 항목 | 경쟁사 | 우리 | 판단 |
|---|---|---|---|
| 입력 | 플레이스 URL 하나 | 매장 + 키워드 2단계 | 그쪽이 진입장벽 낮음. 단 우리 2단계는 상대비교의 전제라 없앨 수 없음 |
| 결과 | 100점 만점 + 8항목 배점 | 등급 + 경쟁사 대비 격차 | **절대 점수는 따라 하지 말 것**(아래) |
| 개선안 | 항목별 행동 지시 전부 무료 공개 | 결핍 지적만, 개선안은 관리자 리포트 | 전략적으로 우리가 맞으나 §6.2 3번 참조 |
| 랜딩 본문 | 기능설명·사용법·FAQ 6문항 | (없었음) | **채택 완료 — §6.1.5 참조** |
| 무료 도구 수 | 4개(순위체크·검색량·AI진단 등) | 진단 1개 + `/rank` | 각각이 검색 유입구. 장기 참고 |

**따라 하면 안 되는 것 — 100점 만점 절대 점수**: 가중치가 자의적이고 경쟁 맥락이 0이다. 그쪽도 FAQ에서 "점수 높다고 1위 보장 안 됨", 결과 화면에서 "다 채워도 저장 신호·리뷰 전환이 받쳐줘야 함"이라고 스스로 한계를 인정한다. **그 공백이 정확히 우리 자리다.** 우리에겐 "상위 6곳이 실제로 리뷰 몇 개인지"라는 실측 기준선이 있다.

**미착수 항목 (사용자 지시로 기록만, 착수 시점 미정)**
- [ ] **2번 · 재진단 동선** — 경쟁사는 사용법 4단계 마지막이 "개선 후 재진단해 점수 변화 확인"으로 **재방문을 설계에 넣었다**. 우리는 A/B/C 등급이라 개선해도 변화가 눈에 안 띈다. 도입한다면 **절대 점수가 아니라 "경쟁사 평균 대비 지수"** 형태여야 정체성이 유지된다. 단 §9.8 하루 3회 제한과 재진단 유도가 충돌하므로 함께 설계할 것
- [ ] **3번 · 무료 개선 힌트 1~2개** — 지금은 "부족합니다"에서 끊고 구체적 개선안은 전부 관리자 PDF에만 있다. 상품 보호로는 맞지만 **결핍만 지적하고 끝나면 불신으로 갈 여지**가 있다. 신뢰 확보용으로 1~2개만 무료 노출하는 안. 어느 항목을 풀지는 §6.1.2b의 자산 판별 기준(절대 수치 = 자산 아님 / 인과 연결 = 핵심 자산)으로 고를 것

**착수 판단 기준**: 둘 다 지금 급하지 않다. 서치콘솔 등록 후 **검색 유입이 실제로 붙는지 보고** 판단한다 — 유입이 없으면 전환율 개선(2·3번)은 의미가 없다.

### 6.3 가이드 문서 (2026-09-08 발행, `worker/src/guides.ts`)

§6.1.0에서 확인한 "본문이 없으면 검색엔진이 주제를 판단할 재료가 없다"에 대한 실행. 순위 페이지(`/rank`)가 데이터 자산이라면 이쪽은 **검색어 자산**이다.

#### 6.3.1 주제 선정 근거 — 실측 우선
네이버 자동완성(`ac.search.naver.com`)으로 씨앗 22개를 확장해 **고유 키워드 51개**를 수집한 뒤 골랐다. 감으로 정하지 않았다.

- **수요 확인됨**: 순위 조회/확인/보는법, 순위 하락, 순위 올리는 방법, 상위노출 방법/비용, 키워드 등록방법/설정하는법/잡는법, 플레이스 최적화
- **수요 없음(자동완성 0~1건)**: `플레이스 경쟁사 비교`, `네이버 지도 순위 알고리즘` — **사용자가 원래 씨앗으로 준 표현이지만 그대로 문서를 만들면 유입이 없다.** 대신 질문형으로 바꿔 GEO/AEO용 즉답 문서(진입선)에 흡수
- **의도적 제외**: 마케팅 대행사·업체(대형 대행사 점유), 스마트플레이스 등록·고객센터(네이버 공식 페이지 고정 1위), 리뷰 삭제·마이플레이스(소비자 의도라 우리 고객 아님), 순위 프로그램·애드핀(어뷰징 수요, 유입돼도 고객 아니고 브랜드 리스크)

> 한계: 자동완성은 **검색량이 아니라 검색 흐름**이다. 실제 월 검색량은 검색광고 API(계정+키 필요)를 붙여야 알 수 있고, 이건 **기획에 포함하지 않기로 확정**(2026-09-08 사용자 결정).

#### 6.3.2 발행한 5편
| 경로 | 노리는 검색어 | 이 문서만의 근거 |
|---|---|---|
| `/guide/플레이스순위조회` | 순위 조회·확인·보는법 | 위치 개인화 실측(§7.15) |
| `/guide/플레이스순위하락` | 플레이스 순위 하락 | 상대평가 원리 + 시계열 관측 필요성 |
| `/guide/플레이스대표키워드` | 키워드 등록방법·설정·잡는법 | 순서 중요성, 단순 나열 감점 |
| `/guide/플레이스상위노출` | 상위노출 방법 (허브 역할) | 기준선 개념, 착수 순서 |
| `/guide/플레이스진입선리뷰` | GEO/AEO 즉답용 | 진입선이 평균보다 정확한 이유 |

**5편 전부 실제로 다른 내용을 썼다**(사양 §2.2 문구 pool 금지). 각 문서의 중심 근거가 다른 것이 그 증거이며, 여기서 문서를 더 늘릴 때도 **새로 쓸 고유 근거가 없으면 만들지 않는다.**

#### 6.3.3 구조
- Answer-First: 첫 문단이 파란 박스로 질문에 즉답(공백 포함 150~200자)
- `Article` + `FAQPage` JSON-LD. 단 **FAQ 리치 결과는 기대하지 않는다**(2023-08 이후 일반 상업 사이트 미표시) — 목적은 본문 확보와 AI 파싱
- URL에 `/guide/` 접두어 사용. **범용 사양 §1.2(루트 직하 평면)와 다른데**, 그 조항은 대량 생성되는 개별 대상 페이지용이고 정보성 문서를 루트에 두면 다른 경로와 충돌 위험이 크다고 판단

#### 6.3.4 상위 문서 해부 결과와 재작성 (2026-09-08)
사용자가 `플레이스 순위 하락 원인`에서 실제로 상위에 있는 문서 2건을 지목해 분석을 요청했다. **감으로 쓰지 않고 두 페이지를 `curl`로 받아 구조를 실측**했다.

| | 네이버 웹문서 1위 | AI요약 인용 2위 | 우리(재작성 전) |
|---|---|---|---|
| 본문 분량 | 13,086자 | 5,525자 | **1,987자** |
| JSON-LD | 5종 | 3종 | 2종 |
| 자사 실측 수치 | 있음 + **출처 절** | 일부 | **없음** |

**도출한 공통 요인** (범용 지침 §2.3에 일반화해 반영):
1. **이름 붙은 분류 프레임**("일곱 갈래", "데이터 5가지") — 단순 순서와 다르다. AI가 인용할 덩어리가 생긴다
2. **자사 실측 수치**를 본문에 투입
3. **"이 글에 쓰인 숫자의 출처"** 별도 절
4. **FAQ에 브랜드 질문** 1개 — AI 답변에 자사명이 딸려 들어간다
5. 분량은 경쟁 문서를 재서 정한다 — 절반 이하면 그 자체로 불리
6. Answer-First 블록에 `id="aeo-direct-answer"` 부여

**가장 뼈아팠던 지점**: 우리는 진입선·순위 변동 실측을 실제로 보유하고 있으면서 **글에 한 줄도 쓰지 않고 일반론만 적었다.** §6.1에서 "독창적 데이터가 인용을 만든다"고 정리해놓고 정작 문서에 적용하지 않은 것이다.

**재작성 내용**(`worker/src/guide-drop.ts`로 분리): 다섯 갈래 분류 프레임(착시·경쟁강세·정보방치·리뷰흐름·정책감점) + 관측 키워드 11개의 진입선 실측(강남미용실 21,466건 ~ 안산 도배 0건, 인형병원 3건) + 출처 절 + 브랜드 FAQ. **1,987자 → 11,183자**.

**지어내지 않은 것**: 경쟁 문서가 쓴 "복구 중앙값 21일" 같은 수치는 우리에게 없으므로 흉내 내지 않았다. 순위 변동 빈도도 관측 회차가 2~5회로 짧아 결론을 낼 수 없어 **출처 절에 한계로 명시**하고 본문에서는 수치로 쓰지 않았다.

> ⚠️ **정정(2026-09-08)**: 위 "11,183자"는 `wc -c`로 잰 **바이트 수**였다. 한글은 한 글자가 3바이트라 부풀려진 값이며, 실제 문자 수는 **4,715자**다(경쟁 1위 문서 13,131자의 36%). 이후 분량을 잴 때는 반드시 **문자 수 기준**으로 비교할 것. 지침 §2.3의 "상위 문서 대비 절반 이하면 불리" 기준에 아직 미달이다.

---

### 6.4 순위 페이지 강화 기획 (2026-09-09 확정, 착수 전 필독)

> **진행 상태(2026-09-09)**: 6.4.1~6.4.7 **구현 완료·배포됨**. 순위 페이지 본문이 934자 → 2,070~2,346자로 늘었다. 다만 **주차별 표(6.4.3)는 관측 3주가 쌓여야 켜진다** — 코드는 들어가 있고 조건이 차면 자동으로 나타난다. 미착수는 6.4.8의 KV 캐시뿐이다.

**목표**: `/rank/:keyword`를 단일 시점 순위표에서 **주차별 시계열 + 해설이 있는 문서**로 키워 검색 유입을 확보한다. 데이터가 쌓일수록 문서가 저절로 길어지는 구조를 만드는 것이 핵심이다.

#### 6.4.1 URL은 바꾸지 않는다 (중요)
슬러그를 `제주도맛집` → `제주도맛집플레이스순위권분석` 식으로 확장하자는 안이 나왔으나 **기각**한다.

- 이미 사이트맵에 올라가 색인 대기 중이라 변경 시 301이 필요하다(사양 §1.1~1.5의 "되돌리기 어려운 결정")
- URL에 이미 `제주도맛집`이 들어 있어 **검색어 완전일치 신호는 확보돼 있다**
- 확장이 필요한 것은 URL이 아니라 **화면에 보이는 title/H1**이다

```
현재  제주도맛집 네이버 플레이스 순위 TOP 6
변경  제주도맛집 플레이스 순위 분석 · 상위 6곳 리뷰·사진 지표 비교
```

#### 6.4.2 ★ 검색 의도 충돌을 막을 것
`제주도 맛집`은 **소비자가 치는 검색어**다. 여기서 유입된 사람은 맛집을 찾는 관광객이지 사장님이 아니다. 이탈률만 올라가고 전환은 0이다.

**title에 `플레이스`·`지표`·`상위노출` 같은 업계 용어를 반드시 넣어 소비자를 걸러낸다.** "제주도 맛집 순위"가 아니라 "제주도맛집 **플레이스 상위노출 지표 분석**"이어야 한다.

#### 6.4.3 주차별 순위표 (사용자 제안, 채택)
최근 **4주**의 주차별 순위를 표로 공개한다.

- **채택 근거**: ⓐ 키워드마다 값이 완전히 달라 대량생성 위험이 없다 ⓑ 네이버는 과거 순위를 안 보여주므로 우리만 가진 데이터다 ⓒ 데이터가 쌓일수록 문서가 저절로 길어진다 ⓓ 재방문 동기가 생긴다(§6.2의 재진단 동선과 같은 효과)
- **자산 판정**: 순위 숫자는 매일 검색하면 누구나 얻을 수 있어 §6.1.2b 기준 **자산이 아니다.** 공개해도 잃을 게 적다. 진짜 자산인 "순위 변동 ↔ 지표 델타의 연결"은 계속 비공개다

**설계 결정**
| 항목 | 결정 |
|---|---|
| 주차 표기 | `9월 2주차 (9/8~9/14)` — 날짜 범위 병기. "몇째 주"는 사람마다 다르게 센다 |
| 주 대표값 | 그 주의 **마지막 관측**. 단순하고 설명하기 쉽다 |
| 공개 범위 | **최근 4주까지만.** 그 이상 이력은 관리자 화면에만 |
| 활성화 조건 | **관측 주차 3주 이상** 쌓인 키워드에서만 표를 켠다. 미만은 기존 단일 시점 표 유지(빈칸투성이 표는 부실해 보인다) |

#### 6.4.4 표기 금지 사항
- **화살표·부정 표현 금지.** `▼ 하락`, `위험` 같은 표시를 붙이는 순간 사실 나열이 아니라 **평가**가 되고 §6.1.3 신용훼손 리스크가 돌아온다. 표에는 순위 숫자만 둔다
- **상승만 실명으로 강조**한다. 하락은 표 안의 숫자로만 존재하고 별도 문장으로 쓰지 않는다. 집계로는 가능("상위 6위권에서 자리가 바뀐 것은 3회입니다")
- **인과 표현 금지.** "활성화 지표가 영향을 준 것으로 보입니다" 같은 문장은 쓰지 않는다. 우리는 인과를 모른다(2026-09-08 사진 가설이 전수 검증에서 17:17로 무너진 사례 참조). 사실만 쓰고 해석은 상담으로 넘긴다
- **지표 델타 비공개.** "그 사이 리뷰가 40건 늘었다"는 우리만 아는 정보다. 그 공백이 곧 상담 유인이다
- **"변동 없음" 문장 금지.** 여러 페이지·여러 주에 같은 문장이 반복되면 그 자체가 중복 콘텐츠 신호다. **변동이 있을 때만 문장이 생기게** 하고 없으면 그 절을 렌더하지 않는다

#### 6.4.5 텍스트 확장 원칙
그냥 늘리면 대량생성이다. **키워드마다 값이 달라 자연히 다른 문장이 되는 것**만 넣는다.

- 키워드 유형 배지(틈새형·중간형·고경쟁형) — 진입선 기준 자동 판정, 가이드 문서의 분류와 연결
- 진입선과 중앙값의 격차 → 왜곡 정도 해석(예: 제주도맛집은 중앙값 2,873 / 진입선 8,617로 3배 차이)
- 상위권 지표 편중도(리뷰 편중인가 사진 편중인가)
- 관측 기간·횟수, 자리가 바뀐 횟수 요약 한 줄
- **갱신 표기는 "주 단위"보다 "관측 이래 누적"**이 안전하다. 변동 없는 주에 쓸 말이 없어지는 문제를 피한다. `lastmod`는 실제 관측일만 쓰고 미래 날짜 금지(사양 §6)

#### 6.4.6 CTA 2단 분리
성격이 달라 하나로 합치지 않는다.
- **상단**: 내 매장 무료 진단하기 (도구 유입)
- **표 마지막 행**: "내 매장은 이 표에 없나요?" → 진단
- **하단**: 순위 상승 컨설팅 상담 신청 (수익 전환)

#### 6.4.7 공개 / 비공개 경계
| 항목 | 공개 | 관리자 전용 |
|---|---|---|
| 순위표·진입선·중앙값 | O | |
| 키워드 유형 판정 | O | |
| 최근 4주 주차별 순위 | O | |
| 상승 업체명 + 상승폭 | O | |
| 하락 | 표 안 숫자만 (실명 문장 X) | 실명·폭 |
| 지표 델타(리뷰 +40 등) | X | O |
| 순위↔지표 상관 해석 | X | O |
| 4주 초과 전체 이력 | X | O |

#### 6.4.8 함께 처리할 것
- **KV 캐시 도입 시점**: 지금은 매 요청마다 D1을 조회한다. 갱신 주기가 정해지면 §7.9 버전키 방식으로 캐시를 얹어 크롤러의 대량 조회 부담을 없앤다
- **관리자 상세는 이미 있다**: `/admin/analytics/:keyword`가 순위 추이 차트와 지표 델타 테이블을 제공하므로 새로 만들지 말고 공개 페이지에서 관리자에게만 링크한다

### 6.5 경쟁사 벤치마킹 2차 (마케팅마법사 `placewizard.kr`, 2026-09-09 분석)

**대상**: `placewizard.kr`(마케팅마법사). §6.2의 삐딱한마케팅이 "무료 진단 도구" 경쟁자였다면, 이쪽은 **무료 도구 → 상담 → 대행 수주**까지 우리와 수익 구조가 완전히 동일한 경쟁자다. 사용자가 사이트 전체 분석을 요청.

#### 6.5.1 그쪽 골격 — 우리와 같다

```
무료도구 3종 (게이트 없음)            → 리드 미끼
  ├ 순위조회 (1~3초, 회원가입 불필요)
  ├ 키워드 검색량 (네이버 검색광고 API)
  └ 카카오톡 채널 순위조회
25일 순위추적 (회원 전용, 프로모션 무료)  → 회원 전환
가이드 9편 (/guide/*)                  → 검색 유입 + 권위
카톡 상담 (프리필 양식)                 → 상담 전환
대행 3상품 (플레이스 상위노출 / 블로그 배포 / 홈페이지 SEO)  → 매출
```

**포지션 차이**: 그쪽 미끼는 "내 순위 몇 위" 단품, 우리는 Gap 분석(상위 평균 + 10위 실측값). **미끼는 우리가 더 세다.** 문제는 미끼 뒤 동선(상담 → 상품)이 우리는 아직 비어 있다는 것. 즉 이 경쟁사에서 베낄 것은 기능이 아니라 **동선과 카피**다.

#### 6.5.2 채택할 카피 문법 8가지 (`/placeai.php`)

| # | 기법 | 원문 | 왜 먹히나 |
|---|---|---|---|
| 1 | 문제제기를 **고객 1인칭 대사**로 | "원하는 검색어에서 우리 가게가 너무 아래에 있어요." | 업체 용어("상위노출 최적화")가 아니라 사장님이 실제로 하는 말. 읽는 순간 "내 얘기"가 된다 |
| 2 | **3단 고정 문법** 반복 | 먼저 봅니다 / 이렇게 합니다 / **보내드립니다** | 세 상품 모두 같은 틀. 특히 "보내드립니다"(산출물 명시)가 핵심 — 대행사 불신은 "뭘 받는지 모름"에서 온다 |
| 3 | **역-보장** | "1위 보장하지 않습니다. 누구도 플랫폼의 검색 결과를 고정할 수 없습니다" / "고정된 기간을 약속할 수 없습니다" | 전 업계가 "1위 보장"을 외칠 때 혼자 안 한다 = 유일하게 정직해 보인다. §2의 "절망 유발 숫자 안 씀" 원칙과 같은 계열 |
| 4 | **분리 판매 명시** | "네. 플레이스만, 블로그만, SEO만 각각 맡길 수 있습니다" | 진입 장벽 제거. §2 3단계 "싸고 빨리 되는 것 순" 정렬과 같은 논리 |
| 5 | 자체도구 **스크린샷 + 출처 캡션** | "네이버 광고 API · 2026.07 조회 예시" | 날짜·출처를 박아두면 스크린샷이 증거가 된다 |
| 6 | **카톡 프리필 양식** | 링크 클릭 시 카톡 대화창에 상담 양식 4문항이 자동 입력됨 (`pf.kakao.com/_fAtxkG/chat?mode=chat&input=...`) | 폼 개발 없이 상담 마찰이 0이 된다. 우리 리드폼이 아직 inert인 상태(§10 로그)의 즉효 대체재 |
| 7 | 사회적 증거 | "카카오톡 채널 이용자 5,000명+" | |
| 8 | 가격 정책 이원화 | 대행 상품 = 상담(비공개), 블로그 배포만 **공개가** | 저가 상품만 공개해 진입시키고 고가는 상담으로 넘긴다 |

#### 6.5.3 가격 벤치마크 (`/blogposting.php`, 공개가 실측)
- **플레이스용 배포형 리뷰: 1,500원/건** — 방문 후기체, 업체명 검색 타겟
- **지역 검색형 "전단지" 배포: 2,500원/건** — 지역+업무+상황 조합마다 **다른 글**을 발행 (예시 원문: "수원 인계동 뷰티샵 철거 원상복구 견적")

두 번째가 우리 "웹문서 제작"과 같은 물건인데, **롱테일 키워드 1개당 글 1개**로 포장해 단가를 67% 올렸다. §2 3단계 처방 상품에 공개 단가를 붙일 때 이 포장법을 그대로 쓴다.

#### 6.5.4 25일 추적 — 기술 분석 (결론: 우리가 이미 상위호환을 갖고 있다)

로그인 뒤라 화면은 못 봤으나 방법론이 가이드(`/guide/slot-tracking-marketing-roi.php`)에 전부 공개돼 있다.

```
1~10일 : 아무것도 안 함 (베이스라인)
11일   : 마케팅 1개만 집행
11~25일: 효과 측정
```
- 추적 키워드 **3개로 제한**(노이즈 감소), **매일 같은 시각** 자동 기록, 마케팅 활동은 **한 번에 하나만**(동시 집행 시 인과 구분 불가)
- 채널별 반응 속도 명시: 광고 3~5일 지연 / 리뷰 이벤트 즉시·일시적 / SNS 최소 10일
- 사례: 강남 치킨집 25만원 체험단 → 9.6계단 상승 → 다음달 예산 3배

**즉 기술이 아니라 서사다.** 매일 순위를 찍는 크론일 뿐이고, "25일"이라는 숫자와 전/후 구간 분할이 상품성의 전부다.

**우리 상태**: §9 리버스엔지니어링 파이프라인이 이미 같은 물건이다 — `rank_snapshots`(키워드×업체×시점 1행), cron 3개(12시 KST 이후, 네이버 순위 변동 시간대 반영), `/admin/analytics/:keyword`(순위 추이 + 순위변동 시점 지표 델타).

**우리 우위**: 그쪽은 순위만 기록한다. 우리는 순위 변동 시 방문자리뷰·블로그리뷰·투표수·사진수 **델타를 함께** 본다. "9.6계단 올랐다"가 아니라 "블로그리뷰가 12건 늘던 주에 9계단 올랐다"까지 말할 수 있다. 25일 추적의 상위호환이며 그대로 상담 근거가 된다.

**제약(착수 시 필독)**: subrequest 한도 50이 이미 빡빡하다(§9.4). 추적 대상이 늘면 크론 3개로 못 버티므로, 경쟁사 상세 스크랩을 생략하고 내 순위만 찍는 **경량 추적 경로**를 따로 파야 한다. 순위 숫자만 필요하니 경량으로 충분하다.

#### 6.5.5 웹문서 → 플레이스 백링크: 카피 주의 (필독)

네이버 플레이스 랭킹에 외부 백링크가 반영된다는 근거는 없다(§9.6 기준으로도 우리가 관측 못 하는 영역). 실제 효과는 ① 웹문서 자체가 검색결과 면적을 차지 ② 클릭 유입이다.

- ❌ "백링크로 플레이스 순위 상승"
- ✅ "검색결과에서 우리 가게 얘기가 차지하는 면적을 늘립니다"

경쟁사도 홈페이지 SEO를 **별도 상품으로 명시 분리**했다("플레이스 순위 상승과는 별도 상품입니다"). 같은 방어를 하라.

#### 6.5.6 리워드형 트래픽: 보류 권고

체험단은 문제없다. **리워드형 플레이스 트래픽은 보류한다.** 어뷰징이고 §6.5.2-3의 역-보장 정직 포지셔닝과 정면 충돌한다. 정직 포지셔닝이 우리 미끼(Gap 진단)의 신뢰를 떠받치는 구조라 리워드 트래픽 하나가 전체를 깎는다. 경쟁사도 `placeslot.php`라는 이름만 남기고 실제로는 팔지 않는다. 한다면 브랜드를 분리할 것.

#### 6.5.7 네이버 검색광고 API 도입 검토 (⚠️ §6.1의 기존 결정을 뒤집는 안 — 착수 전 확인 필요)

§6.1에 "실제 월 검색량은 검색광고 API를 붙여야 알 수 있고, 이건 **기획에 포함하지 않기로 확정**(2026-09-08 사용자 결정)"이 있다. 그건 GEO·pSEO 맥락의 결정이고 §8.1 재논의 금지 항목은 아니지만, **뒤집는 결정이므로 명시적 확인 없이 착수하지 않는다.**

**왜 다시 올라왔나**: §2 2단계 Gap 분석의 마지막 빈칸이다. 지금은 "블로그리뷰 29건만 채우면 1페이지"인데, 여기에 "이 키워드 월 1,900회 검색"이 붙으면 **견적의 근거가 완성된다.** 경쟁사도 무료 도구 3종 중 하나를 이걸로 채웠다.

**스펙**
- `GET /keywordstool`, `hintKeywords` 최대 5개
- 응답 `RelKwdStat[]`: `relKeyword`, `monthlyPcQcCnt`, `monthlyMobileQcCnt`, `compIdx`(낮음/중간/높음), `plAvgDepth`
- **무료.** 검색광고 광고주 계정 + API 라이선스 필요. 호출량은 광고계정 단위이며 명시적 상한 없음
- 인증은 HMAC-SHA256 서명 → Cloudflare Worker의 Web Crypto로 그대로 된다. 별도 런타임 불필요

**함정 2개**
1. 월 10회 미만은 숫자가 아니라 `"< 10"` **문자열**로 온다. 지역 롱테일("안산 상록구 스카이차")은 대부분 여기 걸리므로 그대로 노출하면 "이 키워드 쓸모없음"으로 읽힌다. → **"검색량 적음 = 경쟁 없음 = 1위 쉬움"** 프레임으로 뒤집어 표시할 것
2. 광고 기준 검색량이라 플레이스 실제 노출 수요와 완전히 일치하지 않는다. 경쟁사도 "네이버 광고 API 기준"이라고 캡션으로 박아 방어한다. 똑같이 할 것

**연관검색어**: `ac.search.naver.com/nx/ac`(§6.3.1에서 이미 사용 중)로도 공짜로 되지만, 실호출 결과 검색광고 API 쪽이 상위호환이었다 — §6.5.7b 참조.

#### 6.5.7b 실호출 검증 (2026-09-09, 실제 키 발급 후)

사용자가 계정을 만들어 키 3종을 발급했고 실호출로 전부 확인했다. **HTTP 200, 서명 규칙 이상 없음.**

| 키워드 | PC | 모바일 | 합계 | compIdx | 연관어 수 |
|---|---|---|---|---|---|
| 안산상록구맛집 | 110 | 700 | **810** | 높음 | 71 |
| 안산사다리차 | 80 | 260 | **340** | 높음 | 105 |
| 안산상록구스카이차 | `< 10` | `< 10` | — | 높음 | 1 |
| 수원인계동뷰티샵철거 | `< 10` | `< 10` | — | 낮음 | 1 |

**확인된 것 4가지**

1. **`"< 10"` 함정은 실재한다.** 3어절 이상 롱테일은 그대로 `< 10` 문자열이 온다. §6.5.7 함정1의 프레임 전환(경쟁 없음 = 1위 쉬움)이 반드시 필요하다. 파서는 `{ total: 10, isUnderTen: true }`로 변환한다(0으로 깎으면 "수요 없음"으로 오독됨).
2. **2어절 지역 키워드는 숫자가 제대로 나온다.** "안산상록구맛집 810회"처럼 §2 2단계에 바로 붙일 수 있는 크기다. 즉 **우리 타겟 키워드 대역이 API 유효 구간 안에 있다.**
3. **연관어가 검색량과 함께 71~105개씩 딸려온다.** 자동완성(10개, 검색량 없음)의 상위호환이다. → **§2 2단계의 "업종 기반 추천 칩 3개"를 실검색량 상위 3개로 뽑을 수 있다.** 감으로 고르던 칩이 근거를 갖게 된다. (미착수 — §6.5.8에 항목 추가)
4. **모바일이 PC의 3~6배.** 플레이스는 모바일이 본판이므로 화면에는 합산값을 쓰되, 필요하면 모바일만 강조한다.

**배포 완료 (2026-09-09)**: 중간에 wrangler의 Cloudflare OAuth 토큰이 만료돼(`Failed to fetch auth token: 400 Bad Request` → `Not logged in`) 막혔으나, 사용자가 `npx wrangler login` 재실행 후 해소. 시크릿 3종 등록 → `wrangler deploy`(Version `b59b6aab`) → 실배포 검증까지 완료.

> **wrangler 로그인은 전역이다.** `%APPDATA%\xdg.config\.wrangler`에 저장되므로 어느 폴더에서 로그인하든 상관없다. 앞으로 같은 증상이 나오면 폴더 옮겨가며 재시도하지 말고 `npx wrangler login` 한 번만 하면 된다.

**실배포 end-to-end 검증** — `GET /api/gap?placeId=2058645213&keyword=안산 상록구맛집` (백세보리밥 닭한마리, §6 헬스체크 매장):
```json
"keywordVolume": { "keyword": "안산 상록구맛집", "pc": 110, "mobile": 700,
                   "total": 810, "isUnderTen": false, "competition": "높음" }
```
공백 포함 입력이 그대로 들어와도 내부에서 공백 제거·대문자 정규화 후 매칭되는 것까지 확인했다(API는 공백 없는 형태로 응답하므로 이 정규화가 없으면 항상 `null`이 된다).

**키 로테이션 절차 (나중에 할 때)**: 검색광고 재발급은 액세스라이선스·비밀키가 **한 쌍으로** 갱신될 수 있으므로 화면의 값 둘 다 복사한다(`CUSTOMER_ID`는 불변). 교체는 `wrangler secret put` 2회면 끝이고 **코드 수정·재배포 불필요**하다(`secret put`이 워커를 자동 갱신). 교체 중 키가 안 맞아도 `getKeywordVolume()`이 `null`을 반환해 검색량 표시만 빠지므로 장애가 안 난다.

**가입 절차** (§6.5.7 착수가 확정되면 이 순서로)
1. `searchad.naver.com` 접속 → 네이버 아이디로 **검색광고 회원가입**(사업자/개인 선택, 사업자면 사업자등록번호 필요). 광고 집행·결제는 필요 없다 — 계정만 있으면 된다
2. 로그인 → 우측 상단 **[도구] → [API 사용 관리]**
3. **[네이버 검색광고 API 라이선스 신청]** → 발급 시 3개 값을 받는다
   - `CUSTOMER_ID` (계정 화면 상단에 표시되는 고객 ID, 숫자)
   - `ACCESS_LICENSE` (액세스 라이선스)
   - `SECRET_KEY` (비밀키 — **발급 시점에만 전체가 보인다. 즉시 복사할 것**)
4. `wrangler secret put NAVER_AD_CUSTOMER_ID` / `NAVER_AD_ACCESS_LICENSE` / `NAVER_AD_SECRET_KEY` 로 등록. 로컬은 `.dev.vars`
5. 서명 규칙: `timestamp(ms) + "." + method + "." + path` 를 `SECRET_KEY`로 HMAC-SHA256 → base64. 헤더 `X-Timestamp`, `X-API-KEY`(=ACCESS_LICENSE), `X-Customer`(=CUSTOMER_ID), `X-Signature`
6. 스모크 테스트: `GET https://api.searchad.naver.com/keywordstool?hintKeywords=안산맛집&showDetail=1`

#### 6.5.8 착수 우선순위 (사용자 확인 대기)

| 순위 | 할 일 | 공수 | 근거 |
|---|---|---|---|
| 1 | 랜딩 카피를 §6.5.2 문법으로 리라이트 (고객 1인칭 문제제기 → 먼저 봅니다/이렇게 합니다/**보내드립니다** → 역-보장 FAQ) | 코드 0, 글쓰기만 | 전환율 직결, 리스크 0 |
| 2 | 카톡 프리필 상담양식 URL (§6.5.2-6) | 1시간 | 폼 개발 없이 상담 마찰 제거. 현재 리드폼 inert 상태의 즉효 대체 |
| 3 | ~~검색광고 API 도입~~ → **백엔드·배포·실검증 완료(2026-09-09)**. 남은 것: **프론트 표시만**(`isUnderTen` 프레임 분기) | 소 | 사용자가 키 발급·제공하여 §6.1의 "기획 미포함" 결정은 번복 확정됨 |
| 4 | 블로그/웹문서 상품에 공개 단가 부착 (§6.5.3 벤치마크) | 소 | 저가 진입로 확보 |
| 5 | §2 2단계 추천 칩 3개를 검색광고 API 연관어 **실검색량 상위 3개**로 교체 (§6.5.7b-3) | 소 | 감으로 고르던 칩에 근거가 생긴다. 추가 호출 없이 기존 응답 재사용 |

#### 6.5.9 검색량 노출 정책 — 고객/관리자 분리 (2026-09-09 확정, 구현 완료)

> **확정 사항 — 재논의 금지.** 검색량 **숫자는 고객 화면에 절대 내보내지 않는다.** 관리자 리포트에서만 숫자로 본다.

**왜 분리했나**: 지역 롱테일은 대부분 `< 10`으로 온다(§6.5.7b). 사장님이 "월 10회 미만"을 보면 그 키워드를 포기한다 — §2의 "절망 유발 숫자는 쓰지 않는다"와 같은 이유다. 반대로 상담자는 정확한 숫자가 있어야 키워드 우선순위를 정할 수 있다. 그래서 같은 데이터를 두 화면이 다르게 쓴다.

| | 고객 화면 | 관리자 리포트 (`/admin/:shareId/report`) |
|---|---|---|
| 숫자 | **실수치 표시 안 함.** 구간으로 뭉뚱그린 한 문장만 | PC / 모바일 / 합계 / 경쟁도 전부 표기 |
| 데이터 전달 | `/api/gap` 응답의 `keywordVolumeNote`(문장 1개). **실수치는 안 실린다** | 서버가 `window.__REPORT_VOLUMES__`로 주입 |
| `< 10` | 숫자를 빼고 "아직 경쟁이 붙지 않은 틈새" 프레임 | `< 10` 그대로, 합계는 `—` (거짓 합산 금지) |

**고객 화면 문구 (2026-09-09 사용자 지시로 추가)**: 키워드 입력창 아래, "특정 경쟁사와 1:1로 바로 비교하기" 바로 위에 한 줄 노출한다. 문장은 서버(`describeVolume()`)가 만들어서 내려보내므로 **실수치가 브라우저에 도달하지 않는다.**

- 100회 이상 → `'안산 상록구맛집' 키워드는 월간 조회량 500건 이상의 키워드입니다.`
  구간은 내림 적용: `100 / 500 / 1,000 / 5,000 / 10,000 / 50,000 / 100,000`
- 100회 미만·`< 10` → `'○○' 키워드는 아직 경쟁이 붙지 않은 틈새 키워드입니다. 지금 선점하면 상위 진입이 빠릅니다.`
  **숫자를 아예 빼는 것이 핵심이다.** "월 10회 미만"을 보여주면 사장님이 그 키워드를 버린다.

#### 6.5.9b 리포트 모드는 입력창을 비우지 않는다 (2026-09-09 사용자 지적)

`applyReportMode()`가 `step2Input`을 통째로 숨기고 상단 상호 검색창은 빈 채로 남겨둬서, **리포트를 열면 어떤 매장·어떤 키워드 결과인지 알려면 스크롤을 내려야 했다.**

수정: 입력창을 숨기지 않는다. **값은 그대로 두고 조작 수단만 걷어낸다.**
- `storeInput`에 매장명을 채우고 `readOnly`, `keywordInput`도 `readOnly`
- `gapBtn`·`keywordChips`·1:1 비교 토글 블록만 숨김

원칙: **리포트는 조작하는 화면이 아니라 읽는 문서다.** 앞으로 리포트 모드에서 뭘 숨길지 판단할 때, 그 요소가 "맥락을 알려주는 값"이면 남기고 "누를 것"이면 숨긴다.

**구현 위치**
- `worker/src/searchad.ts` — `getKeywordVolumes(env, keywords[])`가 본체. 고객 문구는 `describeVolume()`. 힌트 5개씩 배치 호출, 키워드 단위 KV 캐시(24h)라 이미 조회된 건 API를 안 탄다. `getKeywordVolume()`은 단일 조회 단축형.
- `worker/src/index.ts` `/admin/:shareId/report` — `raw_data`에서 **진단 키워드 + 상호명 + 대표키워드 5개**(최대 7개)를 뽑아 조회 후 HTML에 주입. 조회 실패해도 리포트는 그대로 열린다.
- `worker/public/index.html` `renderReportVolumes()` — `applyReportMode()`에서만 호출되므로 고객 경로에서는 실행 자체가 안 된다.

**`< 10` 처리 규칙 (중요)**: `pcUnderTen`/`mobileUnderTen`을 필드별로 따로 들고 있다. 파서가 `< 10`을 상한값 10으로 바꾸므로 **합계를 그대로 더하면 "20"이라는 없는 숫자가 만들어진다.** 관리자 표에서 `isUnderTen`이면 합계 칸을 `—`로 비우는 이유다. 0으로 깎지도 않는다(그러면 "수요 없음"으로 오독됨).

**상담 화법**: `< 10`은 "검색량이 적다"가 아니라 **"경쟁이 없어 선점이 쉽다"**로 쓴다. 표 하단에 그 문구를 고정 노출해뒀다.

**실측 참고 (2026-09-09, 백세보리밥 닭한마리)**: 상호명 "백세보리밥닭한마리"가 **월 140회**(PC 40 + 모바일 100)로 잡혔다. 상호 검색량이 `< 10`일 거라는 예상과 달랐다 — 상호는 실제로 찍어봐야 안다. 5개 배치 호출로 상호·대표키워드가 한 번에 잡히는 것도 실호출로 확인했다.

### 6.6 상호 검색량 = 직접 검색 유입 추정축 (2026-09-09 확정, 구현 완료)

> 사용자 정의: **"해당 검색량으로 직접 검색 유입 숫자를 일부 추정 가능함."**

키워드 순위는 "검색어로 들어오는 유입"만 설명한다. 상호를 직접 검색해서 들어오는 유입은 순위와 **무관하게** 존재하고, 이건 상호 월간 검색량으로 근사할 수 있다. 그래서 §9 리버스엔지니어링에 축을 하나 더 붙였다.

**해석 규칙**: 순위는 높은데 상호 검색량이 낮으면 **순위빨**, 반대면 **브랜드빨**이다. 순위가 떨어졌는데 사장님 체감 매출이 안 떨어졌다면 후자를 의심한다.

#### 6.6.1 수집 — `rank_snapshots`에 2개 컬럼 추가

| 컬럼 | 내용 |
|---|---|
| `name_search_volume` | 상호명 월간 검색량(PC+모바일) |
| `name_volume_under_ten` | 위 값이 `< 10` 상한을 더한 **근사치**인지 |

**플래그를 따로 두는 이유 (실제로 밟은 함정)**: 파서가 `< 10`을 상한 10으로 바꾸므로 PC·모바일 둘 다 `< 10`이면 합계가 **20**으로 저장된다. 첫 배포 때 태산김치찜 안산본점이 `vol=20`으로 들어갔고, 이건 §6.5.9에서 이미 경고해둔 "없는 숫자"가 DB에 남은 케이스다. `NULL`(미측정)과 `< 10`(측정했으나 하한 미만)은 의미가 다르므로 컬럼을 합치지 않는다. 화면에서는 플래그가 서면 숫자 대신 `< 10`을 찍는다.

**`/api/gap` 경로에서만 채운다.** cron은 subrequest 한도(§9.4)가 이미 빡빡해서 상호 조회를 얹을 자리가 없다 — cron 행은 `NULL`로 남는다. 상호 검색량은 매일 바뀌는 값이 아니므로 화면에서는 **업체별 최신 non-null 값**을 쓴다.

**subrequest 방어**: 상호 7개(내 매장 + 상위 6)를 KV 캐시로 돌리면 읽기 7 + 쓰기 7 = 14회가 추가된다. **KV도 subrequest로 잡히므로** 여기서는 캐시를 끄고(`getKeywordVolumes(..., { cache: false })`) 5개씩 배치로 fetch 2회만 쓴다. `waitUntil` 안에서 돌려 응답 지연도 없앴다.

#### 6.6.2 관리자 리포트 — 경쟁사 상호 검색량 표

`/admin/:shareId/report`의 "🔍 월간 검색량 (관리자 전용)" 표 **하단**에 "🔍 경쟁사 상호 월간 검색량 (관리자 전용)" 표를 붙였다. 컬럼: 순위 · 상호 · PC · 모바일 · 합계 · 경쟁도. 이쪽은 관리자 경로라 KV 캐시를 그대로 쓴다.

두 표는 **제목 글자 크기를 맞춘다**(`text-sm font-bold` + 설명문 `text-[11px]`). 하위 표처럼 작게 두면 부속 정보로 읽히는데, 상호 검색량은 순위와 대등한 별개 축이라 위계를 낮추면 안 된다.

#### 6.6.3 `/admin/analytics/:keyword` — 순위 변동 시점별 **전 업체** 표

> 사용자 지적: **"1개의 업체만 순위가 떨어졌어도 왜 떨어졌는지 분석하려면 모든 업체 데이터가 필요하다."**

기존엔 순위가 바뀐 **그 업체 한 줄**만 보여줬다. 원인은 대개 그 업체가 아니라 "그 사이 다른 업체가 뭘 했는가"에 있으므로 이 구조로는 추적이 불가능했다.

바뀐 구조 — 순위가 바뀐 **시점마다 표 하나**, 그 시점에 관측된 **모든 업체**를 행으로:

```
2026-09-09 15:50 → 2026-09-10 13:00
업체 | 순위 | 방문자리뷰 | 블로그리뷰 | 투표수 | 사진수 | 사진첨부리뷰 | 쿠폰수 | 월간 상호 검색량
```
- 현재 순위 오름차순 정렬, **순위가 실제로 바뀐 업체는 노란 줄**로 강조
- 지표 칸은 **`1504(+4)` — 현재값(증감)** 형태. 증감만 찍으면 "4건 늘어난 게 큰 건가"를 판단할 기준이 없다(1504 중 4건과 12 중 4건은 의미가 다르다)
- 괄호가 없으면 직전 관측 대비 변화 없음, `–`는 값 자체가 없음
- 증감은 증가 초록 / 감소 빨강
- 최신 **20개 시점**만 렌더 (더 필요하면 SQL로 직접 조회)
- 아무도 안 움직인 시점은 생략

### 6.8 상호 검색량은 "변형의 합"이다 (2026-09-09, 구현 완료)

**사용자 지적**: 상호 하나만 재면 실제 직접 검색 유입이 크게 과소평가된다. 사람들은 `안산 백세보리밥`, `상록구 백세보리밥`, `백세닭한마리`처럼 나눠서 친다. **그 합이 진짜 상호 검색량이다.**

#### 6.8.1 변형 생성 규칙 — 두 번의 실측 실패를 거쳐 확정

변형 출처는 둘이다. ① **네이버 자동완성**(실제 입력 형태) ② **지역명 조합**(자동완성에 안 뜰 만큼 적은 롱테일 보완).

문제는 조합 규칙이었다. 두 번 틀렸고 둘 다 실측으로 잡았다.

**❌ 1차 — 상호의 모든 토큰을 조합했다**
`백세보리밥 닭한마리` → `안산닭한마리` 생성 → **월 230회**. 그런데 이건 *안산에서 닭한마리집을 찾는 사람*이지 우리 매장 손님이 아니다. 상호에 업종어가 섞이면 그대로 과대계상된다.
→ **한국 상호는 대체로 `[브랜드][업종/지점]` 순서다. 첫 토큰만 브랜드로 본다.** 지점 접미사(`본점`·`○○역점`·`N호점`)도 떼어낸다.

또한 광역 지역명(`경기도`)은 아무도 상호 앞에 안 붙인다. 주소 토큰 중 **`시/군/구/동/읍/면`으로 끝나는 것만** 쓴다(`안산`, `상록구`).

**❌ 2차 — 브랜드 토큰 단독을 그대로 합산했다**

| 매장 | 정식 상호 | 브랜드 토큰만 |
|---|---|---|
| 고기굽는방앗간 상록수역점 | 50 | **29,420** |
| 괭이부리마을 상록수점 | 280 | **22,830** |
| 백세보리밥 닭한마리 | 140 | 180 |

앞의 둘은 **전국 지점 전체를 찾는 검색량**이다. 이걸 한 지점에 귀속시키면 588배 부풀려진다.
→ **정식 상호 대비 5배를 넘고 1,000회 이상이면 합계에서 제외한다.** 다만 목록에서 지우지 않고 **취소선으로 남긴다** — 자동 판정이 틀릴 수 있으니 사람이 보고 판단할 여지를 남겨야 한다.

**실측 결과 (2026-09-09)**: 백세보리밥 닭한마리 `140 → 320`. 단일 조회 대비 **2.3배**. 과소평가가 실재했다는 뜻이다.

#### 6.8.1b ❌ 3차 — 브랜드 토큰 자동 판별을 포기했다 (2026-09-09)

`인형수선 안나`에서 **`인형수선`이 월 680회**로 잡혀 합산됐다. 이건 업종 키워드지 이 매장 브랜드가 아니다.

§6.8.1의 "첫 토큰 = 브랜드" 규칙이 깨지는 지점이다. 한국 상호는 두 형태가 섞여 있다:

| 형태 | 예 | 첫 토큰 |
|---|---|---|
| `[브랜드][업종·지점]` | 고기굽는방앗간 상록수역점 | 브랜드 ✅ |
| **`[업종][브랜드]`** | **인형수선 안나** | **업종어 ❌** |

자동 판별을 시도해봤으나 신호가 없다:
- **네이버 카테고리 대조 → 실패.** 실측한 카테고리가 `한식`·`육류,고기요리`·`백숙,삼계탕`처럼 넓어서 상호 토큰과 대조가 안 된다
- **검색량 5배 규칙 → 부분 실패.** `고기굽는방앗간`(29,420)은 걸렀지만 `인형수선`(680)은 1,000회 미만이라 통과한다

**결론: 자동으로 못 가린다. 사람이 판단한다.**
브랜드 토큰 단독 행은 **기본 합산 제외**(`?` 표시)로 두고, 화면에서 체크박스로 켠다. 켜고 끌 때마다 합계가 다시 계산된다. `인형수선`은 끄고 `이동고고갈비`는 켜는 판단은 5초면 되는데, 그걸 코드가 하려다 두 번 틀렸다.

> **설계 원칙**: 이건 관리자 전용 상담 도구다. 애매한 판정을 자동화하다 틀리는 것보다, **판단 재료를 다 보여주고 사람이 고르게** 하는 편이 항상 낫다. 숫자 하나를 확신에 차서 틀리게 내놓으면 상담에서 그대로 신뢰를 잃는다.

#### 6.8.1c ❌ 4차 — 지역 조합에 남의 주소를 썼다 (2026-09-09)

서울 업체인 `인형수선 안나`에 `안산인형수선`·`상록구인형수선` 변형이 붙었다. 프론트가 **내 매장 주소를 전 업체에 돌려쓰고** 있었다.

경쟁사는 대개 같은 상권이라 문제가 안 드러나다가, 다른 지역 업체가 순위에 들어오는 순간 깨진다. `raw_data`에 각 업체 `roadAddress`가 이미 있으므로 `window.__REPORT_REGIONS__`로 매장별 주소를 내려보내 각자의 지역으로 조합하도록 고쳤다.

#### 6.8.2 왜 페이지 로드가 아니라 버튼인가

매장 1곳당 자동완성 1회 + 검색광고 3회 = **4 subrequest**. 리포트의 7곳을 한 번에 돌리면 28회가 페이지 로드에 얹히고, 기존 조회분까지 더하면 한도(50)를 넘긴다(§9.4와 같은 제약).
→ 리포트 표에 **'정밀' 버튼**을 두고 필요한 매장만 요청을 쪼개서 조회한다. 요청이 분리되면 각각 새 한도를 쓴다.

`GET /admin/brand-volume?name=&region=` — 관리자 Basic Auth 안쪽. 응답은 `{ total, underTenCount, excludedTotal, rows[] }`.

### 6.9 검색량 조회 서비스를 공개했을 때의 API 리스크 (2026-09-09 검토)

**질문**: `placewizard.kr/keymany.php`처럼 검색량 조회를 공개 서비스로 열면 네이버 API 제한이나 블랙리스트에 걸리나?

**먼저 구분할 것 — 두 API는 다르다.**
- **네이버 검색 오픈API**(`openapi.naver.com`)는 약관이 명시적으로 빡빡하다. 데이터 **복사·저장·캐싱 금지**, 제3자 제공·판매 금지, AI 학습 활용 금지. 캐싱조차 금지라 우리 KV 캐시 방식과 정면 충돌한다.
- **검색광고 API**(`api.searchad.naver.com`)는 광고주가 자기 계정을 관리하라고 준 것이다. 공개 조회 서비스로 재판매하는 건 취지에서 벗어난다.

**실측 관행**: 마케팅마법사·블랙키위·사장님닷컴 등 다수가 검색광고 API 기반 공개 조회를 운영 중이다. 즉 **사실상 묵인되고 있다.** 다만 "묵인"은 보장이 아니다.

**현실적 리스크 순서**
1. **429 (가장 먼저 온다)** — 계정 단위 초당 호출 제한. 트래픽이 몰리면 즉시 발생
2. **계정 정지** — 광고 계정 자체가 막히면 API도 같이 죽는다
3. 약관 분쟁 — 가능성은 낮지만 서비스가 커질수록 올라간다

**방어 설계 (하려면 이 셋은 필수)**
- **KV 캐시**(이미 있음, 24h). 같은 키워드 재조회는 API를 안 탄다
- **IP당 일일 조회 한도** — §9.8의 `/api/gap` 방어(하루 3건)를 그대로 복제
- **결과를 D1에 축적** — 조회된 키워드를 쌓아두면 시간이 지날수록 API 의존이 줄고, 그 자체가 §6.4 순위 페이지 같은 pSEO 자산이 된다

**판단**: 미끼로 쓸 값어치는 충분하나 **API 키 1개에 공개 트래픽을 그대로 물리면 안 된다.** 위 셋을 갖추고 열되, 대행 수주(§1 수익 1순위)가 본체이고 조회 도구는 유입 장치라는 위치를 유지할 것. 네이버가 막으면 도구는 죽어도 본체는 살아야 한다.

### 6.7 리포트 메일 신청 — 상담 CTA 전면 교체 (2026-09-09 확정, 구현 완료)

**문제**: "내 매장 맞춤 개선 리포트, 무료로 받아보세요 → [연락처 보기]" 버튼이 **전환 0건**이었다. 누르면 번호가 드러나고 `tel:` 링크로 넘어가는 구조.

**사용자 진단 (그대로 채택)**: 원인은 둘이다.
1. **상담받으면 돈 낼까 하는 두려움**
2. **내가 먼저 카톡·문자를 보내야 하는 귀찮음**

전화 걸기 = 영업당하기다. 사장님 입장에서 0건이 정상이다.

**해결**: ①은 문구로, ②는 **이메일 한 칸**으로 없앤다. 카톡 발송은 비용이 들어 나중으로 미룬다 — 메일 없는 사장님은 없다.

#### 6.7.1 무엇을 보내는가 — PDF 첨부가 아니라 링크 (확정)

**Cloudflare Workers는 PDF를 생성할 수 없다**(헤드리스 브라우저가 없다). Apps Script의 HTML→PDF 변환기는 CSS를 대부분 무시하는 데다, 우리 리포트 화면은 JS로 렌더링돼서 변환하면 빈 페이지가 나온다. 진짜 PDF를 첨부하려면 인쇄용 HTML을 서버에서 따로 렌더링해야 하는데 공수가 3~4배다.

그래서 **메일 본문에 요약 + 리포트 링크**로 간다. 링크 페이지에는 이미 인쇄→PDF 버튼이 있어 원하면 본인이 뽑는다. 첨부파일은 스팸함 확률도 올라가고 열람 추적도 안 된다.

**메일 본문에 결핍 상위 3개를 텍스트로 넣는 것이 핵심이다** — 링크를 안 눌러도 값이 남아야 한다.

```
■ 지금 가장 부족한 항목
 1. 블로그·카페 리뷰  현재 12건 → 1페이지 진입선 41건 (29건 부족)
 ...
■ 항목별 개선 방법이 정리된 전체 리포트
   https://dongbiz.com/report/sjzkrwg4
```
정렬은 절대 부족량이 아니라 **부족 비율** 순이다(§2의 "절망 유발 숫자 안 씀"과 같은 이유 — 리뷰 500건 부족이 맨 위에 오면 포기한다).

#### 6.7.2 발송 수단 — 기존 Apps Script 웹훅 재사용 (확정)

새 메일 서비스(Resend 등)를 붙이면 계정·API 키·`dongbiz.com` DNS(SPF/DKIM)가 전부 따라온다. **이미 코드에 붙어 있던 `SHEETS_WEBHOOK_URL`(Apps Script)에 `MailApp` 분기만 추가**한다. 무료, 하루 100통, 새로 필요한 것 없음.

- 스크립트 전문: `worker/apps-script.gs` (설치 순서 주석 포함)
- Worker는 `{ type: 'report_email', to, subject, body }`를 POST, 그 외 payload는 기존대로 시트 append
- **스크립트는 `interpiad@gmail.com` 계정으로 배포한다**(2026-09-09 사용자 변경). Apps Script는 **배포한 구글 계정이 곧 발신자**라, 개인메일로 만들면 개인메일로 나간다. 발신 표시는 `동네장사 <interpiad@gmail.com>`이고 메일 본문 하단 연락처와도 일치한다
- 계정을 옮기면 **웹 앱 URL이 새로 발급된다.** 이전 계정에서 만든 URL은 버리고 `SHEETS_WEBHOOK_URL`을 다시 등록해야 한다
- 남는 한계는 발신 도메인이 `gmail.com`이라는 것. 유입이 붙어 `@dongbiz.com` 발신이 필요해지면 그때 Resend로 갈아탄다

> ⚠️ **2026-09-09 발견**: `SHEETS_WEBHOOK_URL` 시크릿이 **애초에 등록된 적이 없었다.** 구글시트 콜드콜 로깅(§8.2-A)도 그동안 계속 무동작이었다는 뜻이다. 코드가 "미설정 시 조용히 무동작"이라 아무도 몰랐다. 등록으로 두 기능이 같이 살아났다.

#### 6.7.2b Apps Script 함정 3개 (전부 실제로 밟음, 2026-09-09)

**① Apps Script는 실패해도 HTTP 200을 준다 — `res.ok`를 믿으면 안 된다**
스크립트가 예외를 던져도 응답 코드는 200이고, 오류는 **본문에만** 담긴다. 처음 구현이 `res.ok`만 봐서 권한 없이 실패한 발송을 `sent=true`로 기록했다. 리드가 들어와도 메일이 안 나가는 걸 몇 주 몰랐을 수 있는 종류의 버그다.
→ `sendCustomerEmail()`은 **반드시 본문의 `ok` 필드까지 확인**한다. `notify.ts`에 주석으로 박아뒀다.

**② 배포만 하면 Gmail 권한이 안 붙는다**
웹 앱 배포는 스코프 승인을 보장하지 않는다. 승인 없이 호출하면 이 응답이 온다:
```json
{"ok":false,"error":"Exception: MailApp.sendEmail을(를) 호출할 수 있는 권한이 없습니다.
 필요한 권한은 https://www.googleapis.com/auth/script.send_mail입니다."}
```
→ 편집기에서 `authorize()` 함수를 **한 번 직접 실행(▶)**해서 승인창을 띄워야 한다. `apps-script.gs`에 그 용도의 함수를 넣어뒀다(본인에게 테스트 메일 + 잔여 한도 로깅).

**③ 구글 계정이 섞인다**
브라우저에 남은 로그인 세션 때문에, `interpiad@gmail.com`으로 만들려던 스크립트가 개인 계정(`joseungil@`) 소유로 생성됐다. Apps Script는 **소유 계정이 곧 발신자**라 이러면 개인메일로 나간다.
→ 확실한 방법은 **새 브라우저(또는 시크릿창)에서 해당 계정으로만 로그인해 시트부터 새로 만드는 것**. 계정이 바뀌면 웹 앱 URL도 새로 발급되므로 `SHEETS_WEBHOOK_URL`을 다시 등록한다.

#### 6.7.2c 발송 한도와 초과 시 동작

**하루 100통** (일반 `@gmail.com` 계정 기준. Google Workspace 계정이면 1,500통). 자정이 아니라 **롤링 24시간**으로 회복된다.

초과하면 `MailApp.sendEmail`이 예외를 던지고, 그 뒤는 §6.7.4의 실패 경로를 그대로 탄다 — `report_leads`에 `sent=0`으로 남고 **텔레그램 알림이 오므로 리드는 잃지 않는다.** 수동 발송하면 된다.

두 가지를 덧붙였다(2026-09-09):
- **잔여 한도 조기 경보**: Apps Script가 `MailApp.getRemainingDailyQuota()`를 응답에 실어 보내고, **20통 이하로 떨어지면 텔레그램 알림에 경고가 붙는다.** 바닥난 뒤에 아는 건 늦다
- **실패 시 화면 문구 분리**: 발송이 실패했는데 "보내드렸습니다"라고 하면 거짓말이다. `sent=false`면 **"접수됐습니다 · 확인 후 순서대로 보내드리고 있습니다"**로 바뀐다. 텔레그램 알림에는 실패 원인 문자열도 함께 찍는다

**언제 갈아탈 것인가**: 하루 100건은 현재 유입(리드 0건) 대비 한참 여유다. 상한에 닿는다는 건 리드가 하루 100건이라는 뜻이라 그건 좋은 문제다. 그때 선택지는 ① Google Workspace 계정으로 소유권 이전(1,500통, 코드 수정 없음) ② Resend 전환(월 3,000통 무료, `@dongbiz.com` 발신 가능하나 DNS 작업 필요). **①이 훨씬 싸다.**

**참고**: `wrangler secret put`은 등록 직후 몇 초간 전파 지연이 있다. 바로 호출하면 옛 값(또는 미설정)으로 동작할 수 있으니, 실패하면 잠깐 뒤 한 번 더 시도해볼 것. 실제로 첫 호출은 `sent=false`, 30초 뒤 재호출은 `sent=true`였다.

#### 6.7.3 화면 — 고객 리포트와 관리자 리포트 분리

| | 고객 `/report/:shareId` | 관리자 `/admin/:shareId/report` |
|---|---|---|
| 인증 | 없음 | Basic Auth |
| 항목별 개선 주석 | **보인다** | 보인다 |
| 검색량 표 | 안 보임 | 보임 |
| 신청 폼 | 숨김(이미 받은 사람) | 숨김 |

같은 `index.html`을 쓰고 `window.__REPORT_ADMIN__` 플래그로만 가른다. 고객 리포트에 인증을 걸지 않는 이유: 메일 열고 바로 확인하는 게 이 기능의 존재 이유이고, `shareId`는 이미 공유링크로 쓰이는 값이다.

**정책 변경 명시**: §6.2에서 "개선안은 관리자 리포트에만(상품 보호)"이었으나, 리포트 자체가 리드 미끼가 된 이상 **개선 주석이 곧 미끼의 실체**다. 이메일이라는 대가를 받고 공개하는 것이므로 전면 공개와는 다르다.

#### 6.7.4 신뢰 경계

- 이메일 형식 서버 검증(정규식 + 254자 상한). 없으면 웹훅으로 임의 문자열이 흘러간다
- 개인정보 수집 동의 체크박스 필수(`required`)
- 웹훅 발송이 실패해도 `report_leads`에 `sent=0`으로 남기고 **텔레그램 알림을 보낸다** → 수동 발송 가능. 사장님 화면에는 접수 완료로 보인다
- 리드는 `report_leads` 테이블(§schema 6)에 적재


### 6.11 웹문서 배포 — 매장 전용 미니홈피 (2026-09-10 기획, 착수 전)

> **컨셉(사용자)**: `전화번호.dongbiz.com` 형태의 매장 전용 미니홈피를 자동 생성한다.
> 사장님이 손댈 것 없이 **플레이스 대표키워드 + 상세설명 조합**으로 문서를 만들어 보여주고,
> 무료 상태에서는 `noindex`. **비용을 납부하면 IndexNow로 검색 등록**한다.

#### 6.11.1 핵심 질문에 대한 답 — 콘텐츠 차별화는 자동화 가능한가

**가능하다. 단 조건이 있다.**

- ❌ **문장을 바꿔 쓰는 방식으로는 불가능하다.** 같은 템플릿에 상호만 갈아끼우거나, 상세설명을 LLM으로 돌려 쓰는 건 검색엔진이 잡아내는 정확히 그 패턴이다.
- ✅ **매장마다 다른 데이터가 문서의 구조 자체를 결정하게 하면 가능하다.**

이건 이미 검증한 접근이다. §6.4에서 순위 페이지를 만들 때 **"문구 pool이 아니라 데이터 조건이 문단·FAQ 구성을 결정"**하게 해서, 키워드마다 실제로 다른 글이 나오고 분량이 934자 → 2,070~2,346자로 늘었다. 같은 원리를 매장 단위로 적용하는 것이다.

**우리는 이미 매장별 고유 데이터를 수집하고 있다** (`scraper.ts` 반환값 실측):

| 데이터 | 필드 | 차별화 기여 |
|---|---|---|
| **리뷰 투표 키워드 + 횟수** | `topRepKeywords` | **가장 강력하다.** 방문객이 실제로 고른 표현이라 매장마다 완전히 다르다 |
| 메뉴명·가격·추천여부·설명 | `menus` | 업종이 같아도 겹칠 수 없다 |
| 대표키워드 5개 | `keywordList` | 사장님이 직접 고른 것 |
| 상세설명 원문 | `description` | 사장님이 쓴 글 |
| 편의시설·결제수단 | `conveniences`, `paymentMethods` | 조합이 매장마다 다름 |
| 좌표·도로명주소 | `coordinates`, `roadAddress` | 인근 역·상권 문장 생성 가능 |
| 실측 지표 시계열 | `rank_snapshots` | **시간이 지나면 저절로 자란다** (§6.4.1 원칙) |
| 키워드 검색량 | `keyword_volumes` | 무엇을 앞세울지 결정 |

**여기에 자동완성(`autocomplete()`)을 더하면 결정적이다.** 그 상호·업종에 실제로 붙는 질문(`○○ 주차`, `○○ 예약`, `○○ 포장`)을 뽑아 **FAQ를 매장마다 다르게** 구성할 수 있다. 사람들이 실제로 검색하는 질문이라 **콘텐츠 가치가 진짜로 있다** — 이게 "차별화를 위한 차별화"와 갈리는 지점이다.

#### 6.11.2 그래도 정직하게 짚을 위험 4가지

**① 대량 자동 생성은 그 자체가 검색엔진 정책 위반 대상이다**
구글은 2024년 스팸 정책에 **`scaled content abuse`**(대규모 콘텐츠 남용)를 명시적으로 추가했다. "자동 생성이냐"가 아니라 **"사람에게 가치가 있느냐"**가 기준이다. 지표만 나열한 페이지, 상세설명을 재배열한 페이지는 정확히 여기 걸린다.
→ **방어**: 페이지의 주인공은 사장님이 아니라 **그 매장을 찾는 소비자**여야 한다. 소비자는 "리뷰 161건"이 안 궁금하다. 메뉴·가격·주차·예약·영업시간이 궁금하다. 실측 데이터는 문서를 **구성하는 재료**로 쓰되 **전시하지 않는다.**

**② 색인은 요청할 수 있어도 보장할 수 없다**
이건 §6.5.2-3 역-보장 원칙과 직결된다. 상품 문구는 반드시 **"검색 등록 요청"**이지 **"검색 등록 보장"**이 아니어야 한다. 신규 도메인의 대량 하위 페이지는 색인이 안 될 수 있고, 그건 우리가 통제할 수 없다.
→ 판매 문구 후보: *"검색엔진에 색인을 요청하고, 반영 여부를 그대로 알려드립니다."*

**③ IndexNow는 구글에 안 통한다**
IndexNow 참여: **네이버·빙·얀덱스·Seznam**. **구글은 미참여**다.
→ 네이버·빙은 IndexNow로 즉시 통보, **구글은 사이트맵 + 서치콘솔 경로가 따로 필요**하다. "IndexNow로 전 검색엔진 등록"이라고 쓰면 거짓말이 된다. (착수 전 IndexNow 참여사 현황 재확인 필요 — 변동 가능)

**④ 전화번호를 URL로 쓰는 건 개인정보 문제다**
우리는 홈 화면에서 **전화번호를 뒤집어 저장해 스팸 스크래퍼를 피하는 처리까지 했었다**(`OBS_PHONE_REV`, 이후 §6.7에서 제거). 그래놓고 전화번호를 서브도메인에 박으면 **정면으로 모순**이고, 개인 휴대폰 번호인 매장은 그 자체가 수집 대상이 된다.
→ 대안: 플레이스 ID, 매장 슬러그(`baekse-boribap`), 또는 **번호 뒤 8자리**. 사용자 결정 필요(§6.11.6).

#### 6.11.3 URL 설계 — 서브도메인 vs 서브디렉토리

| | `01024900555.dongbiz.com` | `dongbiz.com/p/{slug}` |
|---|---|---|
| 도메인 권위 | **별도 사이트로 취급될 수 있다** | 본 도메인 권위를 그대로 물려받음 |
| 색인 난이도 | 매장마다 새 사이트를 키우는 셈 | 기존 사이트의 하위 페이지 |
| 네이버 웹마스터도구 | **서브도메인마다 등록 필요** | 1회 등록으로 끝 |
| 사장님 체감 | **"내 주소"라는 감각이 강하다** | 평범함 |

**권고: 서브디렉토리를 정식 주소로 두고, 서브도메인은 별칭(301)으로 붙인다.**
마케팅에서는 *"전화번호가 곧 내 홈페이지 주소"*를 그대로 쓸 수 있으면서 SEO 이점은 서브디렉토리 쪽을 챙긴다. Cloudflare에서 와일드카드 DNS + Worker 라우트로 처리 가능하다.

브랜드 체감을 더 중시한다면 서브도메인을 정식으로 쓰되, **초기 색인이 훨씬 느릴 것**을 감수해야 한다.

#### 6.11.4 페이지 구성안 (소비자용, 데이터가 구조를 결정)

```
[히어로]   상호 · 업종 · 한 줄 소개(상세설명에서 추출) · 전화/길찾기 버튼
[메뉴]     menus가 있으면 가격표. 없으면 이 섹션 자체가 사라진다
[이 집은]  topRepKeywords 상위 5개를 문장으로 — "방문객이 가장 많이 고른 표현"
[찾아가기] roadAddress + 좌표 기반 인근 역/상권 안내
[영업정보] 영업시간·편의시설·결제수단·주차 (conveniences 조합대로)
[자주 묻는 것] 자동완성에서 뽑은 실제 질문 3~6개
[사진]     samplePhotos
[구조화데이터] LocalBusiness JSON-LD
```

**핵심 규칙 3가지**
1. **데이터가 없으면 섹션을 만들지 않는다.** 빈 섹션을 문구로 때우는 순간 템플릿이 된다
2. **상세설명을 그대로 복사하지 않는다.** 플레이스와 중복 콘텐츠가 된다. 사실만 뽑아 재구성한다
3. **지표를 전시하지 않는다.** 소비자용 페이지다(§6.11.2-①)

#### 6.11.5 수익 구조

```
무료  : 페이지 자동 생성 + 미리보기 + noindex
유료  : noindex 해제 → sitemap 등록 → IndexNow 통보 → 반영 여부 리포트
```

**이 구조가 좋은 이유**: 미끼에 실체가 있다. "진단해드립니다"가 아니라 **"이미 만들어놨습니다, 보시겠어요"**다. §6.7 이메일 리포트와 같은 계열이고, 전환 장벽이 그보다 더 낮다.

**과금 형태는 1회가 아니라 구독이 맞다** — 호스팅·데이터 갱신·색인 유지가 계속 드는 비용이다. 1회 결제로 팔면 갱신이 멈춘 페이지가 쌓이고, 그게 곧 §6.11.2-①의 저품질 문제가 된다.

#### 6.11.6 확정 사항 (2026-09-10 사용자 결정)

| 항목 | 결정 |
|---|---|
| URL 방식 | **서브디렉토리 정식 + 서브도메인 301 별칭** (권고안 채택) |
| 주소 식별자 | **플레이스 ID** |
| 가격 | **월 25,000원 · 기본 10페이지**, 추가 페이지는 별도 과금 |
| 초기 범위 | **소수로 시작 → 색인 여부 확인 후 확장** (권고안 채택) |
| 삭제 요청 경로 | 샘플 1건을 만들어 보고 정한다 |

**식별자를 플레이스 ID로 정한 이유(사용자)**: 폐업 후 **전화번호가 다른 사람에게 재배정**되는 경우가 있다. 그러면 그 주소가 엉뚱한 매장을 가리키게 되고, 이미 색인된 상태라면 되돌리기 어렵다. 플레이스 ID는 재사용되지 않는다.
→ §6.11.2-④의 개인정보 문제까지 같이 해소된다. **전화번호는 URL에 쓰지 않는다.**

정식 주소는 `dongbiz.com/p/{placeId}`, 별칭은 `{placeId}.dongbiz.com`.

#### 6.11.6b ⚠️ "10페이지"가 설계를 바꾼다 — 페이지 분할 원칙

지금까지 이 기획은 **매장당 1페이지(미니홈피)**를 전제로 했다. 10페이지가 되면 **§6.11.2-①(scaled content abuse) 위험이 10배가 아니라 그 이상으로 커진다** — 같은 매장 정보를 10개로 쪼개면 **내부 중복**이 생기기 때문이다. 외부 중복보다 이쪽이 먼저 문제가 된다.

**분할 기준은 "정보의 종류"가 아니라 "검색 의도 하나"다.**

| ❌ 이렇게 쪼개면 안 된다 | ✅ 이렇게 쪼개야 한다 |
|---|---|
| 같은 소개글을 메뉴편·주차편·예약편으로 자름 | **자동완성·검색량으로 확인된 실제 질문 하나**에 답하는 페이지 |
| 페이지마다 매장 소개가 반복됨 | 페이지마다 답하는 질문이 다르고, 소개는 링크로 넘김 |

페이지 후보는 **그 매장에 실제로 붙는 검색어**에서 나온다(`autocomplete()` + `keyword_volumes`):
`○○ 주차` · `○○ 예약` · `○○ 포장` · `○○ 메뉴` · `○○ 룸` · `○○ 단체` · 대표키워드 5개 조합 등.

> **🚨 판매 문구 주의: "10페이지 제공"이 아니라 "최대 10페이지"여야 한다.**
> 검색 수요가 4개뿐인 매장에 10개를 채우려 하면, 남는 6개는 **내용을 늘리기 위한 페이지**가 된다.
> 그게 정확히 저품질 판정을 받는 형태이고, **한 매장의 저품질 페이지가 도메인 전체 평가를 끌어내린다.**
> 수량을 약속하는 순간 우리 스스로 그 함정에 들어가는 구조가 된다 — §6.5.2-3 역-보장 원칙과 같은 이유로,
> **"검색 수요가 확인된 주제에 대해 최대 10페이지"**로 파는 것이 맞다.

부족한 매장은 페이지 수 대신 **갱신 빈도와 데이터 축적**으로 값어치를 채운다(§6.4.1 "데이터가 쌓일수록 문서가 저절로 길어지는 구조").

#### 6.11.7 재사용 가능한 기존 자산

새로 만들 것이 생각보다 적다.

| 필요한 것 | 상태 |
|---|---|
| 매장 데이터 수집 | ✅ `scrapeFullPlaceMetrics()` 완성 |
| 검색량 | ✅ `getKeywordVolumes()` + `keyword_volumes` |
| 자동완성(FAQ 재료) | ✅ `autocomplete()` |
| 데이터 조건 → 문단 생성 | ✅ §6.4 순위 페이지에서 검증된 패턴 |
| 페이지 렌더링·디자인 | ✅ `pages.ts` 스타일 체계 재사용 |
| 시계열 | ✅ `rank_snapshots` |
| **새로 필요** | 와일드카드 라우팅 · `sites` 테이블 · 결제 · IndexNow 통보 · noindex 토글 |

---
---

## 7. 기술 함정 (반드시 숙지 — 이미 겪은 실수)

### 7.1 Apollo State의 `__ref` 포인터
`window.__APOLLO_STATE__` 파싱 시, 일부 핵심 데이터가 `PlaceDetailBase:{id}` 안이 아니라 `ROOT_QUERY` 내부에 포인터(`__ref`) 형태로 숨어 있다.
`keywordList`(대표키워드), `description`(상세설명), `topPhotos`(사진 탭), `images`(홈 탭 갤러리)는 반드시 `ROOT_QUERY.placeDetail:{...}` 경로로 접근한다.

```javascript
const infoTab = rootQuery[pdKey].informationTab;
const infoRef = infoTab.__ref || infoTab;
const infoObj = typeof infoRef === 'string' ? state[infoRef] : infoRef;
const keywordList = infoObj?.keywordList || [];
```

### 7.2 지표 추출 위치 매핑
- **사진 총 개수**: UI에 노출된 `photoKeys.length`(최대 10개)로 계산하면 안 된다. `topPhotos` / `images`가 `__ref`인지 원본 객체인지 조건부로 판별한 뒤 `total` 또는 `totalImages`를 참조한다.
- **상세설명**: `PlaceDetailBase`에는 없는 경우가 대부분이다. `ROOT_QUERY` 하위의 `pd.description`을 1순위로 참조한다.
- **매장 새소식**: `BiznewsItem:` 또는 `NewsItem:`으로 시작하는 키를 찾아 `createdTs` / `createDate` / `date`를 파싱하여 최근 30일 이내인지 판별한다.
- **찾아오는 길**: `base.directions`가 아니라 **`base.road`**다. (B1)
- **전화번호**: `base.phone`이 없으면 `base.virtualPhone`(스마트콜)로 폴백한다. (B2)

### 7.3 프론트엔드 렌더링
- Raw Data는 `<pre>` 안에 렌더링되므로, 스크롤 `<div>` 등 HTML 태그를 주입할 때는 `.innerText`가 아닌 **`.innerHTML`**을 사용해야 한다.
- 검색결과 컨테이너는 초기 로드 시 완전히 숨겨져 있어야 하며, 백엔드 통신 완료 후에만 노출한다.

### 7.4 ⚠️ 상위 N개 수집 시 중복 ID (신규)
네이버 SERP는 **같은 업체가 상단 비주얼 카드와 하단 피드에 두 번 등장**한다. 기존 정규식을 그냥 10회 반복하면 실제로는 5~6개 업체만 잡히고 평균이 오염된다.
→ `Set`으로 placeId 중복을 제거하면서 10개(순위 표기용으로는 14개)를 채울 때까지 순회한다.

### 7.5 ⚠️ 광고 판별 로직이 취약함 (신규)
현재 구현은 앵커 주변 ±300자에 `'광고'` 문자열이 있는지 검사한다. 리뷰 본문 등에 "광고"라는 단어만 있어도 오가닉 업체가 광고로 잘못 걸러져 **순위 전체가 통째로 밀린다.** 서비스 신뢰와 직결되는 지점이다.
→ 앵커의 부모 컨테이너 단위로 판별하도록 교체한다. 최소한 `ico_ad` / `sp_ad` 클래스만 신뢰하고 `'광고'` 문자열 검사는 제거하는 편이 오탐이 적다.

### 7.6 평균 왜곡 방어 (신규)
상위 10개에 리뷰 5,000건짜리 프랜차이즈가 하나 끼면 평균이 튄다. **평균과 중앙값을 모두 계산**하여 둘의 차이가 크면 중앙값으로 표기한다. 계산 비용은 0이다.

### 7.7 스크랩 횟수와 차단 (신규)
1회 Gap 분석 = 내 매장 1 + 상위 14 = **최대 15회 fetch**. `Promise.all`로 병렬 처리하면 Worker subrequest 한도 내에서 처리 가능하다.
다만 **캐시 없이 운영하면 네이버 차단이 확정적**이다. 같은 키워드를 여러 사장님이 조회하므로 키워드 단위 캐시의 효율이 특히 높다.

**실측 사례(신규)**: 고정 리서치 키워드 수동 재트리거를 짧은 시간(약 10분) 안에 3~4번 반복했더니, **그 특정 키워드/업체들에 한해서만** `플레이스 상태 데이터를 추출할 수 없습니다`(APOLLO_STATE 파싱 실패, 즉 정상 HTML이 안 옴) 에러가 발생함. 전혀 다른 새 키워드("강릉맛집")로는 동시에 정상 작동했음 — **전면 IP 차단이 아니라 같은 대상을 짧은 시간에 반복 조회할 때 걸리는 국소적·일시적 제한**으로 보인다. §1(IP 차단 위험 답변)의 "Cloudflare IP 풀이라 전면 차단은 어렵다"는 판단과 일치하되, **좁은 범위 반복 조회는 여전히 위험**하다는 게 실증됨. 관리자 수동 트리거(`/admin/cron/run/:job`)를 쓸 때 같은 job을 짧은 간격으로 연타하지 말 것 — 몇 분 정도 텀을 두면 자연 회복되는 것으로 보임(확실한 회복 시간은 미측정).

**"Cloudflare는 IP가 매 요청 바뀌는데 왜 막혔나" — 가설 3개 (확정 아님, 정황 증거뿐)**:
1. **대상(URL/키워드) 기준 제한**: 요청자가 누구든 상관없이 "이 리소스가 몇 초 안에 몇 번 조회됐나"로 판단하는 방식이면 IP 회전과 무관하게 걸린다. 실측 정황(반복대상만 막히고 새 대상은 됨)이 이 가설과 가장 잘 맞음.
2. **요청 지문(fingerprint) 기준**: User-Agent가 매번 완전히 동일하고, 쿠키 없음, JS 미실행, 초 단위 반복 — IP가 달라도 이 패턴 자체가 봇 신호로 읽힐 수 있다.
3. **ASN/데이터센터 대역 기준**: 개별 IP는 바뀌어도 Cloudflare가 쓰는 IP 대역 자체를 "데이터센터/프록시 트래픽"으로 통째로 분류해 더 엄격한 기준을 적용할 수 있다. 이러면 "IP가 바뀐다"는 방어 효과가 사실상 없어진다.

가설 1이 지금까지 증거와 가장 부합하지만 확정할 방법은 없다(그 시점 네이버 쪽 다른 이슈였을 가능성도 완전히 배제 못함).

**고객 진단 요청도 이 위험에 걸리는가**: 낮은 편으로 판단. 근거 —
- 정상 사용은 "같은 대상 반복조회"가 아니라 매 고객이 각자 다른 매장/키워드를 1회씩 조회하는 패턴이라, 우리가 겪은 것(같은 URL 3~4회 연타)과 다르다.
- KV 캐시(키워드 6h/매장 24h)가 이미 이 위험을 흡수한다 — 여러 고객이 같은 인기 키워드를 조회해도 **최초 1건만** 실제로 네이버에 요청이 나가고 나머지는 캐시에서 응답한다. 반복조회 자체가 애초에 안 일어남.
- 잔여 위험: 캐시가 없는 **최초 조회** 타이밍이 하필 관리자가 방금 수동테스트로 두들긴 대상과 겹치면 그 고객 요청도 같이 실패할 수 있다(좁은 시간창, 실측상 10분 내외).
- 가설 2·3(지문/ASN 기준)이 맞다면 고객 트래픽 총량이 쌓이며 서서히 위험이 커질 수 있으나, 지금까지 증거는 가설 1(대상 기준)에 더 부합해 당장 급한 문제는 아니라고 판단. User-Agent 로테이션 등 보강은 필요해지면 검토.

### 7.8 ⚠️ `base.*` 구버전 필드 vs `ROOT_QUERY.placeDetail.*` 신버전 필드 (신규, 중요)
`PlaceDetailBase:{id}` 객체(`base`)의 일부 필드는 네이버가 UI를 마이그레이션하면서 더 이상 갱신하지 않는 **구버전 필드**로 추정된다. 실제 값은 `ROOT_QUERY.placeDetail`(`pd`) 하위의 다른 이름 필드에 있다. 실사용 중 사용자 리포트로 2건 확인됨:

- **영업시간**: `base.missingInfo.isBizHourMissing`과 `base.businessHours`는 실제 등록 여부와 무관하게 신뢰할 수 없음(`businessHours`는 거의 항상 `null`). 실제 등록된 영업시간은 `pd.newBusinessHours`(요일별 `WorkingHoursInfo` 배열)에 있다. **반드시 `pd.newBusinessHours` 배열의 존재/길이로 직접 판별할 것** — `missingInfo` 플래그를 그대로 믿지 말 것.
- **블로그/카페 리뷰수**: `base.cafeBlogReviewsTotal`은 실측 사례에서 `0`으로 나왔으나 실제로는 898건이었다. 진짜 값은 `pd.fsasReviews.total`에 있다(리뷰 개별 항목은 `FsasReview:` 접두 키로 별도 저장됨). **`pd.fsasReviews.total`을 우선 사용하고 `base.cafeBlogReviewsTotal`은 폴백으로만 둘 것.**

**교훈**: `base.*`에서 값이 나온다고 끝난 게 아니다. 특히 boolean 플래그(`missingInfo.*`)나 집계 숫자(`*Total`)는 `pd` 하위에 최신 필드가 따로 있을 가능성을 항상 의심하고, 의심되면 실제 매장으로 교차검증할 것. 현재 `isMenuImageMissing`/`isAccessorMissing`/`isConveniencesMissing`/`isDescriptionMissing`은 아직 반례가 발견되지 않아 `missingInfo` 값을 그대로 쓰고 있으나, 검증된 것은 아니다.

### 7.9 ⚠️ KV 캐시가 "고친 버그"를 최대 24시간 되살린다 (신규, 중요)
B10(영업시간)을 배포한 뒤에도 사용자가 같은 매장을 재조회하면 여전히 버그 증상이 나왔다. 원인은 코드가 아니라 **배포 직후~직전에 캐시된 예전(버그) 응답이 KV에 24시간 TTL로 남아있었던 것**. 이미 두 번 겪었다.
→ **해결**: `index.ts`의 `CACHE_VERSION` 상수를 도입해 모든 캐시 키 앞에 버전을 붙인다(`v2:place:...`). **`scraper.ts`의 추출 로직을 바꾸는 배포마다 `CACHE_VERSION`을 반드시 올릴 것.** 버전을 올리면 이전 키는 자동으로 무효화되어 `wrangler kv key delete`를 수동으로 할 필요가 없다. 이 룰을 어기면 "고쳤는데 왜 아직도 안 고쳐졌냐"는 재현하기 혼란스러운 리포트가 또 나온다.

### 7.10 ⚠️ 데스크톱/모바일 통합검색 위젯은 "진짜 상위 10~14위"가 아니다 (신규, 중요·미해결)
현재 `getOrganicRanking`은 `search.naver.com`(desktop 통합검색)의 "플레이스" 위젯 HTML을 파싱한다. 실사용 리포트로 확인됨: 특정 키워드("안산 닭한마리")에서 실제 네이버 지도 순위 8위인 매장이 우리 결과에는 아예 안 잡히고, 위젯 자체에 **오가닉 업체가 6개밖에 없었다.**

**원인 규명**:
- 데스크톱·모바일 통합검색의 "플레이스" 위젯은 애초에 전체 순위 리스트가 아니라 **상위 소수(보통 3~7개)만 보여주는 미리보기 위젯**이다. "지도에서 더보기"를 눌러야 전체 리스트가 나오는데, 이건 별도의 클라이언트사이드 API 호출이다.
- 그 API(`map.naver.com/p/api/search/allSearch`, 백엔드는 `bff-gateway.place.naver.com/graphql`)를 직접 호출해봤으나 **`ncaptcha`(네이버 봇 차단)에 즉시 걸림.** 쿠키 워밍업을 시도해도 동일. 스크립트성 요청을 강하게 차단하고 있어, 우회를 계속 시도하면 IP 차단으로 이어질 위험이 있어 **중단함.**
- 즉 지금 데이터 소스(통합검색 위젯)는 **인기 키워드에서는 우연히 10~14개가 다 잡히지만(예: "안산맛집"은 14개 확인됨), 틈새/저경쟁 키워드에서는 위젯 자체가 몇 개 안 보여줘서 진짜 순위를 놓친다.** 스크래퍼 버그가 아니라 데이터 소스의 구조적 한계.

**임시 조치 (적용 완료)**: "상위 10개 업체"라고 과장 표기하던 문구를 실제로 찾은 개수(`rankSearched`)로 동적 표기하도록 프론트를 수정. 10개 미만이면 "네이버 검색 결과가 N곳뿐이라 10개 미만입니다" 안내 문구 추가. 순위/등급 계산은 찾은 범위 내에서만 정직하게 이루어짐.

**근본 해결은 미착수 — 방향 결정 필요 (§8.3 보류에 추가)**. 후보:
1. 네이버 공식 "지역 검색 오픈 API"(`openapi.naver.com/v1/search/local.json`) 연동 — 캡차 없음, 무료 할당량 있음, 다만 이건 "SEO 오가�)닉 순위"가 아니라 **관련도순 지역검색 결과**라 지금 쓰는 "오가닉 순위"와 의미가 다를 수 있음. 사용 전 실제 순서가 지도 순위와 얼마나 일치하는지 검증 필요
2. 헤드리스 브라우저(예: 별도 서버에서 Playwright)로 실제 지도 페이지를 렌더링해 캡차 우회 — 인프라 복잡도·비용 큰 폭 상승, Worker 아키텍처 이탈
3. 현재 방식 유지, "위젯에 보이는 만큼만" 한계를 명확히 고지하고 넘어감 — 이미 적용한 동적 개수 표기가 이 방향의 최종 형태

### 7.11 ⚠️ 매장새소식은 `BiznewsItem:`/`NewsItem:` 키가 아니라 별도 `/feed` 탭의 `Feed:` 키 (신규, 중요)
`recentNewsDate`가 실제로 소식을 등록한 매장에서도 항상 `null`이 나오는 버그를 실사용 리포트로 발견. 원인은 애초에 스펙이 틀렸던 것 — 지시서 §5.2에 적혀있던 `BiznewsItem:`/`NewsItem:` 키 접두사는 **네이버 데이터에 존재하지 않는 이름**이었다(검증 없이 작성된 추측성 스펙으로 추정). 실제로는:
- `/home` 탭 응답에는 소식 데이터 자체가 아예 없다(`pd.hasFeed.feedExist: true`로 존재 여부만 알 수 있을 뿐).
- 진짜 데이터는 별도 `/feed` 탭(`m.place.naver.com/place/{id}/feed`)에만 있고, 키는 `Feed:{placeId}_{feedId}`, 날짜는 `createdString`(YYYYMMDD 문자열) 필드.
- `type` 필드로 `'FEED'`(매장이 직접 올린 소식/이벤트)와 `'BLOG'`(블로그 언급) 등이 섞여 있으므로 `type==='FEED'`만 필터링해야 한다.

→ `scrapeFullPlaceMetrics(queryOrId, includeFeed)`에 `includeFeed` 파라미터 추가. `/feed` 탭은 요청이 1회 더 늘어나므로 **내 매장 조회 시에만 `true`로 켜고, 경쟁사(최대 10~14곳) 스크랩에는 켜지 않는다** — 경쟁사 소식은 화면에 표시하지 않으므로 불필요한 fetch. `CACHE_VERSION` 올림(v2→v3) 필수(§7.9 규칙).

### 7.12 버튼 재진입 시 로딩 애니메이션이 영원히 안 멎는 버그
`withLoading()`이 `loadingInterval`을 공용 변수 하나에 저장하는데, 키워드 입력창의 Enter 키 핸들러는 버튼의 `disabled` 상태와 무관하게 `runGap()`을 그대로 호출한다. 진행 중에 Enter를 한 번 더 누르면 두 번째 호출이 `loadingInterval`을 덮어쓰고, 첫 번째가 만든 인터벌은 참조를 잃어 영원히 안 멎는다(첫 번째가 끝나도 자기 게 아닌 두 번째 인터벌을 지움). → `withLoading()` 최상단에 `if (btn.disabled) return;` 가드 추가. 모든 진입경로(클릭·Enter)가 이 함수 하나를 거치므로 한 곳만 고치면 전부 막힌다.

### 7.13 ⚠️⚠️ "새로 오픈했어요" 캐러셀이 순위 리스트에 절반 넘게 섞여 들어감 (신규, 심각·수정완료)
사용자가 스크린샷으로 직접 찾아낸 원인. 네이버 통합검색 플레이스 위젯 중간에 **"새로 오픈했어요"**(최근 3개월 이내 개업 매장을 홍보하는 별도 캐러셀 섹션, `id="nop-main-section-root"`)가 끼어있는데, 우리 정규식이 이 캐러셀의 업체 링크도 순위 리스트와 구분 없이 그대로 주워담고 있었다.

**실측 피해 규모**: "안산 상록구 맛집" 키워드에서 뽑힌 14개 중 **9개(6~14위, 64%)가 전부 이 캐러셀**이었다. 그중 하나("야화 혼술바 상록수점")가 하필 블로그 리뷰 2건짜리라 진입선(10위 boundary)이 실제보다 터무니없이 낮게 잡히는 것으로 발견됨.

**해결**: 캐러셀 항목은 `data-nop_res-doc-id="{placeId}"` 속성으로 명확히 구분된다(광고의 `ico_ad`/`sp_ad`, `data-nmb_rese-doc-id`의 `_nad-` 접미사와 같은 급의 마커). 이 속성이 붙은 ID는 오가닉 순위 추출 시 전부 제외. 제외해도 스캔은 캐러셀 뒤까지 계속되므로, 캐러셀 너머에 있던 진짜 순위(예: 진짜 6위)를 정상적으로 찾아낸다.

**참고**: §7.4(광고 판별)·§7.10(위젯이 전체 순위가 아님)과 함께 오가닉 순위 추출의 3대 함정. 이번 건이 실질적 피해가 가장 컸다 — 절반 넘는 "경쟁사"가 실제로는 경쟁사가 아니었던 것이므로.

### 7.14 💡 발견만 해두고 보류: `<a href>` 정규식보다 안정적인 내장 JSON 소스 존재
§7.13을 조사하다가 검색 결과 페이지 자체에 `PlaceListBusinessesItem:{id}:{id}` 형태로 업체 정보가 **이미 JSON으로 내장**되어 있는 걸 발견함(같은 요청, 추가 fetch·캡차 위험 없음). 지금 방식(`<a href="map.naver.com/...">` 정규식)은 네이버가 그 업체를 실제 링크로 렌더링했을 때만 잡히는데, 실측해보니 **같은 업체라도 요청마다 링크로 나올 때도 있고 JS 상태로만 존재해 정규식이 놓칠 때도 있었다**("안산 상록구 맛집"에서 "도깨비왕감자탕" 1곳을 이 방식으로 놓침, 7개 중 6개만 잡힘).
- 이 JSON은 `blogCafeReviewCount`, `roadAddress`, `category`, `phone` 등 필드도 같이 갖고 있어, 순위 추출뿐 아니라 지표 일부도 검색 결과 1회 요청만으로 얻을 수 있을 가능성이 있음.
- 다만 이 블록은 `window.__APOLLO_STATE__`처럼 깔끔한 최상위 변수 할당이 아니라 `naver.place.salt.config` 계열의 큰 초기화 스크립트 안에 묻혀있어, 괄호 짝을 맞춰가며 개별 항목 단위로 파싱해야 함 — 지금의 단순 정규식 한 줄보다 구현 비용이 있음.
- **사용자 확인 결과 "지금은 그대로" 유지 결정.** 나중에 순위 누락이 반복 이슈가 되면 이 방식으로 전환 검토할 것. 코드는 미변경.
- **(2026-09-08 재검토·전환 완료)** §7.14에서 보류했던 그 방식으로 실제 전환함 — 아래 §7.15 참조.

### 7.15 🚨 실사용 버그: `<a href>` 순서가 실제 순위와 다름 → JSON 배열로 전환, 잔여 한계(위치 개인화) 확인
**사용자 리포트**: "꿈있는공방(인형병원 키워드) 실제 1위인데 4위로 나옴."

**원인 조사**: `naver_search.html`을 직접 받아 대조한 결과, 페이지에 이미 박혀있는 `ROOT_QUERY.businesses(...).items` 배열(`{"__ref":"PlaceListBusinessesItem:{id}"}` 목록, §7.14에서 발견해두고 보류했던 그 JSON)은 꿈있는공방을 정확히 1위로 담고 있었음. 반면 기존 `<a href="map.naver.com/...">` 정규식 스캔은 순서가 완전히 뒤섞여 있었고(1위 업체가 4위로), 심지어 그 JSON 배열에는 없는 엉뚱한 업체("호랑이가게" 등, 다른 위젯에서 흘러든 링크로 추정)까지 섞여 들어오고 있었다. **DOM에 렌더링된 `<a href>` 순서 자체가 신뢰할 수 없는 소스였음.**

**수정**: `getOrganicRanking()`이 이제 `"__typename":"PlaceListBusinesses"` 앵커를 찾아 그 안의 `items` 배열에서 `PlaceListBusinessesItem:(\d+)` id를 순서 그대로 추출하는 방식을 최우선으로 쓴다. 이 배열은 광고/'새로 오픈했어요' 캐러셀과 별개 구조라 그 필터링도 자동으로 불필요해짐(실측: `nop_res` doc-id와 겹치는 항목 0건). 기존 href 스캔 코드는 이 JSON 구조를 못 찾을 때만 쓰는 폴백으로 남겨둠(네이버 구조 변경 대비). `CACHE_VERSION`을 v5→v8로 올려 캐시된 잘못된 순위를 즉시 무효화.

**수정 후에도 남은 한계(별개 문제)**: 위 수정 배포 후에도 Worker에서 조회하면 여전히 4위로 나옴 — 이번엔 파싱 버그가 아니라, **Naver의 `items` 배열 자체가 요청자 IP의 위치(GeoIP로 추정)에 따라 다른 순서로 내려옴**을 실측으로 확인했기 때문. 같은 "인형병원" 키워드를, 내 로컬(한국 IP)에서 반복 조회하면 매번 안정적으로 꿈있는공방이 1위(3회 재현 확인)인데, Cloudflare Worker(데이터센터 IP)에서 조회하면 다른 업체 순서가 오고 이것도 매번 일관됨(=요청 자체는 안정적, IP에 따른 결과만 다름). URL에 `x`/`y` 좌표 파라미터를 실어봐도 순서가 안 바뀌는 것까지 확인해서, 클라이언트 요청 파라미터로 우회할 방법은 못 찾음 — 서버사이드 GeoIP 기반으로 추정.
- "지역명이 없는 업종 키워드"(예: "인형병원")에서 이 영향이 발생. "안산 상록구맛집"처럼 지역명이 이미 박힌 키워드는 이 위치 개인화의 영향이 적을 것으로 추정되나 미검증(이번 세션에서 별도 확인 안 함).
- **현재로선 완전한 해결책 없음.** 사용자에게는 "가능하면 지역명을 포함한 키워드로 진단하라"고 안내하는 게 최선. 부분적 우회 아이디어(추후 검토 대상, 미착수): 매장 주소 인근 도시로 GeoIP가 잡히는 별도 프록시/Worker 리전 사용, 또는 네이버 로그인 세션+동네설정 쿠키를 흉내내는 방법 — 둘 다 구현 비용·리스크 있음.



### 8.1 확정 (재논의 금지)

| 사안 | 결론 |
|---|---|
| 고객 동선 | 1단계 진단 → 2단계 키워드 입력 Gap 분석 → 3단계 개별 처방. 3단계 구조 확정 |
| 비교 기준 | 상위 10개 평균(본인 10위 내면 제외한 9개). 1위 단독 비교 폐기 |
| 행동 유발 숫자 | "역전까지 N년" 폐기. **10위 업체값 = 진입선** 기준의 부족분 표기로 대체 |
| 1단계 공개 범위 | 전면 공개. 리드 게이트를 걸지 않는다 |
| 처방 정렬 | 결핍 크기 순이 아니라 "싸고 빨리 되는 것" 순 |
| 무료 항목 | 무료 셀프 가이드를 최소 2개 섞는다 |
| 수익 순서 | 대행 수주 우선, SaaS 후순위 |
| 캐싱 | 필수. KV, placeId 24h / keyword 6h |
| fallback 매장 | 삭제. 엉뚱한 비교를 하느니 "경쟁사 없음"으로 정직하게 응답 |
| 키워드 생성 | 사용자 직접 입력. 하드코딩 자동 생성 폐기 |
| 법적 고지 | 푸터에 "네이버와 무관한 독립 서비스" 고지. 휴대폰 수집 시 동의 체크박스 필수 |
| 오가닉 순위 데이터 소스 (`§7.10`) | **현재 방식(통합검색 위젯) 유지.** 공식 지역검색 API·헤드리스브라우저는 채택 안 함. 틈새 키워드에서 10개 미만만 잡히는 한계는 동적 개수 표기로 정직하게 고지하는 선에서 마무리 |

### 8.2 알림 채널 — 2트랙으로 분리 (확정)

하나로 통일하려 하면 답이 나오지 않는다. **운영자용과 고객용은 요구사항이 완전히 다르다.**

**A. 운영자 알림 — 텔레그램 + 구글시트 (지금 바로, Phase B)**
- 텔레그램: 무료, 심사 없음, 봇 토큰 발급 5분, Worker에서 `fetch` 한 줄. 파싱 실패·차단·신규 진단·상담 유입을 즉시 푸시. 사장님용으로는 부적합하지만 **운영자용으로는 압도적 1위**.
- 구글시트: 진단 이력 누적 = 콜드콜 영업 리스트. Apps Script 웹훅 URL 하나로 append. 개발 비용 0, 정렬·필터·공유가 공짜라 영업팀이 바로 쓴다.
- 비용 0원. 오늘 붙여도 된다.

**B. 고객 알림 — 카카오 알림톡 (Phase D, 유료 SaaS 붙을 때)**
- 타겟(40~60대 로컬 자영업)이 하루 종일 보는 화면이 카톡이다. 텔레그램 침투율은 사실상 0, 메일 열람률은 바닥. **대안이 없다.**
- 병목은 리드타임: 발신프로필 등록 + 템플릿 심사에 1~2주. 그래서 **채널 확정이 아니라 심사 신청만 미리 걸어둔다.**
- 브릿지: 심사 전 초기 소수 고객은 LMS 문자(심사 없음, 즉시 시작, 건당 ~30원). 광고성이면 `(광고)` 표기 + 무료수신거부번호 의무.
- 단가: 알림톡 6.5~9원 vs LMS 30원. 규모가 커지면 알림톡이 4배 저렴하므로 결국 알림톡으로 간다.

**지금 할 일은 하나**: `notify(대상, 메시지)` 함수 하나만 뚫고 내부를 텔레그램으로 채운다. 나중에 내부만 알림톡으로 교체한다. **어댑터 계층이나 설정 파일을 만들지 않는다.** 함수 1개면 충분하고, 이렇게 하면 채널 결정을 미룬 채로 개발을 진행할 수 있다.

### 8.3 보류 (방향 미확정 — 임의 구현 금지)

| 항목 | 상태 | 비고 |
|---|---|---|
| 휴대폰 번호 수집 / 리드폼 | **보류, 화면에서 숨김** | 코드는 남겨두되(`hidden` 클래스) 사용자 눈에 안 보이게 처리. 수집 시점·동의 문구 미확정 |
| 알림톡 발송 기능 | **보류** | `§8.2`-B에 따라 Phase D. 심사 신청만 선행 |
| 블러 게이트 (부분 공개) | **보류** | 어느 항목을 가릴지 미확정. 후보는 2단계 Gap 카드 (1단계는 전면 공개 확정) |
| PDF 리포트 다운로드 | **보류** | 리드 수집 방향 확정 후 |
| `users` 테이블 | 스키마만 유지 | 지금 채울 데이터가 없다. 구현은 Phase D. (`daily_ranks`는 `rank_snapshots`로 대체되어 이미 사용 중 — §9 참조. 아래 신설 섹션) |

---

## 9. 리버스 엔지니어링 데이터 파이프라인

> 목표(사용자 정의): 순위 변동을 관측하고 그 시점의 지표 변화와 대조해 네이버 플레이스 상위노출 로직을 역산한다. 아래는 그 파이프라인의 현재 상태.

### 9.1 구조

```
사용자가 /api/gap 호출
  → 그 시점의 (키워드 × 업체 × 순위 × 전지표) 스냅샷을 rank_snapshots에 한 행씩 기록 (source='user')
  → 다음날 cron이 그 키워드를 기억해뒀다가 자동 재수집 (source='cron')
  → 시간이 지나며 시계열이 쌓임
  → 관리자 대시보드(/admin/analytics)에서 순위 추이 + 순위변동 시점의 지표 델타를 확인
```

**핵심 설계**: 처음부터 전지표를 평탄화해서 쌓는다. `daily_ranks`(순위+리뷰수만 남기던 구스텁, Phase D 스텁이었음)를 폐기하고 `rank_snapshots`로 대체 — 나중에 "순위가 바뀔 때 뭐가 같이 바뀌었나"를 SQL 한 줄로 물어볼 수 있어야 리버스엔지니어링이 가능하기 때문.

### 9.2 `rank_snapshots` 테이블 (`worker/schema.sql`)
키워드-업체-시점 단위 1행. 컬럼: `rank`, `visitor_reviews`, `blog_reviews`, `vote_count`, `photo_count`, `review_score`, `review_medias_total`(사진첨부리뷰), `coupon_count`, `has_booking`, `has_smart_order`, `has_review_penalty`, `is_new_opening`, `is_good_store`, `is_open_now`, `is_biz_hour_missing`, `description_length`, `name_contains_keyword`, `category_matches_keyword`, `distance_from_me_km`, `source`(`user`|`cron`), `collected_at`.
`place_id`에 **의도적으로 FK를 걸지 않았다** — §B9에서 겪은 대로 FK는 두 진입점의 upsert 타이밍이 어긋나면 조용히 깨진다. 경쟁사는 `places` 테이블에 upsert 안 하므로 여기선 단순 TEXT.

### 9.3 신규 수집 필드 (질문4 A/B그룹, `worker/src/scraper.ts` + `index.ts`)
전부 추가 요청 없이 이미 받고 있던 응답에서 실측·확인 후 반영:
- `hasReviewPenalty`, `isNewOpening`, `hasNaverBooking`, `hasSmartOrder`, `couponCount`, `reviewMediasTotal`, `isGoodStore`, `isOpenNow` — `pd`(ROOT_QUERY.placeDetail) 하위에서 실측 확인.
- `rankFactors.nameContainsKeyword` / `categoryMatchesKeyword` / `distanceFromMeKm` — 별도 수집 없이 기존 name/category/좌표만으로 `index.ts`에서 파생 계산(`keywordMatch()`, `haversineKm()`).

### 9.4 자동 시계열 수집 (cron)
크론 트리거를 **3개**로 분산한다(`wrangler.toml`). 이유: 고정 리서치 키워드 8개(§9.4b)를 하루에 다 돌리면 8×최대11=88 subrequest로 Worker 한도(50)를 넘는다.

**시각은 전부 12시(KST) 이후로 잡는다.** 네이버 순위가 매일 10~12시(KST) 사이에 변동되기 때문 — 12시 전에 돌리면 전날 순위를 또 재는 꼴이 된다. (최초엔 09:00 KST에 하나 걸려 있었는데, 사용자 지적으로 셋 다 재조정함.)

| 시각(UTC/KST) | 하는 일 |
|---|---|
| `0 4 * * *` (04:00/13:00) | 헬스체크 + 고정 리서치 키워드 앞 4개 |
| `0 8 * * *` (08:00/17:00) | 사용자 검색 키워드 자동 재수집(`runDailyKeywordCollection`, 최대 `CRON_KEYWORD_BATCH_LIMIT`=3개, `rank_snapshots` 업체별 세부) |
| `0 12 * * *` (12:00/21:00) | 고정 리서치 키워드 뒤 4개 |

`wrangler.toml`의 `[triggers] crons`와 `index.ts`의 `CRON_TIMES` 상수가 반드시 일치해야 한다 — 어긋나면 `scheduled` 핸들러의 `event.cron` 분기가 아무것도 안 걸려서 조용히 무동작한다.

사용자 검색 키워드 배치는 `rank_snapshots`에서 `GROUP BY keyword ORDER BY 최근검색일시 DESC`로 뽑으므로, 최근에 검색이 몰리면 오래된 키워드는 로테이션에서 밀려날 수 있다(하드 고정 아님). 고정 리서치 키워드는 이 규칙과 무관하게 **매일 무조건** 포함된다(§9.4b).

#### 9.4b 리서치용 고정 키워드 (사용자 지정, `FIXED_RESEARCH_KEYWORDS`)
경쟁이 치열해 순위 변동이 잦은 유명 키워드 8개를 매일 고정 관측: 강남맛집·명동맛집·부산맛집·해운대맛집·제주도맛집·강남미용실·홍대미용실·강남성형외과.

**저장 범위 (2026-09-08 변경)**: `keyword_rank_stats`(집계 전용, 키워드당 1행) + `rank_snapshots`(업체별 세부, 상위 7곳) **둘 다** 기록한다. `collectKeywordAggregateStats()` 한 함수 안에서 처리 — 이미 긁어온 데이터라 batch 쓰기 1회(subrequest +1/키워드)만 추가된다.

- 원래는 "특정 업체 지표는 저장하지 않는다"였다(사용자 요청, D1 쓰기량 절감 목적). **철회 이유**: 집계 평균만으로는 §9.1의 핵심 질문("순위가 바뀔 때 뭐가 같이 바뀌었나")에 답할 수 없어 리버스엔지니어링 목적 자체가 반쪽이 된다. 비용 우려도 재계산해보니 근거가 약했다 — 8키워드×7곳 = **56행/일**, D1 무료 한도 10만 write/일 대비 무시할 수준.
- subrequest는 키워드당 9→10, 4개 배치 기준 36→40으로 한도(50) 안쪽 유지.
- 부수 효과: 고정 리서치 키워드 8개가 이제 `/admin/analytics` 목록에도 함께 뜬다(예전엔 다른 테이블이라 안 떴음). `runDailyKeywordCollection()`은 `fixedSet`으로 이미 걸러내므로 중복 수집은 없다.

**subrequest 한도 실측 교훈**: 4개 키워드가 "같은 Worker 호출 안에서 순차 누적"된다는 걸 실측으로 확인했다(당초 계산 착오). 미용실처럼 오가닉 10곳이 꽉 찬 키워드가 섞이면 4개만 돌려도 한도(50)를 넘어 `Too many subrequests` 에러가 남 — D1 쓰기도 subrequest로 잡힌다. 그래서 상위 7곳으로 낮췄고(1검색+7업체+1DB쓰기=9, ×4=36으로 안전), 사용자 검색 키워드 배치도 6→3으로 다시 낮췄다(§9.4 표에 반영). "배치제한 올림" 요청이 있었지만 실측으로 안전 마진을 재확인한 뒤 조정한 것 — 무작정 올리면 실패한다.

**관리자 수동 트리거**: `GET /admin/analytics` 없이도 다음 정기 실행을 안 기다리고 바로 씨앗 심고 싶을 때 `GET /admin/cron/run/:job`(`fixed-a`|`fixed-b`|`user-driven`|`healthcheck`, Basic Auth 필요)로 즉시 실행 가능. **단, 같은 job을 짧은 간격으로 연타하지 말 것** — §7.7에 실측 기록된 국소적 일시 제한에 걸릴 수 있다.

`category: 'medical'`(강남성형외과)는 **분석에서 반드시 분리** — 의료광고법상 네이버 랭킹 로직 자체가 일반 업종과 다르게 취급될 가능성이 높다(실제로 `visitorReviewsScore`가 병원 카테고리에서 자주 "비공개"로 나오는 것도 이 때문일 수 있음). 대시보드(§9.5)에서 이미 표를 분리해뒀다.

실측 흥미로운 점: 미용실 키워드(강남미용실·홍대미용실)는 오가닉 10곳이 꽉 찼는데(§7.10 한계를 거의 안 받음), 지역 맛집 키워드는 3~5곳뿐이었다. **유명 키워드일수록 위젯이 더 완전한 리스트를 보여주는 경향** — §7.14에서 다루려던 "10개 보장 안 됨" 문제가 유명 키워드에서는 자연히 완화됨.

### 9.5 대시보드 — Basic Auth 필요
- **`/admin/analytics`**: 키워드 목록(스냅샷 수·관측 업체 수·관측 기간·cron 재수집 여부). 사용자 진단 키워드 + 고정 리서치 키워드 8개가 함께 뜬다(2026-09-08 §9.4b 변경 이후).
- **`/admin/analytics/:keyword`**: Chart.js 라인차트(업체별 순위 추이, y축 반전) + **순위 변동 이벤트 테이블** — 같은 업체의 연속 스냅샷 사이 순위가 바뀐 구간마다 그 사이 방문자리뷰/블로그리뷰/투표수/사진수/사진첨부리뷰/쿠폰수 델타를 함께 보여준다. 이게 "어떤 지표가 순위에 영향을 줬는가"를 눈으로 보는 핵심 화면.
  - 순위 변동이 있는데 지표 델타가 하나도 없으면 "변동 없음(외부 요인 추정)"으로 표시 — 우리가 안 재는 요인(§질문4 D: CTR, 체류시간 등)이 작용했다는 신호로 읽을 수 있다.
- **`/admin/analytics/market`**(신규): 고정 리서치 키워드 8개의 최신 집계 통계. 일반/의료 표를 분리해서 렌더링.

### 9.6 한계 (정직하게 기록)
- 관측 데이터라 **상관이지 인과가 아니다.** 표본이 쌓이기 전까지는 회귀분석 하지 말고 "순위 오른 업체가 그 기간에 뭘 늘렸나" 빈도표부터 볼 것.
- CTR·체류시간·톡톡 응답률 등은 네이버 내부 로그라 원천적으로 수집 불가(질문4 D그룹). "설명 가능한 부분까지"가 현실적 목표.
- 사용자 검색 키워드는 "최근 검색순" 로테이션이라 하드 추적 보장이 없다. 특정 키워드를 무조건 매일 추적하고 싶으면 `FIXED_RESEARCH_KEYWORDS` 배열에 추가할 것(코드 수정 필요, 관리자 UI로 추가하는 기능은 아직 없음).
- 의료(`medical`) 카테고리 통계를 일반 통계와 섞어서 상관관계를 보면 안 됨 — 반드시 분리해서 해석할 것.

### 9.7 💡 미착수 아이디어: 지도 범위검색으로 "전국순위" 직접 확인 (사용자 제안, 보류)
사용자가 실제로 쓰던 수동 조사법: ① 네이버 지도에서 업종 키워드(예: "펜션") 검색 → ② 지도를 축소해 전국이 보이게 → ③ "현 지도에서 검색" 클릭 → 그 지도범위(bounding box) 내 **전국순위**가 뜸 → ④ 1~10위로 대략적 흐름 파악 → ⑤ 같은 방법을 경기도·강원도 등 일부 지역에도 응용 가능.

**분석**: 이건 지도 좌표범위를 서버에 보내 그 범위 안 순위를 받는 기능으로, §7.10에서 이미 발견한 `map.naver.com/p/api/search/allSearch`의 `searchCoord` 파라미터가 정확히 이 좌표범위 값을 받는 자리로 추정된다. 즉 원리상 같은 API 계열.

**문제**: 그 API는 §7.10에서 이미 직접 호출해봤고 **즉시 ncaptcha에 걸림**을 확인했다. 좌표범위를 넣는 변형이 쿼리만 넣는 변형보다 방어가 약한지는 미확인.

**가치**: 뚫리면 지금 쓰는 위젯 스크래핑(§7.10 미리보기 한계, §7.13 캐러셀 오염 문제)보다 훨씬 정확한 진짜 순위 데이터를 지역 단위로 얻을 수 있어 시도할 가치는 충분하다.

**상태**: 사용자가 "나중에 해보면 됨"으로 보류 확정. §7.7에서 국소 제한을 실측 직후라 당장 새로운 API 계열을 건드리는 건 리스크가 커서 시점도 미룸. **재개 시 주의**: 캡차 우회 시도 자체가 §1(IP차단 위험)에서 다룬 위험 행위이므로, 신중하게 접근하고 결과만 보고 판단할 것.

### 9.8 1일 검색 제한 (남용 방지)
**사용자 지적**: "상호 먼저 검색하고 '상위노출 키워드'만 무한으로 돌릴 수 있음" — `/api/place`는 자기 매장 1회 조회로 끝나지만, 그 다음 키워드 입력창은 같은 매장을 붙든 채 `/api/gap`을 몇 번이든 다시 호출할 수 있다. `/api/gap` 1회 = 검색 1 + 경쟁사 최대 10 스크랩이라 남용 시 실질적인 무제한 크롤링 창구가 됨.

**구현**: `/api/gap`에 **IP당 하루 3건** 제한. `checkDailyGapLimit()` — `CF-Connecting-IP`(Cloudflare 엣지가 붙이는 값, 클라이언트 위조 불가)로 사람을 구분, KV에 `gaplimit:{YYYY-MM-DD}:{ip}` 카운터로 추적. 초과 시 429 + "하루 검색 한도(3건)를 초과했습니다. 내일 다시 시도해주세요."

- `/api/place`(내 매장 조회)는 제한하지 않는다 — 스크랩 비용이 훨씬 작고(1회 fetch), §2에서 확정한 "1단계 전면 공개, 게이트 없음" 원칙과도 맞음. 남용 비용의 핵심은 `/api/gap` 쪽이므로 거기만 방어.
- 날짜 경계는 **KST 자정(24:00) 기준**(2026-09-08 수정 — 원래 UTC 자정이라 실질적으로 KST 09:00에 풀렸던 버그. `toKST()` 헬퍼로 UTC+9 변환 후 날짜 문자열 계산).
- KV 미바인딩 시 제한 없이 통과(§6 패턴과 일관 — 기능은 항상 동작).
- 실배포에서 4번째 요청이 정확히 429로 막히는 것 실측 확인함.

---

### 9.10 pcmap 목록 수집 — "너비" 파이프라인 (2026-09-09 신설)

> §7.10(상위 6곳만 보임)과 §7.5(광고 판별 취약)가 **미해결로 남아 있던 두 항목인데 같이 풀렸다.**

```
GET pcmap.place.naver.com/{업종경로}/list?query={키워드}&sortingOrder={정렬}
→ window.__APOLLO_STATE__
```

#### 9.10.1 무엇이 달라지나

| | 기존(통합검색+개별스크랩) | pcmap 목록 |
|---|---|---|
| 매장 수 | 6곳 | **56곳** (+광고 21곳 분리) |
| 요청 수 | 키워드당 8회 | **키워드당 1회** |
| 광고 판별 | `<a href>` 패턴 추측(§7.5 "취약함") | **타입으로 확정** — `*AdSummary` / `*AdItem` |
| 저장수 | 수집 불가 | **`saveCount` 있음**(음식점 한정) |

매장당 얻는 필드: `saveCount` `visitorReviewCount` `blogCafeReviewCount` `totalReviewCount` `imageCount` `hasBooking` `talktalkUrl` `category` `roadAddress` `x/y` `distance`

**깊이는 여전히 개별 스크랩이 필요하다.** 투표수·설명분량·쿠폰수·리뷰페널티·대표키워드는 목록에 없다.
→ **진단(고객용)은 깊이, 리서치(리버스엔지니어링)는 너비**로 역할을 나눈다.

#### 9.10.2 업종별로 정렬 축이 다르다 (실측)

| 경로 | 정렬 축 | saveCount |
|---|---|---|
| `/restaurant/list` | popular · **saved** · trendy · tv · reviewTotalCount | **있음** |
| `/hairshop/list` | popular · **revisit** · trendy · precision · distance · reviewTotalCount | 없음 |

- **`saved`와 `saveCount`는 음식점 전용이다.** 미용실엔 없다
- **미용실의 `revisit`(재방문)은 예상 못 한 수확이다.** 네이버가 재방문 데이터를 갖고 정렬까지 해준다는 뜻이라, 우리가 절대 못 재는 "보이지 않는 지표"의 대리변수로 쓸 수 있다
- 그 외 업종(`/place/list` 등) 경로는 429에 막혀 미확인. 풀린 뒤 확인할 것

`listTargetFor()`가 키워드로 경로를 고르고, **모르는 업종은 restaurant로 넘겨짚지 않고 건너뛴다**(잘못된 경로는 302를 준다).

#### 9.10.3 🚨 429를 실측했다 — 이게 설계를 결정한다

**빠르게 6회 연달아 호출했더니 `429 Too Many Requests`.** 페이지에 `ncaptcha` 스크립트도 박혀 있다(§7.7 계열).

원래 계획이 "8키워드 × 3정렬 = 24회/일"이었는데 **몰아치면 그 자리에서 죽는다.** 그래서:

- `PCMAP_THROTTLE_MS = 3000` — 요청 사이 3초 강제
- **크론 단독 슬롯**(`0 6 * * *`, 15:00 KST). 다른 작업과 같은 실행에 넣지 않는다 — 대기가 길고 subrequest도 따로 쓴다
- **키워드당 D1 쓰기 1회.** 정렬 3축 결과를 모아서 한 번에 batch. 축마다 쓰면 subrequest가 2배가 된다
- 예산: 21 fetch + 7 batch = **28 subrequest** (한도 50)

시계열은 하루 1점이면 충분하므로 **속도를 포기해도 손해가 없다.** 이 트레이드오프를 기억할 것.

#### 9.10.4 왜 정렬 축을 여러 개 모으나

같은 매장이 정렬 축마다 다른 순위를 갖는다. **그 잔차가 지표별 기여도의 단서다.**

- `saved_rank`는 상위인데 `popular_rank`가 페이지 밖 → **저장 어뷰징이 페널티를 받는다**는 가설의 직접 증거
- `trendy`는 **네이버가 계산해준 모멘텀 지표**다. 우리가 못 만드는 종속변수를 공짜로 얻는 셈

#### 9.10.5 첫 관측 (2026-09-09, `안산 상록구맛집`)

| 순위 | 상호 | saveCount | 방문리뷰 |
|---|---|---|---|
| 1위 | 통영상회 본점 | **~100** | 860 |
| 2위 | 이동고고갈비 연평도대장게장 | 24,000+ | 1,358 |
| 6위 | 샘골로 본점 | **49,000+** | 4,676 |

**저장 최다가 6위, 저장 최소가 1위.** 방문리뷰도 역방향이다.

⚠️ **n=6, 단일 시점, 단일 키워드다. 결론이 아니라 관측이다.** §9.6의 경고가 그대로 적용된다. 다만 "저장수를 올리면 순위가 오른다"는 단순 모델과는 맞지 않는 첫 실측이고, 사장님의 저장 어뷰징 페널티 가설과는 방향이 같다.

#### 9.10.6 미검증 — 순서 보장 (착수 전 필독)

지금은 **APOLLO_STATE 객체의 키 순서**를 순위로 쓴다. 상위 6곳이 기존 데이터와 정확히 일치함을 확인했으나, **권위 있는 순서 배열을 찾은 게 아니다.**

§7.15에서 정확히 이 함정(`<a href>` 순서 ≠ 실제 순위)에 당한 적이 있다. **20위 이하 순위는 아직 신뢰하지 말 것.** 순서 배열을 찾아 교체하는 것이 이 파이프라인의 다음 과제다.

#### 9.10.7 수동 트리거

`GET /admin/cron/run/place-lists` (Basic Auth). `?keyword=강남맛집`으로 **한 키워드만** 돌릴 수 있다 — 429를 겪은 뒤로는 테스트를 좁게 하는 게 안전하다.

### 9.11 검증 대기 중인 두 가설 (2026-09-09 착수, 데이터 축적 중)

사용자 이론 중 **구체적이고 반증 가능한** 두 개다. 나머지("트래픽 가중치가 가장 높다")와 달리 이 둘은 관측으로 답이 나온다.

#### 9.11.1 신규 오픈 부스트 — 지속기간을 잰다

**가설(업계 통설)**: 신규 오픈 매장은 3개월간 점수를 3배로 쳐준다. 첫 달이 가장 높다.

**관측 대상**: `기운` (`place_id=2019121430`, 강남맛집)

| 지표 | 기운 | 같은 키워드 상위권 |
|---|---|---|
| 순위 | **3위** | — |
| 방문자리뷰 | **59** | 청기와타운 2,010 |
| 블로그리뷰 | 306 | 3,898 |
| 투표수 | 260 | 5,923 |
| `is_new_opening` | **1** | 0 |

**모든 가시 지표가 바닥인데 3위다.** 교과서적인 자연실험 재료다.

**⚠️ 지속기간은 이미 답이 나와 있다 (2026-09-09 사용자 제공, 측정 목표 수정)**

네이버 공식 안내 기준 — `새로 오픈했어요` 노출은 **스마트플레이스 검수 완료일로부터 최대 90일**. 노출 조건 3가지:
1. 개업일 90일 이내 신규 등록
2. 개업일 90일 이내 표기된 **사업자등록증 제출**
3. **가격정보 + 업체 사진 최소 1건** 입력

> 이 3조건은 그대로 **신규 매장 처방 체크리스트**가 된다(§2 3단계). 조건을 몰라 부스트를 못 받는 신규 매장이 흔하다.

**실사례로 검증됨 — 백세보리밥 닭한마리**

| | 날짜 | +90일 | 실제 하락(8월 첫째주)과 부합? |
|---|---|---|---|
| 개업일 | 4/21 | 7/20 | ❌ |
| **새로오픈 신청일** | **5/1** | **7/30** | ✅ (검수 며칠 소요 시 8월 초) |

**"검수 완료일 기준"이 맞다는 실증이다.** 개업일 기준이었다면 7월 중순에 떨어졌어야 한다.

**따라서 측정 목표를 바꾼다: 지속기간(이미 앎) → 만료 시 낙폭.**

만료 순간에는 **다른 지표가 거의 안 변한다.** 리뷰·사진·저장수는 하루 사이에 안 바뀐다. 바뀐 건 부스트 하나뿐이다.

```
만료 직전 순위 ─┐
                ├─ 이 낙폭 = 부스트가 밀어올리던 크기
만료 직후 순위 ─┘  (다른 지표가 고정된 상태의 순수 효과)
```

**이게 "3배"에 접근하는 유일한 경로다.** 네이버 점수를 못 봐도, 만료 전후로 지나쳐간 업체들의 지표 차이가 곧 환산 단위가 된다 — "부스트가 리뷰 몇 건어치였나".

**과거 역산은 불가능하다.** `rank_snapshots`는 2026-09-07부터라 백세보리밥의 8월 하락 구간 데이터가 없다. 기운의 만료를 기다리는 수밖에 없다.

**필요했던 것**: pcmap 목록에는 `is_new_opening`·투표수·사진수가 없다. 그래서 워치리스트를 따로 만들었다(§9.11.3).

#### 9.11.2 재방문 — `revisit` 정렬로 검증한다

**가설(사용자)**: 재방문은 영수증 리뷰 기준으로 n회 방문자의 리뷰 수를 센다. 재방문 많은 업체는 **통합검색 순위에도 영향을 준다.**

**관측 대상**: `봉명동 미용실` — 사용자 지정. **시장이 작아 지표가 단순해서 신호가 묻히지 않는다.** 좋은 선택이다.

**측정 설계**: `popular_rank`와 `revisit_rank`를 매일 함께 수집한다.
- 두 순위의 **잔차**가 재방문의 기여도 단서다
- `revisit` 상위인데 `popular`도 상위 → 가설 지지
- `revisit` 상위인데 `popular`는 무관 → 통합검색에 반영 안 됨

`revisit`은 미용실 경로에만 있다(§9.10.2). 음식점에서는 이 검증을 못 한다.

#### 9.11.3 구현 — 워치리스트와 목록 전용 키워드

**`WATCH_PLACE_IDS`** — 개별 추적 대상. 매일 개별 스크랩해서 `sort_mode='watch'`, `rank=NULL`로 기록한다. 순위는 목록 수집 행이 이미 갖고 있으므로 여기선 **목록에 없는 지표만** 남긴다(`is_new_opening`·투표수·사진수·설명분량·리뷰페널티).
> 자연실험은 **한 매장을 끝까지 따라가야** 답이 나온다. 플래그가 내려간 뒤에도 한동안 더 본다 — 꺼지는 순간이 곧 답이기 때문이다.

**`LIST_ONLY_KEYWORDS`** — pcmap 목록만 돌리는 키워드. `FIXED_RESEARCH_KEYWORDS`에 넣으면 안 된다: 거기 넣는 순간 `fixed-a`/`fixed-b`의 비싼 개별 스크랩 배치까지 늘어나 §9.4b의 subrequest 한도를 넘긴다.

**예산 (PLACE_LISTS 슬롯)**: 음식점 5×3 + 미용실 3×3 = 24 fetch + 배치 8 + 워치 2 = **34 subrequest** (한도 50). 대기 시간은 약 75초 — 스로틀이 CPU를 쓰지 않으므로 문제되지 않는다.

#### 9.11.4 순위는 3일에 걸쳐 떨어진다 — 수집 빈도의 근거

> **사용자 실측**: 네이버는 순위를 한 번에 떨어뜨리지 않고 **3일 정도 쿠션을 주며** 내린다. **저품질 판정 때도 동일**하다(백세보리밥 외 다른 사례로도 목격).

**설계에 두 가지 영향을 준다.**

**① 만료 구간은 하루 1점으로는 부족하다.** 3일 램프를 3점으로만 잡으면 낙폭의 형태(계단인지 완만한지)를 못 본다. 만료 전후가 전체 관측에서 가장 값진 구간인데 거기서 해상도가 가장 낮으면 안 된다.

→ 워치리스트를 **15:00 · 17:00 · 21:00 세 슬롯**에서 돌린다. 하루 3점, 3일 램프에 9점.

> ⚠️ **크론 트리거는 이 계정에서 4개가 상한이다.** 5개로 늘리면 배포 자체가 거부된다(실측). 전용 슬롯을 못 만들어서 여유 있는 기존 슬롯에 얹었다. 워치 1곳당 2 subrequest라 얹는 비용은 무시할 수준이다.

**② 하락 알림의 오탐을 줄이는 규칙이 된다.** 순위가 하루 떨어진 건 노이즈일 수 있지만, **3일 연속 단조 하락**은 네이버가 의도적으로 내리는 중이라는 신호다. Phase D 하락 알림(§8.2)의 트리거 조건을 "1일 급락"이 아니라 **"3일 연속 하락"**으로 잡으면 오탐이 크게 준다. 저품질 조기 감지에도 같은 패턴이 쓰인다.

### 9.12 검색량 시계열 — `keyword_volumes` (2026-09-09 신설)

**문제**: 검색광고 API로 조회한 검색량이 **화면에 뿌려지고 그냥 버려지고 있었다.**

| | 저장 상태 (수정 전) |
|---|---|
| 경쟁사 정식 상호 검색량 | ✅ `rank_snapshots.name_search_volume` (진단 경로만) |
| **변형 검색어와 합산** | ❌ 조회 후 폐기 |
| 진단 키워드·대표키워드 검색량 | ❌ 조회 후 폐기 |

사용자 이론의 **핵심 축이 브랜드 검색 유입**인데, 그 값의 시계열이 안 쌓이고 있었다. 단면만 있으면 §9.6에서 지적한 역인과(순위가 올라서 검색량이 는 것인지, 검색량이 늘어서 순위가 오른 것인지)를 영영 구분할 수 없다.

**해결**: `getKeywordVolumes()`가 병목이므로 거기서 한 번에 남긴다. **API를 새로 탄 것만** 기록한다 — 캐시 히트는 이미 남긴 값이라 중복이 된다. KV 캐시가 24시간이라 **자연스럽게 하루 1점**이 된다.

```
context      : gap-keyword | gap-store-name | report | brand-variant
related_to   : 파생 출처 (변형이면 원 상호, 진단이면 대상 키워드)
```

**`related_to`가 핵심이다.** 변형 `안산통영상회`가 `통영상회 본점`에서 파생됐다는 걸 남기므로, **사람 판단(체크박스)을 저장하지 않아도** 분석 시점에 "이 매장의 변형 후보 전체"를 복원해 다시 고를 수 있다. 사용자가 ②(큐레이션 저장)를 뺀 결정이 이걸로 무해해졌다.

**비용**: 조회 1건당 D1 배치 쓰기 1회. `/admin/brand-volume`이 34 → 35 subrequest.

**한계**: `is_under_ten`인 행은 `total`이 상한을 더한 근사치다(§6.6.1). **추세 비교용으로만 쓰고 절대값으로 읽지 말 것.**

---

## 10. 작업 로그

> **세션을 끝낼 때 반드시 한 줄 추가한다.** 다음 세션이 이 줄부터 읽는다.

| 날짜 | 환경 | 한 일 | 다음 할 일 | 막힌 점 |
|---|---|---|---|---|
| 2026-09-07 | VS Code | 코드베이스 전수 파악, 기획 검토, 본 작업지시서 전면 개정 (3단계 동선 확정, 진입선 지표 도입, 알림 2트랙 확정, 버그 B1~B8 기록) | **Phase 0: `git init`** → Phase A 착수 | 없음 |
| 2026-09-07 | VS Code | Phase 0 완료(git init, 최초 커밋). Phase A 대부분 완료: scraper.ts/index.ts 전면 재작성(API 2분할, B1~B8 전부 수정), `worker/public/index.html` 신규 작성(2단계 플로우, 진입선 비교 카드, 실제 missingInfo 감사), `worker/wrangler.toml`에 assets+KV 바인딩 추가, `worker/schema.sql` 이관(B8 컬럼 추가), scratch 43개 파일·구버전 스크래퍼·루트 중복 HTML·루트 package.json 삭제. `wrangler deploy --dry-run`으로 빌드 검증 통과 | ① Cloudflare 로그인 후 D1·KV 실제 생성 및 `wrangler.toml` id 채우기(§4.3) ② `npm run dev`로 실제 네이버 스크랩 동작 브라우저 검증 ③ 검증 끝나면 Phase B(텔레그램 알림) 착수 | Cloudflare 계정 로그인이 이 세션 환경에서 불가하여 D1/KV 실프로비저닝 미완료. 코드는 미바인딩 상태에서도 동작하도록 방어 처리해둠 |
| 2026-09-07 | VS Code | **Phase A 완결**: `wrangler whoami`로 확인해보니 실은 로그인되어 있었음(joseungil@gmail.com) — D1(`dongbiz-db`)·KV(`dongbiz-worker-CACHE`) 실제 생성, `wrangler.toml`에 실 id 반영, 스키마 원격+로컬 적용. `wrangler dev`로 실동작 검증 중 B9(FK 제약 조용한 실패: `/api/gap` 단독 호출 시 `places` 행 없어 `search_histories` INSERT 실패) 발견·수정. **Phase B 완료**: `worker/src/notify.ts` 신설(`notify` 텔레그램, `logDiagnosisToSheet` 구글시트, 미설정시 무동작), `/api/place`·`/api/gap` 실패시 알림, `/api/gap` 성공시 신규진단 알림+시트로깅(waitUntil로 논블로킹), cron 헬스체크(`scheduled` 핸들러 + `[triggers]` 매일 00:00 UTC), `.dev.vars.example` 템플릿 작성. 실제(가짜 자격증명) 통합 테스트로 앱이 깨지지 않음을 확인 | 사용자가 텔레그램 봇 토큰·구글시트 웹훅 URL을 준비해서 `wrangler secret put`으로 채워야 알림이 실제로 발송됨(§6 Phase B 안내 참조). 그 다음 Phase C(결핍→상품 매핑, OG 태그, 경쟁사 직접 지목) 착수 | 없음. 리드폼이 아직 inert라 "상담 유입" 알림 트리거는 걸 데가 없어 보류 처리(§6 Phase B에 명시) |
| 2026-09-07 | VS Code | 사용자가 텔레그램 봇 토큰·chat id 제공 → 시크릿 등록, 실배포(`dongbiz-worker.joseungil.workers.dev`) 후 텔레그램 알림 실착신 확인. 이어서 배포판 사용 중 사용자가 실제 매장으로 버그 리포트 다수 제출 → 라이브 데이터로 원인 추적: **B10**(영업시간 오판정)·**B11**(블로그리뷰 0건 오집계) 둘 다 `base.*` 구버전 필드를 신뢰한 게 원인으로 확인, `pd.newBusinessHours`/`pd.fsasReviews.total` 실측 필드로 교체(§7.8 신규 기록). 프론트 대개편: 진입선 없을 때 "확인불가" 대신 5단계 등급(최우수~위험) 표시, 구조적으로 항상 오탐이던 키워드 미스매치 카드 삭제 후 자동 결핍요약 문장으로 대체, SEO 지표에 (i)툴팁·최근소식·설명분량(1500자) 추가, 지번주소 삭제, 순위/등급 배지를 차트 아래 크게 재배치, `GET /share/:shareId` 신설로 카톡/페이스북 공유 시 동적 OG(매장명·등급·대표사진) 노출 + 상단/하단 공유 버튼. 전 변경사항 배포+실데이터 검증 완료 | Phase C 관리자 화면 착수 예정 | 관리자 인증 방식 미확정으로 보류 |
| 2026-09-07 | VS Code | 관리자 인증 방식 제안(Basic Auth, 이유: URL에 비번 노출 없음, 브라우저 자동 로그인창, 구현 최소) → 사용자 채택 + 계정 직접 지정. Hono `basicAuth` 미들웨어로 `/admin/*` 보호(시크릿 미설정 시 503으로 차단, 뚫린 채 배포되는 사고 방지). `GET /admin`(진단 리스트, D1 최신 100건 JOIN places) + `GET /admin/:shareId`(상세: 내 매장 원본 JSON, 경쟁사 top10 원본 JSON 전부, stats) 구현. `wrangler secret put ADMIN_USER/ADMIN_PASS` 등록, 실배포 후 401/401/200 인증 케이스 3가지 + 리스트/상세 페이지 실데이터로 전부 검증 완료 | Phase C 나머지(결핍→상품 매핑 JSON, 처방 카드 UI, interpiad.com 연결, 경쟁사 직접 지목 입력창) 착수 | 없음 |
| 2026-09-07 | VS Code | 사용자 리포트 3건 처리. ① 방문자평점 4.0 미만/4.8 이상 색상 차등화(rose/lime) 적용. ② "영업시간 여전히 안 됨" 재확인 결과 **코드는 이미 맞았고 KV 24h 캐시가 배포 전 값을 물고 있던 것**(§7.9 신규) — `CACHE_VERSION` 상수 도입으로 앞으로는 스크래퍼 로직 변경 시 버전만 올리면 이전 캐시가 자동 무효화되도록 근본 해결. ③ "8위인데 6개만 비교됨" 조사 중 중대 발견(§7.10): `search.naver.com` 통합검색 플레이스 위젯은 애초에 전체 순위가 아니라 상위 소수(3~7개)만 보여주는 미리보기이고, 전체 리스트 API(`map.naver.com/p/api/search/allSearch`)는 직접 호출 시 `ncaptcha`로 즉시 차단됨(쿠키 워밍업해도 동일) — 더 이상의 우회 시도는 IP 차단 위험 있어 중단. 임시조치로 "상위 10개" 하드코딩 문구를 실제 발견 개수로 동적 표기하도록 수정, 부족 시 안내문구 추가. 근본 해결 방향(공식 지역검색API/헤드리스브라우저/현행유지)을 사용자에게 질문 → **"현재 방식 유지" 확정.** §8.1에 결론 반영, 전 변경사항 배포 완료 | Phase C 나머지(결핍→상품 매핑 JSON, 처방 카드 UI, interpiad.com 연결, 경쟁사 직접 지목 입력창) 착수 | 없음 |
| 2026-09-07 | VS Code | 사용자 UX 리포트 다수 처리(코드 버그 없음, 전부 프론트 문구/표시 다듬기): 검색창 라벨 간결화, 설명분량 한도 1,500→2,000자 정정, 최근소식 기준 1→2개월 완화(SEO Scores·생명력지수 둘 다), SECTION6 갤러리 라벨에 "샘플(최대5장)"임을 명시해 대표사진 총수와 안 헷갈리게 정리, 톡톡/스마트콜 배지에 "과거 등록 기록 기준" 툴팁 추가, "상위 업체 대비 내 위치" 카드 4개 항목 전부에 "실제 앱과 다를 수 있는 이유" 툴팁 추가, 상위평균 표시 소수점 반올림, 생명력 지수에 상세설명(1000자 미만 경고) 3번째 판정항목 추가하고 타이틀은 "매장 생명력 지수"로 고정 + 종합상태는 별도 배지로 분리(정상 항목이 경고처럼 보이던 문제 해소). 전부 배포 후 curl로 문자열 매칭 검증 완료 | Phase C 나머지(결핍→상품 매핑 JSON, 처방 카드 UI, interpiad.com 연결, 경쟁사 직접 지목 입력창) 착수 | 없음 |
| 2026-09-07 | VS Code | 사용자 리포트 3건, 답변 먼저 제시 후 2건 수정. ① "최우수인데 평균 1/3" 질문 — **버그 아님**, 등급은 평균이 아니라 10위 진입선 기준으로 설계된 것이라 설명(§2 확정사항), 수정 안 함. ② 새소식 항상 null 버그 — 원인 규명(§7.11 신규): 지시서 §5.2에 적혀있던 `BiznewsItem:`/`NewsItem:` 키가 애초에 네이버 데이터에 존재하지 않는 이름이었음(검증 안 된 스펙 오류), 실제 데이터는 `/home`이 아닌 별도 `/feed` 탭의 `Feed:` 키. `scrapeFullPlaceMetrics`에 `includeFeed` 파라미터 추가해 내 매장 조회 시에만 `/feed` 추가 조회(경쟁사는 불필요해 생략), `CACHE_VERSION` v2→v3. ③ Gap 분석 버튼이 계산 끝나도 계속 롤링되는 버그 — 원인 규명(§7.12 신규): 공용 `loadingInterval` 변수를 Enter키 재진입이 덮어써서 먼저 시작된 인터벌이 참조를 잃고 영원히 안 멎음. `withLoading()`에 `if (btn.disabled) return;` 가드 추가로 해결. 전부 배포 후 실데이터로 검증(8/29 등록 소식 정확히 추출 확인, 경쟁사는 feed 조회 안 하는 것도 확인) | Phase C 나머지(결핍→상품 매핑 JSON, 처방 카드 UI, interpiad.com 연결, 경쟁사 직접 지목 입력창) 착수 | 없음 |
| 2026-09-07 | VS Code | 사용자가 테스트 샘플 매장 지정: 필두스카이 사용 금지, **백세보리밥 닭한마리(2058645213) + '안산 상록구맛집'**을 표준 테스트 조합으로 삼으라고 요청 → 세션 간 기억되도록 Claude 메모리 파일에 기록(`dongbiz-test-store.md`), cron 헬스체크 고정 매장도 필두스카이에서 이 매장으로 교체. 입력폼 placeholder를 "상호 또는 플레이스 주소를 입력하세요"로 단순화. 지정 조합으로 1·2단계 전체 플로우 재검증: 14개 업체 발견, 10위 진입선(boundary) 전부 정상 산출, 히스토리 저장/조회 정상 — 지금까지 고친 모든 기능이 새 테스트 매장에서도 문제없이 동작함 확인 | Phase C 나머지(결핍→상품 매핑 JSON, 처방 카드 UI, interpiad.com 연결, 경쟁사 직접 지목 입력창) 착수 | 없음 |
| 2026-09-07 | VS Code | 사용자가 "10위(진입선) 블로그리뷰 2건은 이상하다"고 지적 → 조사 결과 해당 업체가 카테고리 "바(BAR)"였음을 확인하고 처음엔 §7.10(위젯 한계)의 연장으로 오판했으나, 사용자가 스크린샷으로 정확한 원인을 직접 찾아냄: 네이버 위젯 중간에 낀 **"새로 오픈했어요"** 캐러셀(최근 3개월 이내 개업 홍보, `id="nop-main-section-root"`)의 업체를 순위 리스트와 구분 못 하고 그대로 주워담고 있었음(§7.13 신규, 심각). 실측 결과 해당 키워드 14개 중 **9개(64%)가 이 캐러셀**이었음. `data-nop_res-doc-id` 속성으로 명확히 식별되길래 그 ID들을 전량 제외하도록 수정, `CACHE_VERSION` v3→v4. 재검증: 오염된 4개(대흥집·화덕고등어·담결·야화) 전부 빠지고, 캐러셀 너머의 진짜 6위(샘골로 본점)를 정상적으로 찾아냄. rankSearched가 14(거짓)→6(정직)으로 줄었지만 이게 맞는 숫자임 | Phase C 나머지(결핍→상품 매핑 JSON, 처방 카드 UI, interpiad.com 연결, 경쟁사 직접 지목 입력창) 착수 | 없음 |
| 2026-09-07 | VS Code | 사용자 질문: "6개가 아니라 10개 다 수집 가능함?" → 문서 전체를 제한 없이 스캔해서 확인: nop 캐러셀 제거는 가짜를 뺀 것이지 새로 진짜를 만드는 게 아니라 10개 보장은 안 됨. 조사 중 §7.14 신규 발견: 검색결과 페이지에 `PlaceListBusinessesItem:` 내장 JSON이 별도로 존재하는데(추가 요청·캡차 위험 없음) 이걸 보니 이 키워드엔 진짜 업체가 6개가 아니라 **7개**였고, 1곳("도깨비왕감자탕")은 `<a href>` 링크로 안 나오고 JS 상태로만 있어서 현재 정규식 방식이 놓치고 있었음. 같은 업체도 요청마다 링크로 나오다 안 나오다 해서 일관성도 없음 확인. 이 JSON 파싱 방식으로 전환할지 사용자에게 확인 → **"지금은 그대로" 유지 결정**, 코드는 미변경, §7.14에 발견 사항만 기록 | Phase C 나머지(결핍→상품 매핑 JSON, 처방 카드 UI, interpiad.com 연결, 경쟁사 직접 지목 입력창) 착수 | 없음. §7.14는 나중에 순위 누락이 반복되면 재검토 대상으로 남겨둠 |
| 2026-09-07 | VS Code | 사용자가 큰 질문 4개(①IP차단 위험 ②리버스엔지니어링 목표에 맞춘 대시보드 ③검색유입 데이터 역산 ④추가 상위노출 지표) 제기 → 전부 답변 후 "제안 우선순위대로 진행" 지시받아 A→B→C→D 전부 착수해 **§9 리버스 엔지니어링 데이터 파이프라인 신설**. A: `hasReviewPenalty`/`isNewOpening`/`hasNaverBooking`/`hasSmartOrder`/`couponCount`/`reviewMediasTotal`/`isGoodStore`/`isOpenNow` 등 8개 필드를 `pd` 하위에서 실측 확인 후 scraper.ts에 추가, 거리/키워드매칭은 기존 좌표·상호명만으로 index.ts에서 파생계산(`rankFactors`). B: `daily_ranks` 스텁을 폐기하고 전지표 평탄화 테이블 `rank_snapshots` 신설(FK 의도적으로 안 걺 — §B9 교훈), `/api/gap` 성공 시 자동 기록 훅 연결. C: `runDailyKeywordCollection()` cron 추가 — 관측된 키워드를 매일 최대 4개(subrequest 한도 고려)까지 재수집해 시계열 자동 축적, source='user'/'cron' 구분. D: `/admin/analytics`(키워드 목록) + `/admin/analytics/:keyword`(Chart.js 순위추이 + 순위변동 이벤트별 지표 델타 테이블) 대시보드 신설. CACHE_VERSION v4→v5. 4단계 전부 로컬+실배포 양쪽에서 실데이터로 end-to-end 검증 완료(cron 수동 트리거로 source='cron' 행 생성 확인, 대시보드 페이지 실제 렌더링 확인) | 시계열이 더 쌓이면 §9.6에 적어둔 "빈도표부터" 분석 시작. 순위 하락 알림(Phase D) 트리거 조건 추가하면 바로 구현 가능한 상태 | 없음 |
| 2026-09-07 | VS Code | 사용자가 "강남맛집·명동맛집·부산맛집·해운대맛집·제주도맛집·강남미용실·홍대미용실·강남성형외과" 8개 유명 키워드를 매일 고정 관측하자고 제안 → 의견 제시(변동 잦은 유명 키워드가 리버스엔지니어링엔 유리, 단 의료업종은 랭킹로직 다를 수 있어 분리 필요, 지금 구조론 자동수집 안 됨을 설명) → 사용자가 "병원은 분석에서 제외, 배치제한 올림, 8개 고정, 특정업체 지표 불필요·전체 통계만" 확정. `FIXED_RESEARCH_KEYWORDS` 8개(category: general 7 + medical 1) 신설, 업체별 세부 대신 훨씬 가벼운 집계 전용 테이블 `keyword_rank_stats`(상위10곳 평균/중앙값/비율만, 업체 식별 없음) 신설. 고정 8개를 하루에 다 돌리면 88 subrequest로 한도(50) 초과해서 **크론을 3개로 분산**(00:00 헬스체크+고정앞4개, 06:00 사용자키워드 자동수집(배치 4→6으로 상향), 12:00 고정뒤4개). `/admin/analytics/market` 페이지 신설(일반/의료 표 분리 렌더링). 로컬 dev로 3개 트리거 전부 수동 실행해 8개 키워드 전부 정상 수집 확인 — 실측 흥미: 미용실 키워드는 오가닉 10곳 꽉 참(§7.10 한계 거의 없음), 맛집은 3~5곳뿐. 원격 D1은 아직 미수집(다음 정기 cron 시각에 자동 채워짐, 최대 12시간 내) | 원격에서 실제 cron 첫 실행 후 `/admin/analytics/market` 데이터 확인. 시계열 쌓이면 일반업종만으로 상관관계 분석 시작(의료 제외) | 없음 |
| 2026-09-07 | VS Code | 사용자 지적: "네이버 순위는 매일 10~12시(KST) 사이 변동됨, 낮 12시 이후 집계되게 해줘" → 확인해보니 크론 3개 중 하나(00:00 UTC=09:00 KST)가 변동시간대 **이전**이라 전날 순위를 재고 있었음. 셋 다 12시 이후로 재조정: 04:00/08:00/12:00 UTC(13:00/17:00/21:00 KST). `wrangler.toml`의 `[triggers] crons`와 `index.ts`의 `CRON_TIMES` 상수 둘 다 동시에 수정(안 맞추면 `event.cron` 분기가 안 걸려 조용히 무동작하므로 주의). 배포 후 `schedule:` 3줄로 새 시각 반영 확인 | 다음 실제 cron(13:00 KST)부터 그날 갱신된 순위를 정확히 잡음. 확인은 익일 `/admin/analytics/market`에서 | 없음 |
| 2026-09-07 | VS Code | 사용자 요청: "다음날 오후 데이터와 비교할 수 있게 최초 1회 지금 수집해달라, 밤에도 순위 변하나?" → 밤 순위변동은 실측한 적 없어 모른다고 정직하게 답변. 수동 씨앗은 프로덕션에 트리거 endpoint가 없어서 **`GET /admin/cron/run/:job`** 신설(fixed-a/fixed-b/user-driven/healthcheck, Basic Auth) 후 배포, 실행. fixed-a 4개는 바로 성공, fixed-b 실행 중 `Too many subrequests` 실제로 발생 — 4개 키워드가 한 Worker 호출 안에서 순차 누적된다는 걸 실측(§7.7·§9.4b 갱신). 상위 스크랩 개수 10→7로 낮추고 사용자키워드 배치도 6→3으로 재하향해 재배포. 재시도했더니 이번엔 다른 에러("플레이스 상태 데이터를 추출할 수 없습니다")가 홍대미용실·강남성형외과 등 **반복 조회한 대상에만** 발생 — 전혀 새 키워드("강릉맛집")로는 동시에 정상 작동 확인해 전면 차단이 아니라 국소적·일시적 제한임을 실증(§7.7 신규 기록). 8개 중 6개는 확실히 씨앗 심어짐, 나머지 2개는 반복 재시도를 멈추고 자연 회복 대기 중 | 몇 분~시간 뒤 `/admin/cron/run/fixed-b` 한 번만 다시 호출해 나머지 2개 채우기. 이후 §7.7의 "짧은 간격 연타 금지" 룰 지킬 것 | 나머지 2개 키워드(홍대미용실·강남성형외과) 미수집 상태로 남음 — 국소 제한 회복 후 재시도 필요 |
| 2026-09-07 | VS Code | 코드 수정 없는 Q&A 3턴, 전부 §7.7·§9.7에 기록. ① 사용자가 실제 쓰던 수동 조사법 공유(지도 축소 → "현 지도에서 검색" → 전국순위 확인 → 지역별 응용) → §7.10에서 이미 찾은 `searchCoord` 파라미터와 원리상 동일한 API로 추정된다고 답변, "나중에 해보면 됨"으로 보류 확정(§9.7 신규). ② "Cloudflare IP 도는데 왜 막혔나" 질문 → 확정 증거 없이 가설 3개 제시(대상기준/지문기준/ASN대역기준), 정황상 대상기준이 가장 유력하다고 답변(§7.7에 추가). ③ "고객 진단요청도 차단되나" 질문 → 낮은 위험으로 판단해 근거 제시(정상 사용은 반복조회 패턴이 아님 + 캐시가 이미 이 위험을 흡수함), 잔여 위험(최초조회 타이밍 겹침)도 명시(§7.7에 추가). 이후 "지금까지 작업내용과 중요 발견을 작업지시서에 기록" 지시받아 이 3개 Q&A 전부 문서화 | 홍대미용실·강남성형외과 재시도(§9.4b 잔여 작업). 여유 생기면 §9.7 지도범위검색 조심스럽게 재개 검토 | 없음 |
| 2026-09-07 | VS Code | 사용자 요청 다수 처리, 전부 배포+실검증 완료. **UI**: 생명력 지수 카드에 찾아오는길/편의시설/상세설명 누락 여부 3항목 추가(SECTION7과 동일 판정, 정보표시만·점수엔 미반영), "❌ 없음 (감점)"에서 "(감점)" 제거, 상단 "대행 파트너 문의" 버튼과 PDF/알림톡 리드폼 블록 `hidden` 처리, 메인 타이틀 "전화번호 하나만" → "상호, 전화번호만"으로 수정, **사용자 화면에서 Raw Data 원본 리포트 전체 삭제**(§2 확정사항 재조정 — 관리자는 `/admin/:shareId`에서 계속 볼 수 있어 검증기능 자체는 유지). **남용 방지**: "상호 검색 후 키워드만 무한 반복 가능"하다는 지적 받아 `/api/gap`에 IP당 하루 3건 제한 신설(`checkDailyGapLimit`, KV 카운터, CF-Connecting-IP 사용, 429 응답). `/api/place`는 비용이 작고 §2 전면공개 원칙과 맞아 제한 안 함. 4번째 요청이 정확히 429로 막히는 것 실측 확인. §2·§5·§8.3·§9.8(신설) 문서 갱신 | 다음 작업 지시 대기 | 없음 |
| 2026-09-07 | VS Code | 사용자가 Phase C 방향을 결정: `interpiad.com` 자동연결 폐기하고 결핍→상담(카톡/문자 문의 → PDF 리포트 → 상담·컨설팅) 흐름으로 선회, 이유는 체험단/리뷰이벤트 벤더 네트워크 부재. 냉철한 평가 요청받아 "수동 프로세스는 안 늘어난다·클릭 로그라도 남겨라" 등 답변 후 연락처(010-2490-0555, 조강사) 받고 **"1,2,3 모두 진행"**(전화번호 난독화+CTA, 경쟁사 직접비교, 클릭로깅) + **"일 3회 제한 나 자신도 우회하게 해달라"** 지시받아 전부 구현. `worker/src/index.ts`: `RATE_LIMIT_BYPASS_TOKEN` 시크릿+`?bypass=` 쿼리로 본인 우회(공개 UI 미노출), `/api/gap`에 `competitorId` 파라미터 추가해 경쟁사 직접 1:1 비교(`isDirectCompare`, 만능 리졸버 재사용, `rank_snapshots` 미기록), `GET /api/lead-click` 신설(클릭 시 조용히 텔레그램+시트 로그). `worker/public/index.html`: 번호를 문자열 뒤집어 저장 후 클릭시 복원하는 방식으로 스팸 스크래퍼 방지 CTA 블록 구현(PDF/알림톡 숨김 블록 대체), 키워드 입력창 아래 접이식 "경쟁사와 1:1 비교" UI, bypass 토큰 URL 픽업 후 API 호출에 자동 첨부. 작업 중 Cloudflare 세션 인증이 만료되어(`wrangler whoami` → code 9109) 배포 차단, 사용자가 `wrangler login` 재인증 후 시크릿 등록+배포 재개. 실배포 후 curl로 전부 검증: `/api/gap`에 bypass 있으면 200(실데이터, myRank null·rankSearched 6), bypass 없으면 429(한도초과 정상 유지), `competitorId` 비교 성공(`targetKeyword:"'필두스카이'와 직접 비교"`), `/api/lead-click` 200, 배포된 HTML 소스에 실제 전화번호 평문 0건·뒤집힌 문자열만 1건(grep으로 확인) | 다음 작업 지시 대기. 카카오 알림톡 심사 신청은 여전히 Phase C 시작 시점에 걸어두는 게 유리(§6 Phase D 항목, 아직 미신청) | 없음 |
| 2026-09-08 | VS Code | 사용자 리포트 2건 처리, 배포+실검증 완료. ① `/admin` 상단에 일일한도 우회링크 배너 추가(`RATE_LIMIT_BYPASS_TOKEN` 있을 때만 노출). ② "1:1 비교인데 상위평균 10개로 나옴, 강점/동일/약점 3단계로 바꿔달라" 지적 → 조사해보니 수치 자체는 이미 맞았음(경쟁사 1곳뿐이라 평균=그 값), 라벨만 "상위 10개 평균"으로 잘못 붙어있던 문제였음. 레이더차트·지표카드 라벨을 실제 경쟁사명으로 교체. 등급도 새로 요청받은 대로 `compareTier()`(강점/동일/약점, 값 직접비교) 신설해 1:1 모드에 적용 — 기존 `gradeTier()`(비율기반 5단계)는 "0표 vs 0표"가 0/0.01=0으로 계산돼 무조건 "위험"이 뜨는 버그가 있었는데, 새 함수는 동률을 정확히 "동일"로 잡아 이것도 같이 해결됨. 이어서 사용자가 맞춤개선 PDF 리포트용 항목별 주석 15개 초안을 요청 → 초안 제시 → 사용자가 직접 첨삭(키워드 순서 중요성, 단순나열 감점, 체험단/대가성 리뷰 허용 및 가짜영수증 감점 등 실무 지식 추가) 확정. **맞춤개선 PDF 리포트 신규 구현**: `GET /admin/:shareId/report`(기존 admin Basic Auth 그대로 적용), 항목별 판정+확정 주석(`REPORT_NOTES` 상수)을 함께 노출, 1:1 비교 진단이면 `serverCompareTier`(프론트와 동일 로직 서버 복제)로 자동 전환, 하단 상담 연락처 고정 노출. PDF는 라이브러리 없이 인쇄 CSS로 처리(브라우저 "인쇄→PDF저장"). `/api/lead-click`에 `shareId` 파라미터 추가해 CTA 클릭 시 텔레그램 알림에 리포트 링크 자동 첨부 — 전체 흐름(고객 클릭→텔레그램 링크→관리자가 PDF저장→카톡 전달→상담) 완성. 실배포 후 curl로 검증: 라벨/등급 수정은 배포된 HTML에 `compareTier`·`약점` 문자열 존재로 확인, 리포트 라우트는 미인증 401(존재+인증게이트 확인)과 `/api/lead-click?shareId=`로 실제 링크 포함 알림 발송까지 확인 — 단, 관리자 인증정보가 이 세션엔 없어 리포트 페이지 실제 레이아웃 렌더링은 사용자가 브라우저에서 직접 확인 필요 | 사용자가 `/admin/{shareId}/report` 레이아웃 직접 확인 후 피드백 | 없음 |
| 2026-09-08 | VS Code | 사용자가 방금 만든 PDF 리포트 방식에 피드백: "주석만 모아놓은 요약페이지 말고, 고객이 실제로 보는 진단 화면에 항목별로 주석이 바로 붙어있으면 좋겠다. 그래야 화면 번갈아 안 봄" → 서버에서 판정 로직을 다시 그리는 대신 **`worker/public/index.html`(고객용 실제 페이지)을 그대로 재사용**하는 방식으로 전면 재구현. `GET /admin/:shareId/report`가 이제 `c.env.ASSETS.fetch()`로 정적 index.html을 가져와 `<script>window.__REPORT_SHARE_ID__=...</script>` 한 줄만 주입해서 반환(서버측 REPORT_NOTES·serverGradeTier·serverCompareTier·reportItem 등 중복 판정 코드 전부 삭제). `wrangler.toml` `[assets]`에 `binding = "ASSETS"` 추가(신규 바인딩이라 배포 후 실제 동작하는지가 이번 검증의 핵심). 프론트에 `IS_REPORT` 플래그 + 프론트 전용 `REPORT_NOTES`(서버 상수와 동일 문구, 프론트/서버 런타임이 달라 중복 불가피) + `addReportNote()` 도입해서 실제 렌더링되는 각 데이터 행(li 또는 flex 행) 바로 아래에 `insertAdjacentHTML`로 주석 삽입. 같은 항목이 SECTION7과 생명력지수 카드 두 곳에 중복 표시되는 3개(찾아오는길/편의시설/상세설명 누락)는 SECTION7에만 붙여 중복 방지. 리포트 모드에선 헤더·2단계 검색폼·공유버튼 숨김, 상담 전화번호는 클릭 없이 즉시 평문 노출, 인쇄/PDF 버튼 추가. 실배포 후 검증: 신규/오답 인증 둘 다 401(라우트+인증 미들웨어는 정상, ASSETS 바인딩이 배포에는 포함됨을 간접 확인), `/api/lead-click?shareId=`로 리포트 링크 포함 알림 발송 확인 — **단, 관리자 인증정보가 이 세션엔 없어 ASSETS.fetch()가 실제로 정상 HTML을 반환하는지, 주석이 올바른 위치에 붙는지, 인쇄 결과가 깔끔한지는 전부 사용자가 브라우저에서 직접 열어 확인 필요(이번 것도 미확인 상태로 인계)** | 사용자가 `/admin/{shareId}/report` 직접 열어서: ① ASSETS 바인딩이 정상 동작하는지(빈 페이지·에러 아닌지) ② 주석이 올바른 항목 아래 붙는지 ③ 인쇄 결과 확인 | 없음 |
| 2026-09-08 | VS Code | 사용자가 새 PDF 리포트 방식 직접 확인 후 "잘 나옴, 세부 수정사항은 추후 논의" 확정 — ASSETS 바인딩·주석 삽입 위치·인쇄 결과 전부 정상 동작 검증됨. **`dongbiz.com` 도메인 연결**: Cloudflare 대시보드 사용법 질문 2건에 답변(Workers&Pages의 Domains 탭은 "구입"이 아니라 이미 Cloudflare 존에 있는 도메인을 Worker에 "연결"만 하는 곳임을 설명, 서브도메인 칸을 비우면 루트 도메인으로 연결됨을 확인해줌) → 사용자가 대시보드에서 직접 Add Domain 실행. `curl -o /dev/null -w "%{http_code}" https://dongbiz.com/`로 200 확인 — 도메인 연결 완료. §6 Phase D·§4.3 문서 갱신(도메인 연결 항목 체크, `/admin/:shareId/report` 항목 §4.3에 추가 반영). **사용자가 "남은 phase 없으면 여기까지 기록" 요청** → Phase A/B 완료, Phase C 완료(방향전환 반영), Phase D는 시계열 축적+도메인 연결 완료·나머지(pSEO, 순위하락알림, 카카오알림톡 심사, 결제연동) 미착수 상태로 전체 로그 마무리 | 카카오 알림톡 심사 신청, 순위 하락 알림, pSEO 자동발행, 결제 연동 중 우선순위 정해서 재개. PDF 리포트 세부 수정사항 추후 논의 | 없음 |
| 2026-09-08 | VS Code | 도메인 연결 후 브랜딩 마무리 요청 6건 처리, 배포+실검증 완료. 로고를 `<a href="/">`로 감싸 클릭 시 홈 이동 추가, "(동)" 파비콘 신설(SVG를 base64 data URI로 인라인 — 별도 이미지 파일/빌드파이프라인 없이 `<link rel="icon">`만 추가), 로고 옆 "dongbiz" 영문 배지 삭제, 히어로 상단 "네이버 플레이스 상위노출 Gap 실시간 진단" 알림 pill 전체 삭제, 헤드라인 문구 "상호, 전화번호만" → "상호나 전화번호만"으로 수정. 검색창 카드 바깥 하단에 `<details>`(네이티브 접이식, JS 없음) 로 입력방법 안내 4종(상호/전화번호/지도공유주소/플레이스 고유숫자) 추가 — 기본 접힘 상태라 히어로가 복잡해지지 않음. 푸터에 사업자 정보(상호 인터피아드 · 사업자등록번호 124-35-56796 · 문의 interpiad@gmail.com) 추가. 실배포 후 `curl https://dongbiz.com/`로 6개 항목 전부 grep 매칭 확인 완료(파비콘 링크 존재, dongbiz 배지 0건, pill 문구 0건, 새 헤드라인 문구 존재, 인터피아드 존재, 로고 앵커 존재) | 다음 작업 지시 대기 | 없음 |
| 2026-09-08 | VS Code | 사용자 요청 4건(SEO 점검, og:image 점검, 일일한도 KST 초기화, 관리페이지 KST 확인) 처리. **SEO 감사 결과**: `<meta name="description">` 부재(og:description만 있어 구글 스니펫에 못 쓰임), `canonical`·`og:url` 부재, `robots.txt`·`sitemap.xml` 부재 확인 → 전부 신설/추가(`worker/public/robots.txt`, `worker/public/sitemap.xml`, index.html에 description·canonical·og:url·twitter:card 추가). **og:image 감사 결과**: 루트 페이지(`/`)엔 og:image가 아예 없었음(카톡/페이스북 공유 시 미리보기 이미지 없이 뜸) — `/share/:shareId`는 매장 자체 사진이 있을 때만 표시. og:image 태그는 추가해뒀으나 실제 이미지 파일(`worker/public/og-image.png`, 1200x630)이 아직 없어 **주석처리 상태로 배포**(빈 이미지 참조가 크롤러에 캐시되는 게 더 나쁨) — 사용자에게 이미지 생성 프롬프트 별도 제공, 파일 받으면 주석 해제+배포 예정. **일일 검색한도 KST 버그 수정**: `checkDailyGapLimit()`가 `new Date().toISOString()` UTC 자정 기준이라 실제로는 KST 09:00에 풀리던 버그 확인 → `toKST()` 헬퍼(UTC+9) 신설해 KST 24:00 기준으로 수정. **관리페이지 KST 확인 결과**: cron 실행 시각(13/17/21시 KST) 자체는 이미 맞게 잡혀있었지만(§7 이전 세션 수정), `/admin` 계열 페이지들이 D1 `CURRENT_TIMESTAMP`(UTC) 값을 변환 없이 그대로 표시하고 있어 관리자가 보는 시각이 전부 UTC였던 것 확인 → `fmtKST()` 헬퍼 신설해 진단 리스트·진단 상세·지표분석 목록·시장통계·순위추이 차트(라벨+이벤트표) 전부 KST로 표시 변경(DB 저장값 자체는 UTC 유지, 표시할 때만 변환 — 저장은 원시값이 맞는 관행). 전부 실배포 후 robots.txt/sitemap.xml 응답 확인, description/canonical/og:url 태그 존재 grep 확인, fmtKST 변환 로직 node로 별도 검증(UTC→KST 날짜이월 케이스 포함) — 단 admin 표시 결과 자체는 인증정보 없어 이 세션에서 직접 못 봄 | 사용자가 blog.dongbiz.com vs interpiad.com/blog 질문에 대한 답변(아래 채팅) 참고해 결정, og-image 프롬프트로 이미지 생성해서 전달 | 없음 |
| 2026-09-08 | VS Code | 사용자가 프롬프트로 og-image.png(2848x1504, 4.7MB) 생성해 `worker/public/`에 전달 → 이미지 확인(로고+헤드라인+부제 구성 양호) 후 index.html의 og:image 태그 주석 해제, twitter:card를 summary→summary_large_image로 변경, 배포. `curl https://dongbiz.com/og-image.png` 200, og:image 태그 존재 확인. 파일 용량이 권장치(1MB 이하)보다 커서 리사이즈 권고만 하고 기능상 문제없어 그대로 유지 | 여유 있을 때 og-image 1200x630/300KB 이하로 리사이즈 권고(선택사항) | 없음 |
| 2026-09-08 | VS Code | 사용자가 og-image를 1200x630/385KB로 리사이즈해서 재전달 → 이미지 재확인(구도 동일, 용량만 최적화됨) 후 재배포. `curl https://dongbiz.com/og-image.png`로 200 + 용량 385704 bytes 확인 | 다음 작업 지시 대기 | 없음 |
| 2026-09-08 | VS Code | 사용자 버그 리포트: "꿈있는공방, 인형병원 검색시 순위 1위임에도 4위로 집계함". `/api/gap`으로 재현(myRank:4) 후 raw HTML 직접 받아 대조 조사 — §7.14에서 발견만 해두고 보류했던 내장 JSON(`PlaceListBusinesses.items`)이 꿈있는공방을 정확히 1위로 담고 있는 반면, 기존 `<a href>` 정규식 스캔은 순서가 완전히 뒤섞이고 그 JSON에 없는 엉뚱한 업체까지 섞여 들어오는 걸 확인(§7.15 신규, B14 신규) — §7.13에서 고친 캐러셀 오염과는 별개의, 더 근본적인 "DOM 순서 자체가 신뢰 불가"였던 문제. `getOrganicRanking()`을 JSON 배열 최우선 추출로 전환(href 스캔은 폴백으로만 유지), `CACHE_VERSION` v5→v8로 캐시 무효화. 배포 후 재검증 중 **잔여 문제 발견**: 수정 후에도 Worker에서는 여전히 4위로 나옴 → `wrangler tail`로 라이브 디버그 로그 찍어 확인한 결과 파싱 자체는 정상 동작(JSON 정상 추출)이었고, **네이버가 요청자 IP(GeoIP 추정)에 따라 이 JSON 배열 자체를 다른 순서로 내려주는 것**이 원인이었음 — 로컬(한국 IP)에서 3회 반복 조회는 매번 안정적으로 1위, Worker(데이터센터 IP)에서는 다른 순서지만 그것도 매번 일관됨. URL x/y 좌표 파라미터로 우회 시도했으나 효과 없음 확인. "지역명 없는 업종 키워드"에서 발생하는 것으로 추정(미검증: 지역명 포함 키워드는 영향 적을 것). 완전한 해결책은 못 찾아 §7.15에 잔여 한계로 기록, 디버그용 임시 console.log는 정리 후 재배포 | 지역명 없는 키워드의 위치개인화 문제 완전 해결 방안 탐색(우회 프록시/쿠키 등, 리스크·비용 있어 신중 검토 필요). 지역명 포함 키워드는 이 문제 영향 적은지 실측 검증 | 위치개인화 문제는 미해결 상태로 남음 — 근본 원인이 네이버 서버 정책이라 클라이언트 측에서 완전히 통제 불가능할 가능성 있음 |
| 2026-09-08 | VS Code | §7.15 위치개인화 한계에 대한 사용자 조치: 완전 해결 대신 **투명하게 안내하는 방식**으로 확정 — Gap 결과의 순위·등급 배지 바로 아래에 "네이버 순위는 검색자의 위치에 따라 순위 보정이 일어납니다. 알고계신 순위와 차이가 발생할 수 있습니다. 진단 데이터는 위치가산점 없는 원본 데이터를 기반으로 측정합니다" 고정 안내문구 추가. 배포 후 `curl`로 텍스트 존재 확인 | 다음 작업 지시 대기 | 없음 |
| 2026-09-08 | VS Code | 배포 방식을 `wrangler deploy` CLI 단독에서 **GitHub 연동 + Cloudflare Workers Builds**로 전환 시작. 사용자가 GitHub 계정은 있으나 저장소 없음을 확인(AskUserQuestion) → 신규 빈 저장소 생성 안내 → `https://github.com/joseungil-kr/dongbiz.git` 수신, `git remote add origin` 후 히스토리에 시크릿 없음 확인, **사용자 명시 확인("올려도됨") 받고서야 push 실행**(auto mode classifier가 미확인 push를 1차 차단한 사례 — 이후 유사 작업은 항상 사전 확인 필요) — 35커밋 푸시 성공. Cloudflare Workers Builds 설정 가이드 제공(Root directory: worker, Deploy command: npx wrangler deploy, Production branch: master). 사용자가 올린 설정 스크린샷 검토 중 `Build command: npm run build` 오류 발견(`worker/package.json`에 build 스크립트 없어 CI 실패할 것) → 비워두도록 안내, 나머지 설정은 정상 확인. 별도로 Runtime secrets 목록에 `SHEETS_WEBHOOK_URL` 누락 확인 | 사용자가 Build command 필드 비우고 저장 완료했는지 확인 | 없음 |
| 2026-09-08 | VS Code | 사용자가 `SHEETS_WEBHOOK_URL` 설명 요청 → `notify.ts`의 `logDiagnosisToSheet()` 역할(진단완료·CTA클릭 시 구글시트에 행 추가, 콜드콜 영업리스트 용도, 미설정 시 조용히 무동작) 설명 + Google Apps Script 웹앱 생성 가이드 제공. 사용자가 Apps Script 배포(`doPost`로 시트에 append) 후 웹앱 URL(`.../macros/s/AKfycbxJE.../exec`) 전달 → curl로 실동작 검증(302→exec 리다이렉트를 GET으로 재요청해야 정상 응답 "OK" 받는 구조 확인, curl -L 자체 버그로 1차 테스트 실패했던 것 재확인). 첫 테스트에서 시트에 한글이 깨져 들어간 것 확인 → Windows Git Bash 터미널 인코딩(CP949) 문제로 진단(Worker의 실제 `fetch()`는 UTF-8 정상 전송이라 실서비스엔 영향 없음), UTF-8 파일로 재테스트해 정상 한글 확인 완료. 사용자가 Cloudflare Runtime secrets에 `SHEETS_WEBHOOK_URL` 추가 예정 | 사용자가 Cloudflare에 SHEETS_WEBHOOK_URL 추가 완료 확인, Workers Builds 첫 자동배포 성공 여부 확인 | 없음 |
| 2026-09-08 | VS Code | **비교 기준 상위 10개→6개 변경**: 사용자가 "네이버 검색 결과가 7곳뿐이라 10개 미만입니다" 안내가 뜨는 걸 보고 "7개든 6개든 평균 내서 비교해주는 게 낫다, 모든 키워드 1면(6개) 기준으로 통계 내면 어떤가" 제안 — 네이버 지도 1페이지 실노출이 6곳이라 10 기준이 애초에 현실과 안 맞았음. `TOP_N = 6` 상수 신설해 `/api/gap` 비교군·진입선(`ranking[TOP_N-1]`) 전부 6 기준으로 전환, 프론트 문구 8곳(meta description·og:description·히어로·2단계 안내·레이더 제목·차트 라벨·요약 문구·"N개 미만" 안내) 전부 6으로 수정, 순위 배지 색상 임계값도 10→6. `CACHE_VERSION` v8→v9(§7.9 규칙대로 캐시 무효화). DB 컬럼명(`top10_*`)과 JSON 필드명(`top10Competitors`)은 과거 데이터 호환 위해 유지하고 주석으로만 실제 기준을 명시. **배포 방식도 이번에 전환 완료** — `wrangler deploy` 시도했으나 CF 인증 또 만료돼서, 사용자 선택에 따라 git push로 배포(Workers Builds 자동배포). push 후 `curl https://dongbiz.com/`에 "상위 6개" 3곳 노출 확인 — **GitHub 연동 자동배포 정상 작동 최초 검증됨** | 다음 작업 지시 대기 | 없음 |
| 2026-09-08 | VS Code | 사용자 질문("리서치 키워드 통계는 뭘 수집하나, 업체별 세부데이터도 저장되나")에 답변하다가 **§9.4b 설계 결정 철회**. 고정 리서치 키워드 8개는 `keyword_rank_stats`(집계 전용)에만 쌓고 업체별 세부는 버리고 있었는데(과거 사용자 요청, D1 비용 절감 목적), 사용자가 "원하는 건 특정 업체 순위변화 시 세부 데이터값 변화인데 기획취지에 없었나"라고 지적 — §9.1의 리버스엔지니어링 취지("순위가 바뀔 때 뭐가 같이 바뀌었나")와 정면으로 어긋나던 예외였음. 비용 재계산 결과 절감 근거도 약했음(8키워드×7곳=56행/일 vs D1 무료한도 10만 write/일). `collectKeywordAggregateStats()`에 `insertRankSnapshotRows()` 호출 추가해 집계+업체별 세부 둘 다 기록하도록 변경(이미 긁어온 데이터 재사용이라 subrequest는 키워드당 +1, 배치 4개 기준 36→40으로 한도 50 안쪽). `runDailyKeywordCollection()`의 `fixedSet` 필터가 이미 있어 중복수집 없음. 부수효과로 고정 키워드가 `/admin/analytics` 목록에 함께 뜨게 되어 해당 안내 문구와 §9.4b·§9.5 문서 갱신 | 다음 cron 실행(KST 13/21시) 후 `/admin/analytics`에 고정 키워드 8개가 뜨는지, 순위변동 이벤트 테이블이 정상 계산되는지 확인 | 없음 |
| 2026-09-08 | VS Code | 사용자 UI 요청 3건 처리·배포. ① 다크모드에서 2단계 입력창이 안 보인다는 제보 → 원인은 `color-scheme` 미선언으로 크롬/삼성인터넷 **강제 다크 변환**이 걸린 것, `only light` 옵트아웃으로 차단(근본 수정). ② 같은 카드가 1단계 결과와 똑같은 흰 카드라 페이지 일부처럼 묻히는 문제 → 파란 테두리·배경으로 분리 + STEP 2 배지 + 맥박 애니메이션. 단 카드가 화면 아래라 등장 즉시 재생하면 사용자가 내려왔을 때 이미 끝나 있어 `IntersectionObserver`로 **실제 노출 시점에** 재생하도록 연결. ③ 인기키워드 없는 매장(병원 등)에 하드코딩 `'#친절해요'`가 그 매장 실제 데이터처럼 보이던 문제 → 라벨까지 함께 바꿔 실제 보유 정보(연락처/업종)로 대체. 추가로 소비자 키워드 리뷰 카드에 집계 기준 안내문구, 마케팅 결핍 진단에 "순위 역전 가능" 경고문 삽입 | 다음 작업 지시 대기 | 없음 |
| 2026-09-08 | VS Code | 사용자가 외부 GEO 조언 2건("소스코드를 뜯어고쳐야 한다", "서치콘솔 등록을 미뤄야 한다") 검증 요청 → 둘 다 과장/오류로 판정하고 **진짜 병목이 따로 있음을 발견**: `/share/:shareId`가 본문 한 줄짜리 리다이렉트 껍데기이고 결과가 전부 JS 렌더라, JS를 실행하지 않는 AI 크롤러 입장에선 인용할 본문 자체가 없었음. 이어 사용자가 "기획 먼저" 지시 + 독창성·SEO·N개 상호명 노출 가능성을 질문 → **§6.1 신설**. 기획 과정에서 1순위의 형태를 바꿈(매장별 공유페이지 색인 → 키워드별 순위 페이지). 이어 사용자가 "3위→1위 사이 리뷰 40건 증가" 같은 데이터 공개는 경쟁사에 컨설팅 상품을 무료로 넘기는 것 아니냐고 지적 → 타당하여 **§6.1.2b 공개범위 3단 분리 확정**(1단 공개: 순위표·집계·진입선·변동성만, 해석 문장 없음 / 2단 noindex: 등급·결핍 / 3단 비공개: 업체별 시계열·상관). 절대 수치는 네이버에서 누구나 보므로 자산이 아니고, **시계열 변화량과 그 인과 연결이 진짜 자산**이라는 판별 기준을 문서에 남김 | §6.1.5 순서대로 착수 | 없음 |
| 2026-09-08 | VS Code | §6.1.5 착수·완료. `/rank/:keyword`(SSR 순위표+집계+변동성, 해석 문장 0), `/rank` 허브, `sitemap.xml` 동적 라우트(정적 파일 삭제) 신설. 발행 조건은 **관측 2회 이상**이고 미달 키워드는 404+noindex. 배포 후 사이트맵에 "안산 상록구맛집"과 "안산 상록구 맛집"이 각각 잡혀 **사실상 같은 페이지가 2장 색인될 뻔한 것을 발견** → `normKeyword()`(공백 제거)로 동일성 판정해 관측 많은 표기만 발행하고 변형 표기는 canonical로 통합. 이어 사용자가 "고아 페이지여도 검색에 문제없나" 질문 → 발견은 사이트맵으로 되지만 권위 전달·재크롤링 우선순위에서 불리하다고 답하고 홈 푸터→`/rank`, 상세→허브 역링크 추가. JSON-LD `ItemList`는 순위·상호명 등 사실만 담아 삽입. **`/share` 본문 SSR은 착수 직전 취소** — §6.1.2b에서 noindex로 확정한 순간 "AI가 인용할 본문이 없다"는 명분이 사라졌기 때문(§6.1.5 2번에 사유 기록). 배포 후 사이트맵 4건·허브 4개 일치 확인, 실제 페이지 렌더링(인형병원 TOP 6) 육안 확인 | 서치콘솔·네이버 서치어드바이저 등록(§6.1.5 5번). 고정 리서치 키워드 8개는 스냅샷 2회 누적되는 다음 cron 이후 자동 등재되므로 확인 필요 | 없음 |
| 2026-09-08 | VS Code | 관리자·차트 UI 정리 4건. ① `/admin/analytics/:keyword` 차트에서 y축 제목("순위(낮을수록 상위)") 제거하고 **오른쪽에 업체명 축 신설**(최신 관측 기준, 축 범위는 전체 관측 최소~최대로 잡아야 과거 하위권 업체 선이 안 잘림). ② 관리자 5개 페이지에 공통 상단 메뉴 `ADMIN_NAV` 추가(진단리스트·지표분석·시장통계·공개순위 + cron 수동실행 3종) 하고 페이지마다 흩어져 있던 뒤로가기 링크 4개 제거. ③ 진단 상세의 "공유 링크(고객용)"와 "실제 앱 화면으로 보기"가 **결국 같은 화면**이라는 지적 받아 하나로 통합, 목록의 "상세보기"→"원본데이터" 개명. ④ 사용자가 강릉맛집 차트 범례에 선 없는 '백세보리밥 닭한마리'가 뜬다고 제보 → 원인은 `logRankSnapshots`가 진단 유발 매장을 항상 기록하는데 그 매장이 순위권 밖이면 rank가 NULL이라 선은 안 그려지고 범례만 남는 것 → **한 번도 순위에 든 적 없는 업체만** 차트에서 제외(신규 진입·이탈 이력은 유지되도록 조건 설계) | 다음 작업 지시 대기 | 없음 |
| 2026-09-08 | VS Code | 사용자가 경쟁사(삐딱한마케팅) 무료 진단 도구의 실제 결과 전문을 가져와 벤치마킹 지점 분석 요청 → **§6.2 신설**. 핵심 판단은 "그쪽은 절대 점수(내 매장 자체 점검), 우리는 상대 비교(경쟁사 대비 격차)라 그대로 베끼면 차별점이 사라진다"는 것. 100점 만점 절대 점수는 가중치가 자의적이고 경쟁 맥락이 없어 **의도적으로 채택 안 함**(그쪽도 FAQ에서 한계를 자인하고 있고, 그 공백이 우리 자리). 반면 **랜딩 본문 볼륨은 즉시 채택** — 우리 홈에 히어로·검색창 외 본문이 사실상 없어 검색엔진이 페이지 주제를 판단할 재료가 부족했음(§6.1.0에서 지적만 하고 미착수였던 부분). 사용 방법 4단계 + FAQ 8문항 초안을 사용자에게 먼저 제시("바로배포" 승인) 후 `<details>` 네이티브 접이식으로 구현하고 동일 내용을 `FAQPage` JSON-LD로 표기, 리포트 모드에선 숨김 처리. 배포 후 접이식 9개(FAQ 8+기존 입력안내 1)·순수 본문 텍스트 약 7,155자 확인. 네이버 서치어드바이저 소유확인 메타태그도 삽입·배포 확인 | 사용자가 구글 서치콘솔 도메인 속성(DNS TXT) 등록 + 사이트맵 제출. 이후 유입 붙는지 보고 §6.2의 2·3번(재진단 동선, 무료 개선 힌트) 착수 판단 | 없음 |
| 2026-09-08 | VS Code | **범용 지침 세트 신설** — 사용자가 "사이트마다 통일성이 없고 빠진 것도 있다"며 다른 프로젝트의 지침 3종(flower1st 작업지시서·구조사양서, baroflowers 브리핑)을 가져와 장점 추출을 요청. 검토 결과 우리 문서에 없던 것 다수 확인: **①사양서 우선·복사 금지 원칙**(두 곳에 있으면 갈라진다) ②**"고유 데이터 블록이 비면 도어웨이"**라는 검사 가능한 pSEO 판정선(우리는 "보일러플레이트 낮춰라"로 추상적이었음) ③되돌리기 비용 순 정렬 ④AI에게 주는 STOP 지점·추정값 금지 ⑤**"체크리스트는 현황 기록이지 할 일 목록이 아님"**(+신규 작성 시는 예외) ⑥IndexNow 대량 색인과 Selenium 조작 금지 ⑦발행일 분산 자동 공개 ⑧이미지 규격(base64 금지·슬라이스 금지·텍스트 이미지 금지). 세 문서 모두에 없던 법적리스크·데이터 부패·롤백·비밀정보도 추가해 `C:\project\_범용지침_사이트구축\`에 4파일로 정리(README·01사이트구조사양·02바이브코딩지침·03작업지시서템플릿). **문구 pool 조합 지침은 어디에도 없었고 오히려 두 문서가 상반**(flower1st는 config 문구를 1,100장에 반복, baroflowers는 기계적 생성 금지) → 사양 §2.2에 "문장 다양화는 데이터에서 나와야 한다"로 정리. 법령은 사용자 요청대로 의료법 §56·변호사법 §23·공인중개사법 §18의2를 포함해 표로 정리하고, **config 플래그로 금칙어 스캔해 빌드 실패시키는** 실행 장치까지 명시. 이후 중복 제거: 프로젝트의 `AI_바이브코딩_SEO_AEO_GEO_지침서.md`는 범용_02가 상위호환이라 git에서 삭제, 가져온 지침_ 3파일은 원본이 원래 폴더에 있음을 확인 후 삭제, 템플릿의 대량생성 데이터준비 절도 다른 문서와 중복이라 4줄로 압축 | 다음 프로젝트 시작 시 이 폴더를 복사해 실사용해보고 부족한 점 보완 | 없음 |
| 2026-09-08 | VS Code | 리포트 주석 개선 요청 다수 처리. **주석을 문제 항목에만 표시하도록 전환** — 정상 등록된 항목까지 설명이 달려 정작 고쳐야 할 것이 묻힌다는 지적. `addReportNote()`에 조건 인자 추가해 누락·미달일 때만 노출. 작업 중 **최근소식 주석이 아예 안 보이던 버그 발견** — 같은 항목이 매장정보 상단과 생명력 지수 두 곳에 표시되는데 주석은 아래쪽에만 붙어 있었음, 상단으로 옮기고 아래는 중복 제거. 평점이 '비공개'일 때 감점처럼 보이는 문제에 안심 문구 추가, 새소식 문구를 '활성화지표' 관점으로 재작성, 메뉴/상품 등록 수에 대표상품 우선·스팸 키워드 경고 상시 표시, 설명 분량 아래에 **찾아오는 길 글자 수 신설**(이미 `base.road`를 수집 중이라 세기만 하면 됐음), 편의시설 카드 세로 중앙정렬 제거해 옆 카드와 첫 줄 정렬. 또 **키워드 투표수가 내 매장·평균·진입선 모두 0인데 '위험'으로 뜨는 문제** 수정 — ratio 기반 판정이 0/0.01=0으로 계산하던 것으로, 회색 '집계 없음' 배지로 분리하고 결핍 요약에서도 제외(1:1 비교 모드는 `compareTier`로 이미 처리돼 있었고 키워드 모드만 남아 있었음). 이때 비교 기준 6개 전환 시 놓친 `10위(진입선)` 라벨·안내문·주석도 함께 정정 | 다음 작업 지시 대기 | 없음 |
| 2026-09-08 | VS Code | **가이드 문서 5편 발행(§6.3 신설)**. 네이버 자동완성으로 씨앗 22개→고유 키워드 51개를 수집해 주제를 실측으로 선정. 사용자가 준 씨앗 중 `플레이스 경쟁사 비교`·`네이버 지도 순위 알고리즘`은 자동완성 0~1건이라 **수요 없음을 근거로 그대로 쓰지 않고** 질문형으로 바꿔 진입선 문서에 흡수. 어뷰징·소비자의도·네이버공식 키워드는 의도적 제외. `worker/src/guides.ts`에 5편을 작성하고 `/guide` 허브 + `/guide/:slug` SSR 라우트, `Article`+`FAQPage` JSON-LD, 사이트맵 등재까지 처리. 배포 후 문서별 본문 1,791~1,989자 확인. 이어 사용자가 "각 페이지는 고아페이지인가" 질문 → **사이트맵 전 URL을 크롤링해 역색인을 만들어 실측**(추측하지 않음): 고아 0개이나 **순위 페이지 유입링크가 각 1개(허브뿐)**로 사양 §1.7의 횡 연결이 빠진 상태 확인 → 2-gram 겹침 기준 형제 키워드 링크 추가, 재측정으로 최대 11개까지 증가 확인. 이 시점에 고정 리서치 키워드들이 스냅샷 2회를 채워 순위 페이지가 4개→11개로 자동 증가한 것도 함께 확인(발행 조건 정상 동작). 사용자가 og-image 교체 후 반영 확인 요청 → 로컬·배포본 MD5 대조로 이미 반영됨을 확인하고, 카톡·페북은 자체 캐시라 파일만 바꿔선 갱신 안 되므로 URL 변경이 필요함을 안내(현재 1192x630/1MB로 권장치와 어긋나 재작업 권고) | 색인 결과 관측(수일~2주). 이후 §6.2의 2·3번 착수 판단 | 없음 |
| 2026-09-09 | VS Code | 가이드 문서 5편을 §2.3 규칙(분류 프레임·실측 수치·출처 절·브랜드 FAQ)으로 전면 보강. 이 과정에서 **직접 쓴 문장이 데이터와 어긋난 것을 사용자가 지적해 정정** — '리뷰가 적은데 상위인 업체는 사진이 많더라'고 썼으나 해당 사례 35건을 전수로 세어보니 사진이 더 많은 경우 17건, 더 적은 경우 17건으로 신호가 전혀 없었다(체리피킹). '설명되지 않는다'는 사실을 그대로 쓰는 쪽으로 교체하고, 이후 실측 주장은 전수 검증 후에만 쓰기로 함. 이어 사용자 도메인 지식(보이지 않는 지표: 방문자수·재방문률·체류시간·유입 키워드, 그리고 리뷰 내용과 목표 키워드의 정합)을 '축 이름까지만 공개, 진단·교정법은 상담'으로 수위 조절해 반영(강남동태탕 예시). 순위 하락 문서에는 네이버 점검·알고리즘 변동을 둘째 갈래로 추가(1페이지 전체 움직임으로 세 양상 판별). 분량 측정 기준이 바이트였던 것도 정정(문자 기준 4,715자, 경쟁 1위의 36%). 백세보리밥 닭한마리 실데이터 진단 결과 '안산 맛집'은 진입선 리뷰 7,962건으로 362배 격차라 목표 키워드 자체를 카테고리·대표키워드·리뷰가 이미 가리키는 좁은 키워드로 바꾸라고 조언. 마지막으로 **§6.4 순위 페이지 강화 기획 확정** (주차별 4주 순위표, URL 유지·title만 확장, 검색 의도 충돌 차단, 표기 금지사항 5종) | §6.4 착수. 단 주차별 표는 관측 3주 이상 쌓인 뒤 활성화되므로 데이터 축적 대기. 가이드 이미지 4장은 로컬에만 있고 미커밋 상태이며 장당 700~800KB로 리사이즈 필요 | 가이드 분량이 경쟁 문서의 36%로 아직 부족 — 관측 키워드가 늘어야 채울 근거가 생김 |
| 2026-09-09 | VS Code | §6.4 구현·배포 + 버그 1건. **중복 표시 버그(B15)**: 사용자가 '상록구 삼계탕 페이지에 3개 업체가 두 번씩 노출'이라고 제보 → 재현 후 원인이 두 겹임을 확인. 한 관측 배치에 같은 place_id 행이 중복 저장된 것이 직접 원인이고, 더 근본적으로는 **§7.4에 함정으로 이미 적어둔 중복 방어를 §7.15에서 JSON 우선 추출로 교체하며 옮기지 않은 것**이었다(폴백 경로에만 `seen` 셋이 남아 있었음). 렌더 단계 `dedupeByPlace()` + 스크래퍼 `new Set()` 양쪽 수정. **§6.4 구현**: 주차별 4주 표(관측 3주 이상 조건부, 화살표·하락 표기 없음), 키워드 유형 배지(틈새/중간/고경쟁), Answer-First 블록, 유형별 접근법 해설, 리뷰 1위와 순위 1위 비교 문단, 조건부 자동 FAQ + FAQPage 스키마, 출처 절, 상담 CTA 분리. **문구 pool이 아니라 데이터 조건이 문단·FAQ 구성을 결정**하게 해 키워드마다 실제로 다른 글이 되도록 함(934자 → 2,070~2,346자, 유형 판정도 키워드별로 갈림). 확인용 `/rank/__preview`는 더미 4주치로 화면만 미리 보여준 뒤 삭제(DB엔 미기록). **가이드 이미지**: 문서당 1장으로 정리하고 원본(750KB~1MB)을 리사이즈본(117~208KB)으로 교체, `loading="lazy"` 추가. **홈 title 개편**: `동네장사 (dongbiz.com) - 상위노출 Gap 실시간 진단` → `네이버 플레이스 순위 분석 · 내 매장 순위 확인 | 동네장사`. 브랜드·도메인이 앞자리를 먹고 아무도 검색 안 하는 'Gap'이 들어 있던 것을 목표 키워드 중심으로 재배치하고, description·og·h1까지 같은 키워드로 정렬 | 관측 3주 누적 후 주차별 표 실제 노출 확인. 서치콘솔에서 홈 URL 재색인 요청(내용이 채워진 상태라 §6.1.0 기준 문제없음). §6.4.8 KV 캐시는 미착수 | 없음 |

| 2026-09-09 | VS Code | **경쟁사 벤치마킹 2차(§6.5 신설)**: `placewizard.kr`(마케팅마법사) 전 페이지 + 가이드 문서 분석. §6.2(삐딱한마케팅)가 무료 진단 도구 경쟁자였다면 이쪽은 무료도구→상담→대행수주까지 수익 구조가 우리와 동일한 경쟁자로, 베낄 것은 기능이 아니라 **동선과 카피**로 판단. 채택 대상 정리: `/placeai.php`의 카피 문법 8가지(고객 1인칭 문제제기 / 먼저 봅니다·이렇게 합니다·**보내드립니다** 3단 고정 문법 / 역-보장 / 분리 판매 명시 / 스크린샷 출처 캡션 / **카톡 프리필 양식 URL** / 사회적 증거 / 가격 이원화), 블로그 배포 공개 단가 실측(플레이스용 1,500원·지역검색형 2,500원, 롱테일 키워드 1개당 글 1개 포장으로 단가 67% 상향). **25일 추적 기술분석 결론**: 서사일 뿐이고 §9 `rank_snapshots`+cron이 이미 같은 물건, 우리는 순위변동 시 지표 델타까지 보여줘 상위호환(§6.5.4). 착수 시 subrequest 한도 50 때문에 경량 추적 경로 필요함을 기록. 리스크 2건 기록: 웹문서→플레이스 백링크는 랭킹 근거 없으므로 "면적 확보"로 카피 전환(§6.5.5), 리워드형 트래픽은 역-보장 정직 포지셔닝과 충돌하므로 보류(§6.5.6). **네이버 검색광고 API 재검토(§6.5.7)**: `/keywordstool` 스펙·무료·Worker Web Crypto로 HMAC 가능 확인, `"< 10"` 문자열 함정과 프레임 전환책 기록, 가입 6단계 절차 문서화. 연관검색어는 기존 `ac.search.naver.com`으로 이미 무료 확보됨을 재확인(2026-09-09 실호출) — API가 필요한 건 검색량 숫자뿐 | §6.5.8 우선순위대로 ①랜딩 카피 리라이트 ②카톡 프리필 URL 착수. ③검색광고 API는 §6.1의 "기획 미포함" 결정 번복이라 **사용자 확인 선행** | 없음. 25일 추적 고객 공개 항목은 사용자 지시로 이번 정리에서 제외(분석만 §6.5.4에 보존) |
| 2026-09-09 | VS Code | **검색광고 API 백엔드 구현(§6.5.7)**. 사용자가 계정 생성·키 3종 발급 제공 → §6.1의 "기획 미포함" 결정 번복 확정. `worker/src/searchad.ts` 신설: `getKeywordVolume()`, HMAC-SHA256 서명을 Web Crypto로 처리(외부 의존성 0), `"< 10"` 문자열을 `{total:10, isUnderTen:true}`로 변환(0으로 깎으면 "수요 없음"으로 오독됨), KV 24시간 캐시(`searchad:v1:{키워드}`)로 반복조회 시 subrequest 0, 키 미설정·오류·매칭실패 전부 `null` 반환해 진단 본체 무영향(알림/캐시와 같은 방어 패턴). `index.ts`: `Env`가 `SearchAdEnv` 상속, `/api/gap` 응답에 `keywordVolume` 추가(경쟁사 직접지목 모드는 null). `.dev.vars.example`에 변수 3종 추가. `wrangler deploy --dry-run` 빌드 통과. **실호출 4건 검증 완료(§6.5.7b)**: 안산상록구맛집 810회/월(연관 71개)·안산사다리차 340회/월(연관 105개)로 우리 타겟 대역은 숫자가 정상 반환됨을 확인, 3어절 롱테일(안산상록구스카이차·수원인계동뷰티샵철거)은 실제로 `"< 10"`으로 와서 프레임 전환 필요성 확정. 부수 발견: 응답에 **연관어가 검색량과 함께 71~105개** 딸려와 자동완성(10개·검색량 없음)의 상위호환 → §2 2단계 추천 칩을 실검색량 상위 3개로 뽑는 안을 §6.5.8-5로 추가. 모바일이 PC의 3~6배 확인 | ① **wrangler 재로그인**(아래 막힌 점) 후 시크릿 3종 등록 + 배포 ② 프론트에 검색량 표시(`isUnderTen`이면 "경쟁 없음 = 1위 쉬움" 프레임으로 분기) ③ §6.5.8-5 추천 칩 교체 | **wrangler Cloudflare OAuth 토큰 만료** — `Failed to fetch auth token: 400 Bad Request` → `Not logged in`. 비대화형 환경이라 재로그인 불가. 대화형 터미널에서 `npx wrangler login` 실행하거나 `CLOUDFLARE_API_TOKEN` 설정 필요. 코드·검증은 전부 끝났고 배포만 막혀 있음 |
| 2026-09-09 | VS Code | **검색광고 API 배포 완결(§6.5.7b)**. 직전 세션에서 막혔던 wrangler OAuth 만료를 사용자가 `npx wrangler login`으로 해소(로그인은 전역 저장이라 폴더 무관 — §6.5.7b에 명시). 시크릿 3종(`NAVER_AD_CUSTOMER_ID`/`NAVER_AD_ACCESS_LICENSE`/`NAVER_AD_SECRET_KEY`) 등록 후 `secret list`로 확인, `wrangler deploy` 완료(Version `b59b6aab-fb8f-4699-8755-611f7c50dd32`). **실배포 end-to-end 검증**: `/api/gap?placeId=2058645213&keyword=안산 상록구맛집` → `keywordVolume: {pc:110, mobile:700, total:810, isUnderTen:false, competition:"높음"}` 정상 수신. 공백 포함 입력의 정규화(공백제거+대문자) 경로도 실동작 확인 — 이게 없으면 API 응답과 매칭이 안 돼 항상 null이 된다. 키 로테이션 절차(액세스라이선스·비밀키 한 쌍 갱신, `secret put` 2회, 코드수정·재배포 불필요, 교체 중에도 null 폴백이라 무장애)를 §6.5.7b에 기록 | **프론트 표시만 남았다**: `keywordVolume`을 §2 2단계 화면에 렌더링하되 `isUnderTen`이면 숫자 대신 "경쟁 없음 = 1위 쉬움" 프레임으로 분기. 이어서 §6.5.8-5(추천 칩을 실검색량 상위 3개로 교체), §6.5.8-1·2(랜딩 카피 리라이트, 카톡 프리필 URL) | 없음 |
| 2026-09-09 | VS Code | **검색량 노출 정책 고객/관리자 분리 구현(§6.5.9 신설, 사용자 지시)**. 고객 화면은 숫자 미표시, 관리자 리포트만 숫자 표기 + 상호검색량 + 대표키워드 5종 검색량까지. `searchad.ts`에 `getKeywordVolumes()` 신설(힌트 5개씩 배치, 키워드 단위 KV 24h 캐시라 조회분은 API 미호출), `KeywordVolume`에 `pcUnderTen`/`mobileUnderTen` 필드별 플래그 추가 — `< 10`을 상한 10으로 바꾼 값을 그냥 더하면 "20"이라는 없는 숫자가 생기므로 관리자 표에서 합계를 `—`로 비우기 위함. **`/api/gap`에서 `keywordVolume` 제거** — 고객이 응답 JSON을 열어도 숫자가 없도록 아예 안 내려보낸다. `/admin/:shareId/report`가 `raw_data`에서 진단 키워드+상호명+대표키워드 5개(최대 7개)를 뽑아 조회 후 `window.__REPORT_VOLUMES__`로 주입, 조회 실패해도 리포트는 정상 오픈. 프론트 `renderReportVolumes()`는 `applyReportMode()`에서만 호출돼 고객 경로에서는 실행 자체가 안 됨. 배포 완료(Version `d1ddb7d4`). 실측: 상호 "백세보리밥닭한마리"가 예상과 달리 월 140회로 잡힘 — 상호 검색량은 찍어봐야 안다 | 관리자 리포트 실화면 확인(Basic Auth라 이 세션에서 열 수 없음 — 사용자 확인 필요). 이후 §6.5.8-5(추천 칩 실검색량 교체), §6.5.8-1·2(랜딩 카피, 카톡 프리필 URL) | 없음. 단 정책 변경 전에 생성된 기존 진단 1건은 `raw_data`에 `keywordVolume`이 남아 있다(화면 렌더링은 안 되므로 무해, 신규 진단부터는 저장 안 됨) |
| 2026-09-09 | VS Code | 사용자가 관리자 리포트 표 정상 노출 확인 후 리포트 2건 요청 처리. **① 리포트 모드 입력창 유지(§6.5.9b)**: `applyReportMode()`가 `step2Input`을 통째로 숨기고 상단 상호 검색창은 빈 채로 둬서 "무슨 매장·무슨 키워드 결과인지" 알려면 스크롤해야 했음 → 입력창은 남기고 값(`storeInput`=매장명, `keywordInput`=진단 키워드)을 채운 뒤 `readOnly` 처리, `gapBtn`·`keywordChips`·1:1 비교 토글 블록만 숨김. 원칙 정립: **리포트는 조작 화면이 아니라 읽는 문서** — "맥락을 알려주는 값"은 남기고 "누를 것"만 숨긴다. **② 고객 화면 검색량 구간 문구 추가(§6.5.9 정책 개정)**: 실수치 금지는 유지하되 뭉뚱그린 한 문장은 허용하기로 사용자 지시. `describeVolume()` 신설 — 구간 내림(100/500/1,000/5,000/10,000/50,000/100,000)으로 `'○○' 키워드는 월간 조회량 500건 이상의 키워드입니다.` 생성, 100회 미만·`< 10`은 숫자를 아예 빼고 "아직 경쟁이 붙지 않은 틈새 키워드입니다. 지금 선점하면 상위 진입이 빠릅니다."로 뒤집음. **문장을 서버에서 완성해 내려보내므로 실수치가 브라우저에 도달하지 않는다** — `/api/gap` 응답은 `keywordVolumeNote` 문자열 하나뿐. 키워드 입력창 아래·1:1 비교 토글 위에 배치. 배포(Version `b1524884`) 후 실호출 검증: 안산 상록구맛집(실제 810회) → "월간 조회량 500건 이상", 응답에 실수치 미포함 확인 | 사용자 화면 확인(리포트 모드 입력창, 고객 화면 구간 문구). 이후 §6.5.8-5(추천 칩 실검색량 교체), §6.5.8-1·2(랜딩 카피, 카톡 프리필 URL) | 없음. `< 10` 틈새 분기는 코드 경로만 확인하고 실호출 미검증(하루 3건 제한을 사용자 테스트용으로 남겨둠) |
| 2026-09-09 | VS Code | **§6.6 신설 — 상호 검색량을 직접 검색 유입 추정축으로 편입(사용자 지시 4건 처리)**. ① `rank_snapshots`에 `name_search_volume` + `name_volume_under_ten` 2개 컬럼 추가(원격 D1 ALTER 적용, `schema.sql` 반영), `/api/gap`이 내 매장+상위 6곳 상호 검색량을 함께 기록. **KV 캐시를 끄고**(`{cache:false}`) 5개씩 배치로 fetch 2회만 사용 — KV도 subrequest로 잡혀서 상호 7개면 읽기/쓰기 14회가 붙는데 §9.4 한도(50)를 위협하기 때문. `waitUntil` 안에서 돌려 응답 지연 0. ② 관리자 리포트 검색량 표 하단에 **경쟁사 상호 검색량 표**(순위·상호·PC·모바일·합계·경쟁도) 추가. ③④ `/admin/analytics/:keyword`의 순위변동 이벤트를 **시점별 전 업체 표**로 재구성 — 기존엔 순위가 바뀐 업체 한 줄만 보여줘서 "한 업체만 떨어진 이유"를 추적할 수 없었음(사용자 지적). 이제 순위 변동 시점마다 표 1개, 그 시점 관측된 모든 업체를 순위순으로, 지표 6종 델타를 컬럼으로, **'월간 상호 검색량' 컬럼 포함**. 변동 업체는 노란 줄 강조, `·`=변화없음 `–`=직전 관측 없음, 최신 20개 시점만 렌더. **실배포 검증**: gap 1회 호출로 7개 업체 전부 `name_search_volume` 기록 확인(통영상회 150 / 이동고고갈비 200 / 괭이부리마을 280 / 백세보리밥 140 등). 이 검증에서 **태산김치찜이 `vol=20`으로 저장되는 걸 발견** — PC·모바일 둘 다 `< 10`인 값의 상한을 더한 "없는 숫자"(§6.5.9에서 경고했던 그 케이스가 DB에 남은 것) → `name_volume_under_ten` 플래그 컬럼을 즉시 추가해 NULL(미측정)과 `< 10`(측정했으나 하한 미만)을 분리, 화면에서는 숫자 대신 `< 10` 표기하도록 수정 후 재배포(Version `0dea8352`) | 관리자 화면 2곳 육안 확인 필요(Basic Auth라 이 세션에서 열 수 없음): 리포트 경쟁사 표, analytics 시점별 전 업체 표. analytics 표는 **순위 변동이 최소 2시점 이상 쌓여야** 나타나므로 내일 cron 이후 확인이 정확하다 | 플래그 컬럼 추가 **이전에** 기록된 06:50 배치 1건은 태산김치찜이 `20`(실제 `< 10`)으로 남아 있다. 신규 수집분부터는 정상. 소급 수정은 어느 값이 근사치였는지 되짚을 근거가 없어 하지 않음 |
| 2026-09-09 | VS Code | 사용자 피드백 2건 반영(§6.6.2·6.6.3 갱신). ① 리포트 경쟁사 표 제목을 "🔍 경쟁사 상호 월간 검색량 (관리자 전용)"으로 바꾸고 `h5 text-xs` → `h4 text-sm`으로 올려 위 표("🔍 월간 검색량 (관리자 전용)")와 글자 크기 통일 — 작게 두면 부속 정보로 읽히는데 상호 검색량은 순위와 대등한 별개 축이라 위계를 낮추면 안 됨. ② `/admin/analytics/:keyword` 시점별 표의 지표 칸을 증감만 찍던 것에서 **`1504(+4)` 현재값(증감)** 형태로 변경. 증감만 보면 "4건 늘어난 게 큰 건가"를 판단할 기준이 없음(1504 중 4건과 12 중 4건은 의미가 다름). 범례도 갱신 — 괄호 없으면 변화 없음, `–`는 값 자체가 없음. 배포 완료(Version `b6bc68a0`) | 관리자 화면 2곳 육안 확인(리포트 경쟁사 표, analytics 시점별 표). analytics 표는 순위 변동이 2시점 이상 쌓여야 나타나므로 내일 cron 이후가 정확 | 없음 |
| 2026-09-09 | VS Code | **§6.7 신설 — 상담 CTA를 이메일 리포트 신청으로 전면 교체**. 사용자 진단(전환 0건의 원인 = ①상담하면 돈 낼까 하는 두려움 ②내가 먼저 연락해야 하는 귀찮음)을 그대로 채택. `연락처 보기 → tel:` 버튼을 걷어내고 이메일 입력 한 칸 + 동의 체크박스로 교체, 문구에 "전액 무료이고 영업 전화도 드리지 않습니다" 명시. **전달 형식은 PDF 첨부가 아니라 링크로 확정** — Workers는 PDF 생성 불가, Apps Script HTML→PDF 변환기는 CSS 무시 + 우리 리포트가 JS 렌더라 변환 시 빈 페이지. 대신 메일 본문에 **결핍 상위 3개를 텍스트로** 넣어 링크를 안 눌러도 값이 남게 함(정렬은 절대 부족량이 아니라 부족 비율 순 — 리뷰 500건 부족이 맨 위면 포기하므로). **발송 수단은 기존 Apps Script 웹훅 재사용** — Resend는 계정·API키·DNS(SPF/DKIM)가 전부 따라오는데 이쪽은 무료·100통/일로 충분. `notify.ts`에 `sendCustomerEmail()` 추가, `worker/apps-script.gs` 신규 작성(설치 순서 주석 포함). 신규: `POST /api/report-email`(이메일 정규식+254자 서버 검증, `summarizeGaps()`, 텔레그램 알림), 공개 라우트 `GET /report/:shareId`, D1 `report_leads` 테이블. 관리자/고객 리포트는 `window.__REPORT_ADMIN__` 플래그 하나로 분리(고객은 검색량 표·연락처 비노출, 개선 주석은 노출). 죽은 코드 정리: `realPhone()`/`OBS_PHONE_REV`/`requestConsult()` 삭제. 배포(Version `b9f2f817`) 후 검증 — 잘못된 이메일 400, 없는 shareId 404, `/report/t24fjrpa` 200, `/report/zzzzzzzz` 404, 실제 신청 시 `report_leads` 적재 확인 | **사용자 작업 필요**: `worker/apps-script.gs`를 Apps Script에 배포하고 `wrangler secret put SHEETS_WEBHOOK_URL` 등록. 그 전까지 메일은 안 나가고 `sent=0` + 텔레그램 알림만 감 | **`SHEETS_WEBHOOK_URL` 시크릿이 애초에 등록된 적이 없었음을 발견** — §8.2-A 구글시트 콜드콜 로깅도 그동안 계속 무동작이었다. 코드가 "미설정 시 조용히 무동작"이라 아무도 몰랐음. 등록하면 두 기능이 같이 살아난다 |
| 2026-09-09 | VS Code | **§6.7 리포트 메일 발송 실동작 완료.** 사용자가 등록한 웹훅이 `sent=true`인데 메일이 안 온다는 리포트로 시작 → 웹훅을 직접 POST해 본문을 열어보니 `{"ok":false,"error":"MailApp.sendEmail을(를) 호출할 수 있는 권한이 없습니다"}`. **함정 3개를 §6.7.2b에 기록**: ① Apps Script는 스크립트가 예외를 던져도 HTTP 200을 주고 오류는 본문에만 담는다 — `res.ok`만 보던 `sendCustomerEmail()`이 실패를 성공으로 기록하고 있었음(리드가 들어와도 메일 미발송을 몇 주 모를 수 있는 종류의 버그). 본문 `ok` 필드까지 확인하도록 수정하고 `notify.ts`에 주석 고정. ② 웹 앱 배포만으로는 Gmail 스코프가 승인되지 않는다 → `apps-script.gs`에 `authorize()` 함수 추가(편집기에서 ▶ 실행하면 승인창, 본인에게 테스트 메일 + 잔여 한도 로깅). ③ 브라우저 로그인 세션 때문에 `interpiad@` 계정으로 만들려던 스크립트가 개인 계정 소유로 생성됨 — Apps Script는 **소유 계정이 곧 발신자**라 개인메일로 나간다. 사용자가 새 브라우저에서 `interpiad@`로만 로그인해 시트부터 새로 생성·배포. 새 URL로 `SHEETS_WEBHOOK_URL` 재등록 후 **실발송 성공(`sent=true`, 본문 검증 통과)**. 배포 Version `9fd0e24d` | 사용자 수신함에서 실제 메일 확인. 이후 며칠간 `report_leads`로 전환율 관측 — 0건이던 자리가 실제로 움직이는지 | 없음. 참고: `wrangler secret put`은 전파에 수 초 걸린다(첫 호출 `sent=false` → 30초 뒤 `sent=true`). §6.7.2b에 기록 |
| 2026-09-09 | VS Code | **§6.8 상호 변형 검색량 합산 구현 + §6.9 API 리스크 검토**. 사용자 지적(상호 하나만 재면 직접 검색 유입이 크게 과소평가됨)을 받아 `brandVariants()` + `autocomplete()` 신설, `GET /admin/brand-volume` 라우트와 리포트 표의 '정밀' 버튼 추가. **변형 조합 규칙을 실측으로 두 번 고쳤다**: ① 상호 전 토큰을 조합했더니 `백세보리밥 닭한마리`에서 `안산닭한마리`(월 230회)가 생성 — 이건 안산에서 닭한마리집 찾는 사람이지 우리 손님이 아님 → 한국 상호는 `[브랜드][업종/지점]` 순이므로 **첫 토큰만** 브랜드로 사용, 지점 접미사 제거, 광역 지역명(경기도) 제외하고 시/군/구/동만 사용. ② 브랜드 토큰 단독을 합산했더니 `고기굽는방앗간` 29,420회·`괭이부리마을` 22,830회가 잡힘 — 전국 지점 전체 검색량이라 한 지점에 귀속하면 588배 과대계상 → **정식 상호 대비 5배 초과 + 1,000회 이상이면 합계에서 제외**하되 목록엔 취소선으로 남겨 사람이 판단하게 함. 실측: 백세보리밥 닭한마리 140 → **320(2.3배)**. 페이지 로드가 아니라 버튼인 이유는 매장당 4 subrequest라 7곳이면 28회가 로드에 얹혀 한도(50) 초과하기 때문(요청을 쪼개면 각각 새 한도). **§6.9**: 검색 오픈API는 약관상 캐싱조차 금지라 우리 방식과 충돌하지만 검색광고 API는 별개이고 경쟁사 다수가 공개 운영 중 — 사실상 묵인이나 보장은 아님. 리스크 순서는 429 → 계정정지 → 약관분쟁이고, 공개하려면 KV 캐시 + IP당 일일 한도(§9.8 복제) + D1 축적 셋이 필수라고 결론. 배포 Version `b6d22ffe` | 관리자 리포트에서 '정밀' 버튼 실동작 확인(Basic Auth라 이 세션에서 불가). 검색량 공개 도구는 §6.9 방어 3종을 갖춘 뒤 착수 여부 결정 | 없음. 워크스페이스 이전은 100통 한도 경고가 뜰 때로 사용자가 보류 확정 |
| 2026-09-09 | VS Code | **§6.8 변형 합산 3·4차 수정(사용자 실사용 리포트)**. ③ `인형수선 안나`에서 `인형수선`(월 680회)이 합산됨 — 업종 키워드지 브랜드가 아님. "첫 토큰=브랜드" 규칙이 `[업종][브랜드]` 형태 상호에서 깨진다. 자동 판별 시도 전부 실패: 네이버 카테고리 대조는 실측값이 `한식`·`육류,고기요리`처럼 넓어서 신호가 없고, 5배 규칙은 `고기굽는방앗간`(29,420)은 걸렀지만 `인형수선`(680)은 1,000회 미만이라 통과. → **자동 판별 포기, 사람이 판단**: 브랜드 토큰 단독 행은 기본 합산 제외(`?` 표시)로 두고 체크박스로 켜면 합계가 즉시 재계산되도록 변경. 설계 원칙을 §6.8.1b에 명문화 — 관리자 상담 도구에서는 애매한 판정을 자동화하다 틀리는 것보다 재료를 다 보여주고 사람이 고르게 하는 편이 낫다(확신에 찬 틀린 숫자는 상담에서 신뢰를 잃는다). ④ 서울 업체인 `인형수선 안나`에 `안산인형수선` 변형이 붙음 — 프론트가 **내 매장 주소를 전 업체에 돌려쓰고** 있었음. 경쟁사가 같은 상권일 땐 안 드러나다 타지역 업체가 순위에 들면 깨짐. `raw_data`에 각 업체 roadAddress가 이미 있어 `window.__REPORT_REGIONS__`로 매장별 주소를 내려보내 각자 지역으로 조합. 그 전에 표 레이아웃도 수정 — 변형 목록을 마지막 열에 넣으니 표가 밀려서 아래 전체 폭 줄(colspan)로 분리하고 숫자 열에 줄바꿈 금지 적용. `cell.innerHTML` 교체 후 `btn.closest('tr')`을 호출해 null 참조로 터지던 버그도 수정(교체 시점에 버튼이 DOM에서 분리됨 — 참조를 교체 **전에** 잡도록 순서 변경, 주석 고정). 배포 Version `2e55dd16` | 체크박스 켜고 끌 때 합계 재계산 실동작 확인, 타지역 업체에서 지역 조합이 그 업체 주소로 붙는지 확인 | 없음 |
| 2026-09-09 | VS Code | **§9.10 pcmap 목록 수집 파이프라인 신설 — §7.10·§7.5 동시 해결.** 사장님 이론(브랜드 검색 유입이 순위 최대 가중치, 저장은 어뷰징으로 페널티) 검증에 필요한 저장수 수집 가능 여부를 조사하다 `pcmap.place.naver.com/{업종}/list`의 `__APOLLO_STATE__`에 `saveCount`가 있음을 확인. 부수 성과가 더 컸다: **1회 요청에 오가닉 56곳**(기존 6곳) + 광고 21곳이 **타입으로 분리**(`*AdSummary`)돼 §7.5의 취약한 `<a href>` 패턴 판별이 불필요해짐. 매장당 saveCount·방문리뷰·블로그리뷰·총리뷰·이미지수·예약·톡톡·좌표를 함께 준다. **업종별 정렬 축이 다름을 실측**: restaurant는 `saved`(+saveCount), hairshop은 `revisit`(재방문) — 미용실 revisit은 우리가 못 재는 "보이지 않는 지표"의 대리변수로 쓸 수 있는 예상 밖 수확. **🚨 조사 중 429를 실측**(6회 연속 호출) — 원래 계획한 "8키워드×3정렬=24회"를 몰아치면 즉사하므로 설계를 바꿈: 요청 간 3초 스로틀, **크론 단독 슬롯 신설**(`0 6 * * *`=15:00 KST, wrangler.toml·CRON_TIMES 동시 반영), 정렬 3축을 모아 **키워드당 D1 쓰기 1회**로 축소(21 fetch + 7 batch = 28 subrequest). `rank_snapshots`에 `sort_mode`·`save_count_raw`·`save_count_min`·`is_ad`·`image_count`·`total_review_count` 6개 컬럼 추가(원격 D1 ALTER 완료). `scraper.ts`에 `getPlaceList()` 신설(중괄호 균형 파싱 — 정규식으로 자르면 본문 `}`에 깨진다), 수동 트리거 `/admin/cron/run/place-lists?keyword=` 추가. 저장해둔 HTML로 파서 오프라인 검증(429 회피): 77건 파싱 → 광고 21 / 오가닉 56, 상위 6곳 순서가 기존 데이터와 정확히 일치. **첫 관측이 흥미롭다** — `안산 상록구맛집`에서 저장 최다(49,000+)가 6위, 저장 최소(~100)가 1위, 방문리뷰도 역방향. n=6 단일시점이라 결론은 아니지만 "저장 올리면 순위 오른다"는 단순 모델과 불일치하고 사장님 어뷰징 페널티 가설과는 방향이 같다. 사용자 결정으로 큐레이션 저장(②)·교란변수 태깅(③)은 제외 — 분석을 within-store 시계열로 가면 매장 고유 특성이 자동 상쇄돼 태깅이 불필요해진다는 판단 반영. 배포 Version `9089909d` | 내일 15:00 KST 첫 크론 자동 수집 확인. **§9.10.6 순서 보장 검증이 다음 과제** — 지금은 APOLLO 객체 키 순서를 순위로 쓰고 있고 상위 6곳만 대조했다(§7.15와 같은 함정). 20위 이하는 아직 신뢰 금지. 429 풀린 뒤 `/place/list` 등 비음식점 경로 확인 | 429 실측으로 조사 중단. 공방 등 일반 업종 경로 미확인 |
| 2026-09-09 | VS Code | **관리자 차트 버그 수정 + §9.11 가설 검증 장치 착수.** ① **§9.10 도입으로 생긴 버그 발견·수정**: 한 키워드에 정렬 3축(popular/saved/trendy)이 쌓이는데 `/admin/analytics/:keyword`가 이를 구분 없이 한 선으로 그려서, 저장순 순위와 관련도순 순위가 같은 계열에 얹히고 있었다(화면에 19:56·19:57 두 점으로 드러남). 쿼리에 `sort_mode` 필터 추가(구버전 NULL 행은 popular로 취급), 제목 아래 정렬축 전환 링크 배치. ② 차트 가독성: 업체가 95곳까지 잡혀 선이 전부 겹치고 범례가 화면 절반을 먹던 문제 → **상위 12위까지만 렌더**(`?top=N`으로 조절, 하위권은 표에 그대로 유지), y축을 그린 범위에 맞춤, **범례 제거**(오른쪽 축이 이미 현재 순위별 업체명을 같은 높이에 표시하므로 중복). ③ **§9.11 신설 — 반증 가능한 가설 2건에 관측 장치 부착**. 신규오픈 부스트: `기운`(2019121430)이 방문리뷰 59·블로그 306·투표 260으로 주변 대비 바닥인데 3위이고 `is_new_opening=1` 확인 → 배수("3배")는 못 재지만 **부스트가 꺼지는 시점**은 잴 수 있고 지속기간이 처방에 더 유용하다고 판단, `WATCH_PLACE_IDS` 워치리스트 신설(매일 개별 스크랩, `sort_mode='watch'`·rank NULL, 목록에 없는 is_new_opening·투표수·사진수·설명분량만 기록). 재방문 가설: 사용자 지정 `봉명동 미용실`을 `LIST_ONLY_KEYWORDS`로 추가해 popular/revisit 순위를 매일 함께 수집 — 두 순위의 잔차가 재방문 기여도 단서. **`FIXED_RESEARCH_KEYWORDS`에 넣지 않은 이유**: 거기 넣으면 fixed-a/fixed-b의 비싼 개별 스크랩 배치까지 늘어나 §9.4b 한도를 넘긴다. PLACE_LISTS 슬롯 예산 재계산: 24 fetch + 8 batch + 2 watch = 34 subrequest(한도 50). 사용자 결정으로 진단 경로(`/api/gap`)는 6곳 유지(A안) — §2/B15의 "지도 1페이지 실노출 개수" 기준이 평균·진입선의 근거라 조용히 바꾸면 진단 결과가 통째로 달라진다. 배포 Version `afed9cda` | 내일 15:00 KST 크론에서 봉명동 미용실 revisit·워치리스트 첫 수집 확인. 기운 순위 감쇠 관측 시작 — 미끄러지기 시작하는 날이 부스트 감쇠 시작 시점 | 없음 |
| 2026-09-09 | VS Code | **관리자 메뉴 툴팁 + §9.11 측정 목표 수정(사용자 도메인 지식 반영).** ① 메뉴 이름만으로 동작을 알 수 없다는 지적 → 전 메뉴에 `ⓘ` 아이콘과 `title` 툴팁 부착(`navItem()` 헬퍼 신설). 특히 수동 수집 4개는 **누르면 즉시 네이버를 긁으므로** 목적·자동 실행 주기·주의사항(연타 금지, 소요 75초)까지 명시 — 고정A/B가 앞뒤 4개로 나뉜 이유(한 번에 8개면 요청 한도 초과)도 툴팁에 넣었다. ② **신규오픈 부스트 측정 목표를 "지속기간"에서 "만료 시 낙폭"으로 변경.** 사용자가 네이버 공식 조건(검수 완료일로부터 최대 90일, 조건 3가지)과 실사례(백세보리밥: 개업 4/21·신청 5/1·하락 8월 첫째주) 제공 → **신청일+90=7/30이 실제 하락과 부합하고 개업일+90=7/20은 불일치, 즉 "검수 완료일 기준"이 실증됨.** 지속기간은 이미 답이 있으므로 잴 필요가 없고, **만료 순간엔 다른 지표가 안 변하므로 그때의 낙폭이 곧 부스트 크기**라는 설계로 전환(§9.11.1). 네이버 점수를 못 봐도 만료 전후로 지나쳐간 업체들의 지표 차이가 환산 단위가 된다. 과거 역산은 불가 — rank_snapshots가 09-07부터라 8월 데이터가 없다. 노출 조건 3가지(90일 내 등록·사업자등록증·가격정보+사진)는 **신규 매장 처방 체크리스트**로 §2 3단계에 쓸 수 있다고 기록. ③ **§9.11.4 신설 — "순위는 3일에 걸쳐 쿠션을 주며 떨어진다"**(사용자 실측, 저품질 판정도 동일). 하루 1점으로는 3일 램프를 3점으로 뭉개므로 워치리스트를 15/17/21시 세 슬롯에서 실행(하루 3점, 램프당 9점). **크론 전용 슬롯 신설을 시도했으나 트리거 4개가 계정 상한이라 배포 거부됨** — 기존 슬롯에 얹는 방식으로 전환(워치 1곳당 2 subrequest라 부담 없음). 부수 효과로 Phase D 하락 알림 트리거를 "1일 급락"이 아니라 **"3일 연속 단조 하락"**으로 잡으면 오탐이 준다는 설계 지침도 기록. 배포 Version `1cdcc3ba` | 기운의 `is_new_opening`이 0으로 바뀌는 시점 포착이 최우선 관측 목표. 그 전후 며칠이 전체 관측에서 가장 값진 구간 | 크론 트리거 4개 상한 확인(5개 시도 시 `/schedules` API 거부). 앞으로 새 주기 작업은 기존 슬롯에 얹어야 한다 |
| 2026-09-09 | VS Code | **§9.12 `keyword_volumes` 신설 — 검색량 시계열 저장.** 사용자 질문("경쟁사 상호 검색량 + 변종 키워드 검색량까지 저장되는가")을 코드로 확인한 결과 **반만 저장되고 있었다**: 정식 상호는 `rank_snapshots.name_search_volume`에 남지만(진단 경로만), **변형 검색어·합산·진단 키워드·대표키워드 검색량은 화면에 뿌리고 전부 폐기**. 사용자 이론의 핵심 축인 브랜드 검색 유입의 시계열이 안 쌓이고 있었고, 단면만으로는 §9.6의 역인과를 영영 구분 못 한다. → `keyword_volumes` 테이블 신설(원격 D1 + `schema.sql` 반영, `norm_keyword` 인덱스). `getKeywordVolumes()`가 모든 조회의 병목이라 거기 한 곳에서 기록하고, **API를 새로 탄 것만** 남긴다(캐시 히트는 중복) — KV 24h 캐시 덕에 자연히 하루 1점이 된다. `SearchAdEnv`에 선택적 `DB` 바인딩 추가, 4개 호출부에 `context`(gap-keyword/gap-store-name/report/brand-variant)와 `related_to` 부착. **`related_to`가 핵심**: 변형이 어느 상호에서 파생됐는지 남으므로 사람 판단(체크박스)을 저장하지 않아도 분석 시점에 "이 매장의 변형 후보 전체"를 복원해 다시 고를 수 있다 — 사용자가 ②를 뺀 결정이 이걸로 무해해졌다. 죽은 코드 정리: 호출부가 사라진 `getKeywordVolume()` 단축형 삭제. 배포 Version `ebf62ca9` | **첫 행 적재 확인 필요.** 기존 키워드는 전부 KV 캐시(24h)에 있어 캐시 히트로 빠지므로 당장은 안 쌓인다 — 리포트에서 아직 안 눌러본 매장에 '정밀'을 누르거나 내일 캐시 만료 후 자동 적재됨 | 없음 |
| 2026-09-11 | Codex | 수집 안정화 1차 로컬 구현: 오류 분류·시간 초과·동시 요청 병합·ID 캐시 통합·공개 테스트 경로 차단. 회귀 검사 11개, 타입 검사, dry-run 빌드 통과. 운영 배포/DB/KV 변경 없음 | 상세 인계는 0911-수집안정화-진행상태.md 참조. 실환경 확인과 롤백 버전 확보 후 운영 반영을 별도 진행 | 운영 장애 원인 확정 및 분산 전역 요청 제어는 미완료 |
| 2026-09-11 | Codex | 사용자 승인으로 b87d6ff 운영 배포 완료(e53d8f53-255a-4b40-9943-d097607c33c8). 홈페이지 200, 공개 테스트 404, 지정 매장 Step 1 실수집 200 확인 | Step 2는 기존 일일 IP 한도 429로 실수집 미검증. 인계 문서에 기록 | 즉시 롤백 대상 33c572e7-9f21-4527-8f80-9090603d0ef4 확보 |
| 2026-09-11 | Codex | 사용자 제공 제한 우회 권한으로 지정 매장 Step 2 실검증 완료: 안산 상록구맛집, 200, 경쟁사 6곳. 공유 h4sy9r16 저장·복원 데이터 일치 | 다음 범위는 검색 정확도/순위 해석/데이터 분류 개선 | 토큰 기록 없음, 해당 표본만 검증 |

| 2026-09-11 | Codex | 검색 개선 52cf0ca 배포(b69bccbc-a56d-444e-a7ae-677da2fdf3f7). Step 1 동일 상호 후보 선택, Step 2 지도 목록 최대350 순차 수집·출처/범위/시각/내 매장 순위 저장, 실패 미차감, UI 개선. 자동검사19개·타입·빌드·브라우저 검증 통과. 운영 실측300곳/비교6/36초, 공유 lyetkxex 복원 일치 | 상세 0911-검색개선-진행상태.md. 대상 매장 수집300곳 내 미발견이며 전체 순위 밖으로 해석 금지 | 롤백 e53d8f53-255a-4b40-9943-d097607c33c8. 공급자 목록 종료로 이 표본은300개. 전역 분산 요청 제어 및 업종별 확대 검증은 별도 |
| 2026-09-11 | Codex | 인계 문서와 실제 구현 대조: Phase D pSEO는 §6.1.5대로 이미 완료됐으나 WBS 체크만 누락된 상태여서 `[x]`로 정정. 검색 개선 배포본 회귀 검사·타입 검사·dry-run 재검증 | 다음 구현 후보는 순위 하락 알림. 수신 대상·구독 상태가 아직 없어 운영자 연구 알림인지 고객 SaaS 알림인지 결정 후 착수 | 사용자 미추적 조사 파일은 보존, 운영 배포 없음 |
| 2026-09-11 | Codex | 기존 수집값 고객 노출 확장: Step1 예약·스마트오더·쿠폰·방문자리뷰 첨부 미디어 수/리뷰당 개수, Step2 리뷰당 첨부 미디어 비교 카드와 경쟁사 평균 직선거리 추가. 운영 실측 1,959 미디어/880 리뷰로 `visitorReviewMediasTotal`은 사진리뷰 건수가 아닌 사진·영상 총개수임을 확인해 즉시 교정. 블로그/카페 리뷰 제외 | 교정판 배포 후 운영 표본 확인 | 추가 외부 요청·DB·비교수 변경 없음 |
| 2026-09-11 | Codex | 첨부 미디어 의미 교정 5d96861 배포(11d6fc96). 운영 재검증: 1,959/880=리뷰당2.23개, 경쟁사5곳 평균1.1개, 평균직선거리2.9km, 구 비율 필드 제거 확인 | 모바일 실화면 문구 확인 | 롤백 6b430a94, DB/추가 외부요청 없음 |
| 2026-09-11 | Codex | 상위 6곳 비교 유지 + 음식점 저장수 5축 차트 로컬 구현. 저장수 공개 표본 5/6 이상일 때만 표시하고 null은 0이 아닌 결측으로 제외, 표본 수와 하한값 성격 고지. 회귀 19개·타입·인라인 JS·dry-run 통과 | 해당 파일만 커밋 후 발견 사례 운영 검증·배포 확인 | DB/크론 변경 없음, 기존 공유 결과는 4축으로 하위 호환 |
| 2026-09-11 | Codex | 저장수 1차 배포 후 운영 표본에서 상세페이지 값이 전부 null임을 발견. 다른 정렬축을 섞지 않고 기본 지도순위 API 동일 행의 saveCount를 수집·결합하도록 보완, 목록 캐시 v2. 도배솔루션/안산시 도배는 수집 목록 18위·D1·공유복원 일치로 발견 사례 검증 완료 | 보완판 배포 후 음식점 저장수 5축 실표본 확인 | 1차 배포 6c75326c, DB/크론 변경 없음 |
| 2026-09-11 | Codex | 저장수 보완판 2a83943 배포(252549dd). 음식점 실표본에서 기본목록 1위·저장수 공개 5/5·5축 및 저장수 비교 카드 표시·공유복원 확인. 일반 업종 도배솔루션은 18위 저장·복원 일치, 저장수 미공개로 4축 유지 | 모바일 헤더/1:1 토글 개선 또는 미발견 원인 세분화 | 롤백 6c75326c. 우회 값은 검증에만 사용, 미기록. DB/크론 변경 없음 |
| 2026-09-11 | Codex | Step 2 장시간 무응답처럼 보이던 UX 개선: 진행 막대 + 정상 진행 문구 + 50곳 단위 예상 확인 구간 + 경과시간 표시, 완료/실패 시 정리. 실제 서버 퍼센트는 단일 JSON 응답 구조라 허위 표기하지 않음. 회귀19·타입·인라인JS·dry-run 통과 | 커밋·배포 후 모바일 진행 표시 확인 | DB/API/수집 로직 변경 없음 |
| 2026-09-11 | Codex | Step 2 진행 표시 a09779d 배포(b573d308). 운영 홈페이지 200, 진행 영역·정상 진행 문구·50곳 단위 예상 구간·모션 감소 설정 반영 확인 | 실제 장시간 검색 중 모바일 체감 확인 | 롤백 252549dd, DB/API/수집 로직 변경 없음 |
| 2026-09-12 | Codex | **키워드 검색량 공개 도구 배포**: `/keyword-volume` + `/api/keyword-volume` 추가, 홈페이지·공통 메뉴·푸터 연결. 검색광고 API의 월간 PC·모바일·합계와 관련 키워드 태그(첫 30개·더 보기)를 표시하고, 태그 재조회·이전 검색·조회/빈결과/오류 상태·늦은 응답 무시를 구현. `< 10`은 `10 미만`, 포함 합계는 `합계 확정 불가`로 표시. 기존 `getKeywordVolumes()`는 그대로 두고 관련어 보존 전용 함수를 추가했다. 전용 KV 24시간 캐시 + IP별 시간당 30회 제한으로 플레이스 진단 일 3회와 분리, API 키는 서버에만 둠 | 추이 그래프·데이터랩 API는 범위 밖으로 미구현. `keyword_volumes`는 공개 검색마다 수백 관련어를 저장하지 않기 위해 미사용 | 코드 `830d519`, 운영 `3e600a4a-db4b-4d0a-bb90-72b98c2bf457`, 롤백 `bb274337-e643-4273-8ae4-a78ce8ca053b`. 회귀 21개·dry-run·운영 API/화면 확인 |
